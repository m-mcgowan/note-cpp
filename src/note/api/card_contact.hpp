// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once
#include <note/note_config.hpp>

// NOTE_EXTRAS default is set in note_config.hpp.
#if NOTE_EXTRAS
#include <note/dyn_field.hpp>
#endif
#include <note/generic_builder.hpp>
#include <note/generic_sink.hpp>
#include <note/notecard.hpp>
#include <note/arena.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/binary_request.hpp>
#include <note/print.hpp>
#include <note/response_release.hpp>
#include <note/safety.hpp>
#include <note/string_pool.hpp>
#include <note/types.hpp>
#include <note/progmem.hpp>
#include <note/target.hpp>

namespace note::api {









/// Used to set or retrieve information about the Notecard maintainer. Once set,
/// this information is synced to Notehub.
///
/// @skus{CELL,CELL+WIFI,SKYLO,WIFI}
struct CardContact {

    struct Get {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.contact";
            static constexpr char email[] NOTE_FLASH_ATTR = "email";
            static constexpr char name[] NOTE_FLASH_ATTR = "name";
            static constexpr char org[] NOTE_FLASH_ATTR = "org";
            static constexpr char role[] NOTE_FLASH_ATTR = "role";
            static constexpr char rsp_email[] NOTE_FLASH_ATTR = "email";
            static constexpr char rsp_name[] NOTE_FLASH_ATTR = "name";
            static constexpr char rsp_org[] NOTE_FLASH_ATTR = "org";
            static constexpr char rsp_role[] NOTE_FLASH_ATTR = "role";
        };

        static constexpr string_view notecard_request = "card.contact";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::ReadOnly;
        static constexpr RadiosSupport radios = RadiosSupport::from(Radios::Cell, Radios::CellWifi, Radios::Skylo, Radios::WiFi);
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// Set the email address of the Notecard maintainer.
        struct email_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// Set the email address of the Notecard maintainer.
            CardContact::Get& operator()(note::string_view v);
        } email{};
        /// Set the name of the Notecard maintainer.
        struct name_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// Set the name of the Notecard maintainer.
            CardContact::Get& operator()(note::string_view v);
        } name{};
        /// Set the organization name of the Notecard maintainer.
        struct org_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// Set the organization name of the Notecard maintainer.
            CardContact::Get& operator()(note::string_view v);
        } org{};
        /// Set the role of the Notecard maintainer.
        struct role_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// Set the role of the Notecard maintainer.
            CardContact::Get& operator()(note::string_view v);
        } role{};


#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "email") return note::dyn_field_for(email);
            if (k_ == "name") return note::dyn_field_for(name);
            if (k_ == "org") return note::dyn_field_for(org);
            if (k_ == "role") return note::dyn_field_for(role);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Successful response
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(65) +
                ::note::detail::arena_cost(49) +
                ::note::detail::arena_cost(49) +
                ::note::detail::arena_cost(33) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// Email address of the Notecard maintainer.
            note::ResponseField<note::string_view> email{};
            /// Name of the Notecard maintainer.
            note::ResponseField<note::string_view> name{};
            /// Organization name of the Notecard maintainer.
            note::ResponseField<note::string_view> org{};
            /// Role of the Notecard maintainer.
            note::ResponseField<note::string_view> role{};

            /// Allocator that minted this Response's interned string fields,
            /// attached by Notecard execute paths when the Response is parsed.
            /// Empty == no cleanup needed (default-constructed Response, or a
            /// tree-mode parse with no `set_allocator` configured).
            ::note::AllocatorRef alloc_;

            ~Response() {
                if (!alloc_) return;
#if NOTE_RESPONSE_RELEASE_LOOP
                ::note::detail::release_string_fields(*alloc_,
                    &email,
                    4);
#else
                ::note::detail::deallocate_if_present(*alloc_, email.value());
                ::note::detail::deallocate_if_present(*alloc_, name.value());
                ::note::detail::deallocate_if_present(*alloc_, org.value());
                ::note::detail::deallocate_if_present(*alloc_, role.value());
#endif
            }

            // Move-only: copying a Response would alias the interned-string
            // ownership and double-free on the second destruction. AllocatorRef
            // nulls the source's pointer so only the live owner runs cleanup.
            Response() = default;
            Response(Response&&) noexcept = default;
            Response& operator=(Response&& o) noexcept {
                if (this != &o) {
                    this->~Response();
                    ::new (this) Response(::std::move(o));
                }
                return *this;
            }
            Response(const Response&) = delete;
            Response& operator=(const Response&) = delete;

#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("email")) rsp.email = reader_->get_string("email");
                if (reader_->has("name")) rsp.name = reader_->get_string("name");
                if (reader_->has("org")) rsp.org = reader_->get_string("org");
                if (reader_->has("role")) rsp.role = reader_->get_string("role");
                rsp.reader_ = std::move(reader_);
                return rsp;
            }

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("email")) rsp.email = reader_.get_string("email");
                if (reader_.has("name")) rsp.name = reader_.get_string("name");
                if (reader_.has("org")) rsp.org = reader_.get_string("org");
                if (reader_.has("role")) rsp.role = reader_.get_string("role");
                return rsp;
            }
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_email) == k_) { rsp.email = v_; return; }
                    if (note::flash(keys_::rsp_name) == k_) { rsp.name = v_; return; }
                    if (note::flash(keys_::rsp_org) == k_) { rsp.org = v_; return; }
                    if (note::flash(keys_::rsp_role) == k_) { rsp.role = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };

            void intern_strings(::note::StringPool& pool) {
                if (!email.empty()) email = pool.intern(email);
                if (!name.empty()) name = pool.intern(name);
                if (!org.empty()) org = pool.intern(org);
                if (!role.empty()) role = pool.intern(role);
            }

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"email\":");
                n += note::detail::print_json_value(p, email.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"name\":");
                n += note::detail::print_json_value(p, name.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"org\":");
                n += note::detail::print_json_value(p, org.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"role\":");
                n += note::detail::print_json_value(p, role.value());
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardContact::Get&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (email) {
                n += p.print(",\"email\":");
                n += note::detail::print_json_value(p, *email);
            }
            if (name) {
                n += p.print(",\"name\":");
                n += note::detail::print_json_value(p, *name);
            }
            if (org) {
                n += p.print(",\"org\":");
                n += note::detail::print_json_value(p, *org);
            }
            if (role) {
                n += p.print(",\"role\":");
                n += note::detail::print_json_value(p, *role);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Used to set or retrieve information about the Notecard maintainer. Once
    /// set, this information is synced to Notehub.
    ///
    /// @skus{CELL,CELL+WIFI,SKYLO,WIFI}
    struct Set {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.contact";
            static constexpr char email[] NOTE_FLASH_ATTR = "email";
            static constexpr char name[] NOTE_FLASH_ATTR = "name";
            static constexpr char org[] NOTE_FLASH_ATTR = "org";
            static constexpr char role[] NOTE_FLASH_ATTR = "role";
            static constexpr char rsp_email[] NOTE_FLASH_ATTR = "email";
            static constexpr char rsp_name[] NOTE_FLASH_ATTR = "name";
            static constexpr char rsp_org[] NOTE_FLASH_ATTR = "org";
            static constexpr char rsp_role[] NOTE_FLASH_ATTR = "role";
        };

        static constexpr string_view notecard_request = "card.contact";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios = RadiosSupport::from(Radios::Cell, Radios::CellWifi, Radios::Skylo, Radios::WiFi);
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// Set the email address of the Notecard maintainer.
        struct email_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// Set the email address of the Notecard maintainer.
            CardContact::Set& operator()(note::string_view v);
        } email{};
        /// Set the name of the Notecard maintainer.
        struct name_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// Set the name of the Notecard maintainer.
            CardContact::Set& operator()(note::string_view v);
        } name{};
        /// Set the organization name of the Notecard maintainer.
        struct org_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// Set the organization name of the Notecard maintainer.
            CardContact::Set& operator()(note::string_view v);
        } org{};
        /// Set the role of the Notecard maintainer.
        struct role_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// Set the role of the Notecard maintainer.
            CardContact::Set& operator()(note::string_view v);
        } role{};


#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "email") return note::dyn_field_for(email);
            if (k_ == "name") return note::dyn_field_for(name);
            if (k_ == "org") return note::dyn_field_for(org);
            if (k_ == "role") return note::dyn_field_for(role);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Successful response
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(65) +
                ::note::detail::arena_cost(49) +
                ::note::detail::arena_cost(49) +
                ::note::detail::arena_cost(33) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// Email address of the Notecard maintainer.
            note::ResponseField<note::string_view> email{};
            /// Name of the Notecard maintainer.
            note::ResponseField<note::string_view> name{};
            /// Organization name of the Notecard maintainer.
            note::ResponseField<note::string_view> org{};
            /// Role of the Notecard maintainer.
            note::ResponseField<note::string_view> role{};

            /// Allocator that minted this Response's interned string fields,
            /// attached by Notecard execute paths when the Response is parsed.
            /// Empty == no cleanup needed (default-constructed Response, or a
            /// tree-mode parse with no `set_allocator` configured).
            ::note::AllocatorRef alloc_;

            ~Response() {
                if (!alloc_) return;
#if NOTE_RESPONSE_RELEASE_LOOP
                ::note::detail::release_string_fields(*alloc_,
                    &email,
                    4);
#else
                ::note::detail::deallocate_if_present(*alloc_, email.value());
                ::note::detail::deallocate_if_present(*alloc_, name.value());
                ::note::detail::deallocate_if_present(*alloc_, org.value());
                ::note::detail::deallocate_if_present(*alloc_, role.value());
#endif
            }

            // Move-only: copying a Response would alias the interned-string
            // ownership and double-free on the second destruction. AllocatorRef
            // nulls the source's pointer so only the live owner runs cleanup.
            Response() = default;
            Response(Response&&) noexcept = default;
            Response& operator=(Response&& o) noexcept {
                if (this != &o) {
                    this->~Response();
                    ::new (this) Response(::std::move(o));
                }
                return *this;
            }
            Response(const Response&) = delete;
            Response& operator=(const Response&) = delete;

#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("email")) rsp.email = reader_->get_string("email");
                if (reader_->has("name")) rsp.name = reader_->get_string("name");
                if (reader_->has("org")) rsp.org = reader_->get_string("org");
                if (reader_->has("role")) rsp.role = reader_->get_string("role");
                rsp.reader_ = std::move(reader_);
                return rsp;
            }

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("email")) rsp.email = reader_.get_string("email");
                if (reader_.has("name")) rsp.name = reader_.get_string("name");
                if (reader_.has("org")) rsp.org = reader_.get_string("org");
                if (reader_.has("role")) rsp.role = reader_.get_string("role");
                return rsp;
            }
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_email) == k_) { rsp.email = v_; return; }
                    if (note::flash(keys_::rsp_name) == k_) { rsp.name = v_; return; }
                    if (note::flash(keys_::rsp_org) == k_) { rsp.org = v_; return; }
                    if (note::flash(keys_::rsp_role) == k_) { rsp.role = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };

            void intern_strings(::note::StringPool& pool) {
                if (!email.empty()) email = pool.intern(email);
                if (!name.empty()) name = pool.intern(name);
                if (!org.empty()) org = pool.intern(org);
                if (!role.empty()) role = pool.intern(role);
            }

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"email\":");
                n += note::detail::print_json_value(p, email.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"name\":");
                n += note::detail::print_json_value(p, name.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"org\":");
                n += note::detail::print_json_value(p, org.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"role\":");
                n += note::detail::print_json_value(p, role.value());
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardContact::Set&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (email) {
                n += p.print(",\"email\":");
                n += note::detail::print_json_value(p, *email);
            }
            if (name) {
                n += p.print(",\"name\":");
                n += note::detail::print_json_value(p, *name);
            }
            if (org) {
                n += p.print(",\"org\":");
                n += note::detail::print_json_value(p, *org);
            }
            if (role) {
                n += p.print(",\"role\":");
                n += note::detail::print_json_value(p, *role);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };
};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardContact::Get& CardContact::Get::email_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Get, email));
}
inline CardContact::Get& CardContact::Get::name_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Get, name));
}
inline CardContact::Get& CardContact::Get::org_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Get, org));
}
inline CardContact::Get& CardContact::Get::role_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Get, role));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardContact::Get> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardContact::Get::keys_::rsp_email, static_cast<uint16_t>(offsetof(::note::api::CardContact::Get::Response, email)), ::note::FieldType::String},
        {::note::api::CardContact::Get::keys_::rsp_name, static_cast<uint16_t>(offsetof(::note::api::CardContact::Get::Response, name)), ::note::FieldType::String},
        {::note::api::CardContact::Get::keys_::rsp_org, static_cast<uint16_t>(offsetof(::note::api::CardContact::Get::Response, org)), ::note::FieldType::String},
        {::note::api::CardContact::Get::keys_::rsp_role, static_cast<uint16_t>(offsetof(::note::api::CardContact::Get::Response, role)), ::note::FieldType::String},
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardContact::Get::keys_::email, static_cast<uint16_t>(offsetof(::note::api::CardContact::Get, email)), ::note::ReqFieldType::String},
            {::note::api::CardContact::Get::keys_::name, static_cast<uint16_t>(offsetof(::note::api::CardContact::Get, name)), ::note::ReqFieldType::String},
            {::note::api::CardContact::Get::keys_::org, static_cast<uint16_t>(offsetof(::note::api::CardContact::Get, org)), ::note::ReqFieldType::String},
            {::note::api::CardContact::Get::keys_::role, static_cast<uint16_t>(offsetof(::note::api::CardContact::Get, role)), ::note::ReqFieldType::String},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardContact::Get::build(JsonBuilder& b) const {
    using meta_ = ::note::detail::request_traits<CardContact::Get>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<typename CardContact::Get::Response> CardContact::Get::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardContact::Get>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardContact::Get::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardContact::Get>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardContact::Set& CardContact::Set::email_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Set, email));
}
inline CardContact::Set& CardContact::Set::name_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Set, name));
}
inline CardContact::Set& CardContact::Set::org_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Set, org));
}
inline CardContact::Set& CardContact::Set::role_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Set, role));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardContact::Set> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardContact::Set::keys_::rsp_email, static_cast<uint16_t>(offsetof(::note::api::CardContact::Set::Response, email)), ::note::FieldType::String},
        {::note::api::CardContact::Set::keys_::rsp_name, static_cast<uint16_t>(offsetof(::note::api::CardContact::Set::Response, name)), ::note::FieldType::String},
        {::note::api::CardContact::Set::keys_::rsp_org, static_cast<uint16_t>(offsetof(::note::api::CardContact::Set::Response, org)), ::note::FieldType::String},
        {::note::api::CardContact::Set::keys_::rsp_role, static_cast<uint16_t>(offsetof(::note::api::CardContact::Set::Response, role)), ::note::FieldType::String},
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardContact::Set::keys_::email, static_cast<uint16_t>(offsetof(::note::api::CardContact::Set, email)), ::note::ReqFieldType::String},
            {::note::api::CardContact::Set::keys_::name, static_cast<uint16_t>(offsetof(::note::api::CardContact::Set, name)), ::note::ReqFieldType::String},
            {::note::api::CardContact::Set::keys_::org, static_cast<uint16_t>(offsetof(::note::api::CardContact::Set, org)), ::note::ReqFieldType::String},
            {::note::api::CardContact::Set::keys_::role, static_cast<uint16_t>(offsetof(::note::api::CardContact::Set, role)), ::note::ReqFieldType::String},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardContact::Set::build(JsonBuilder& b) const {
    using meta_ = ::note::detail::request_traits<CardContact::Set>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<typename CardContact::Set::Response> CardContact::Set::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardContact::Set>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardContact::Set::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardContact::Set>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif


} // namespace note::api
