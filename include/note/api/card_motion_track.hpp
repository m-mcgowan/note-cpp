// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once

#include <note/dyn_field.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/notecard.hpp>
#include <note/safety.hpp>
#include <note/types.hpp>
#include <note/units.hpp>
#include <note/target.hpp>

namespace note::api {







struct CardMotionTrack {
    static constexpr string_view notecard_request = "card.motion.track";
    static constexpr bool supports_cmd = true;
    static constexpr Safety safety = Safety::Idempotent;
    static constexpr Skus skus{ Rat::Cell | Rat::WiFi | Rat::Ntn };

    Notecard* nc_ = nullptr;

    /// The number of most recent motion buckets to examine.
    struct count_t : Field<int32_t> {
        using Field<int32_t>::Field;
        using Field<int32_t>::operator=;
        CardMotionTrack& operator()(int32_t v);
    } count{};
    /// The Notefile to use for motion capture Notes. See the [`_motion.qo`
    /// Notefile's documentation](/api-reference/system-notefiles#motion-qo) for
    /// details on the format of the data captured.
    struct file_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        CardMotionTrack& operator()(note::string_view v);
    } file{};
    /// The maximum period to capture Notes in the Notefile.
    struct minutes_t : Field<note::Minutes> {
        using Field<note::Minutes>::Field;
        using Field<note::Minutes>::operator=;
        CardMotionTrack& operator()(note::Minutes v);
    } minutes{};
    /// Set to `true` to trigger the immediate creation of a `_motion.qo` event
    /// if the orientation of the Notecard changes (overriding the `minutes`
    /// setting).
    struct now_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardMotionTrack& operator()(bool v);
    } now{};
    /// `true` to start motion capture.
    struct start_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardMotionTrack& operator()(bool v);
    } start{};
    /// `true` to stop motion capture.
    struct stop_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardMotionTrack& operator()(bool v);
    } stop{};
    /// The number of buckets that must indicate motion in order to capture.
    struct threshold_t : Field<int32_t> {
        using Field<int32_t>::Field;
        using Field<int32_t>::operator=;
        CardMotionTrack& operator()(int32_t v);
    } threshold{};


    template<typename T>
    auto& extra(note::string_view key, T value) {
        if (extras_count_ < NOTE_EXTRAS_MAX)
            extras_[extras_count_++] = {key, note::DynValue{value}};
        return *this;
    }
    auto& extra(note::string_view key, const char* value) {
        return extra(key, note::string_view{value});
    }

    note::DynField operator[](note::string_view k_) {
        if (k_ == "count") return note::dyn_field_for(count);
        if (k_ == "file") return note::dyn_field_for(file);
        if (k_ == "minutes") return note::dyn_field_for(minutes);
        if (k_ == "now") return note::dyn_field_for(now);
        if (k_ == "start") return note::dyn_field_for(start);
        if (k_ == "stop") return note::dyn_field_for(stop);
        if (k_ == "threshold") return note::dyn_field_for(threshold);
        if (extras_count_ < NOTE_EXTRAS_MAX) {
            auto& slot = extras_[extras_count_++];
            slot.key = k_;
            return note::dyn_field_for(slot.value);
        }
        return {};
    }

    std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
    uint8_t extras_count_ = 0;

    using Response = void;

    void build(JsonBuilder& b) const {
        if (count) b.add("count", *count);
        if (file) b.add("file", *file);
        if (minutes) b.add("minutes", *minutes);
        if (now) b.add("now", *now);
        if (start) b.add("start", *start);
        if (stop) b.add("stop", *stop);
        if (threshold) b.add("threshold", *threshold);
        for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
            std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                       extras_[i_].value);
    }

    auto execute() const { return nc_->execute(*this); }
    auto execute(Notecard& nc) const { return nc.execute(*this); }
    Result<void> command() const { return nc_->command_typed(*this); }
    Result<void> command(Notecard& nc) const { return nc.command_typed(*this); }

};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardMotionTrack& CardMotionTrack::count_t::operator()(int32_t v) {
    Field<int32_t>::operator=(v);
    return *reinterpret_cast<CardMotionTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardMotionTrack, count));
}
inline CardMotionTrack& CardMotionTrack::file_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardMotionTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardMotionTrack, file));
}
inline CardMotionTrack& CardMotionTrack::minutes_t::operator()(note::Minutes v) {
    Field<note::Minutes>::operator=(v);
    return *reinterpret_cast<CardMotionTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardMotionTrack, minutes));
}
inline CardMotionTrack& CardMotionTrack::now_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardMotionTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardMotionTrack, now));
}
inline CardMotionTrack& CardMotionTrack::start_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardMotionTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardMotionTrack, start));
}
inline CardMotionTrack& CardMotionTrack::stop_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardMotionTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardMotionTrack, stop));
}
inline CardMotionTrack& CardMotionTrack::threshold_t::operator()(int32_t v) {
    Field<int32_t>::operator=(v);
    return *reinterpret_cast<CardMotionTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardMotionTrack, threshold));
}
#pragma GCC diagnostic pop


} // namespace note::api
