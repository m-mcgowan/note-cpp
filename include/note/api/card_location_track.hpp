// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once

#include <note/dyn_field.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/notecard.hpp>
#include <note/safety.hpp>
#include <note/types.hpp>
#include <note/target.hpp>

namespace note::api {







struct CardLocationTrack {
    static constexpr string_view notecard_request = "card.location.track";
    static constexpr bool supports_cmd = true;
    static constexpr Safety safety = Safety::Idempotent;
    static constexpr Skus skus{ Rat::Cell | Rat::WiFi | Rat::Ntn };

    Notecard* nc_ = nullptr;

    /// The Notefile in which to store tracked location data. See the
    /// `_track.qo` Notefile's [documentation](/api-reference/system-
    /// notefiles/#track-qo) for details on the format of the data captured.
    struct file_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        CardLocationTrack& operator()(note::string_view v);
    } file{};
    /// When `start` is `true`, set to `true` to enable tracking even when
    /// motion is not detected. If using `heartbeat`, also set the `hours`
    /// below.
    struct heartbeat_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardLocationTrack& operator()(bool v);
    } heartbeat{};
    /// If `heartbeat` is true, add a heartbeat entry at this hourly interval.
    /// Use a negative integer to specify a heartbeat in minutes instead of
    /// hours.
    struct hours_t : Field<int32_t> {
        using Field<int32_t>::Field;
        using Field<int32_t>::operator=;
        CardLocationTrack& operator()(int32_t v);
    } hours{};
#if NOTE_API_VERSION >= NOTE_VERSION(7, 5, 2) || !defined(NOTE_API_STRICT)
    /// A base64-encoded binary payload to be included in the next `_track.qo`
    /// Note. See the guide on [Sampling at Predefined
    /// Intervals](/notecard/notecard-walkthrough/time-and-location-
    /// requests/#sampling-at-predefined-intervals) for more details.
    ///
    /// @since firmware 7.5.2
#if NOTE_API_VERSION < NOTE_VERSION(7, 5, 2)
    [[deprecated("requires firmware >= 7.5.2")]]
#endif
    struct payload_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        CardLocationTrack& operator()(note::string_view v);
    } payload{};
#endif
    /// Set to `true` to start Notefile tracking.
    struct start_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardLocationTrack& operator()(bool v);
    } start{};
    /// Set to `true` to stop Notefile tracking.
    struct stop_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardLocationTrack& operator()(bool v);
    } stop{};
    /// Set to `true` to perform an immediate sync to the Notehub each time a
    /// new Note is added.
    struct sync_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardLocationTrack& operator()(bool v);
    } sync{};


    template<typename T>
    auto& extra(note::string_view key, T value) {
        if (extras_count_ < NOTE_EXTRAS_MAX)
            extras_[extras_count_++] = {key, note::DynValue{value}};
        return *this;
    }
    auto& extra(note::string_view key, const char* value) {
        return extra(key, note::string_view{value});
    }

    note::DynField operator[](note::string_view k_) {
        if (k_ == "file") return note::dyn_field_for(file);
        if (k_ == "heartbeat") return note::dyn_field_for(heartbeat);
        if (k_ == "hours") return note::dyn_field_for(hours);
#if NOTE_API_VERSION >= NOTE_VERSION(7, 5, 2) || !defined(NOTE_API_STRICT)
        if (k_ == "payload") return note::dyn_field_for(payload);
#endif
        if (k_ == "start") return note::dyn_field_for(start);
        if (k_ == "stop") return note::dyn_field_for(stop);
        if (k_ == "sync") return note::dyn_field_for(sync);
        if (extras_count_ < NOTE_EXTRAS_MAX) {
            auto& slot = extras_[extras_count_++];
            slot.key = k_;
            return note::dyn_field_for(slot.value);
        }
        return {};
    }

    std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
    uint8_t extras_count_ = 0;

    struct Response {
        /// The tracking Notefile, if provided.
        note::string_view file{};
        /// `true` if heartbeat is enabled.
        bool heartbeat{};
        /// The `heartbeat` interval in minutes, if provided.
        int32_t minutes{};
        /// If tracking is enabled and a heartbeat `hours` value is not set, the
        /// tracking interval set in `card.location.mode`.
        int32_t seconds{};
        /// `true` if tracking is enabled.
        bool start{};
        /// `true` if tracking is disabled.
        bool stop{};

        static Response parse(std::unique_ptr<JsonReader> reader_) {
            Response rsp;
            rsp.file = reader_->get_string("file");
            rsp.heartbeat = reader_->get_bool("heartbeat");
            rsp.minutes = reader_->get_int("minutes");
            rsp.seconds = reader_->get_int("seconds");
            rsp.start = reader_->get_bool("start");
            rsp.stop = reader_->get_bool("stop");
            rsp.reader_ = std::move(reader_);
            return rsp;
        }

        // Non-owning parse: string_views point into the reader's data.
        // The reader (and its underlying JSON buffer) must outlive the Response,
        // or the caller must consume all string fields before the reader is reused.
        static Response parse(const JsonReader& reader_) {
            Response rsp;
            rsp.file = reader_.get_string("file");
            rsp.heartbeat = reader_.get_bool("heartbeat");
            rsp.minutes = reader_.get_int("minutes");
            rsp.seconds = reader_.get_int("seconds");
            rsp.start = reader_.get_bool("start");
            rsp.stop = reader_.get_bool("stop");
            return rsp;
        }

        // SAX sink — zero-allocation streaming parse into Response fields.
        // String fields are string_views into the JSON buffer; caller must
        // ensure the buffer outlives the Response (or intern strings after).
        struct Sink : ::note::JsonSink {
            Response& rsp;
            explicit Sink(Response& r) : rsp(r) {}
            void on_string(::note::string_view key, ::note::string_view val) override {
                if (key == "file") { rsp.file = val; return; }
            }
            void on_bool(::note::string_view key, bool val) override {
                if (key == "heartbeat") { rsp.heartbeat = val; return; }
                if (key == "start") { rsp.start = val; return; }
                if (key == "stop") { rsp.stop = val; return; }
            }
            void on_number(::note::string_view key, ::note::string_view raw) override {
                if (key == "minutes") { rsp.minutes = ::note::parse_int(raw); return; }
                if (key == "seconds") { rsp.seconds = ::note::parse_int(raw); return; }
            }
        };

    private:
        std::unique_ptr<JsonReader> reader_;
    };

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
    void build(JsonBuilder& b) const {
        if (file) b.add("file", *file);
        if (heartbeat) b.add("heartbeat", *heartbeat);
        if (hours) b.add("hours", *hours);
#if NOTE_API_VERSION >= NOTE_VERSION(7, 5, 2) || !defined(NOTE_API_STRICT)
        if (payload) b.add("payload", *payload);
#endif
        if (start) b.add("start", *start);
        if (stop) b.add("stop", *stop);
        if (sync) b.add("sync", *sync);
        for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
            std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                       extras_[i_].value);
    }
#pragma GCC diagnostic pop

    auto execute() const { return nc_->execute(*this); }
    auto execute(Notecard& nc) const { return nc.execute(*this); }
    Result<void> command() const { return nc_->command_typed(*this); }
    Result<void> command(Notecard& nc) const { return nc.command_typed(*this); }

};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline CardLocationTrack& CardLocationTrack::file_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardLocationTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationTrack, file));
}
inline CardLocationTrack& CardLocationTrack::heartbeat_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardLocationTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationTrack, heartbeat));
}
inline CardLocationTrack& CardLocationTrack::hours_t::operator()(int32_t v) {
    Field<int32_t>::operator=(v);
    return *reinterpret_cast<CardLocationTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationTrack, hours));
}
#if NOTE_API_VERSION >= NOTE_VERSION(7, 5, 2) || !defined(NOTE_API_STRICT)
inline CardLocationTrack& CardLocationTrack::payload_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardLocationTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationTrack, payload));
}
#endif
inline CardLocationTrack& CardLocationTrack::start_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardLocationTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationTrack, start));
}
inline CardLocationTrack& CardLocationTrack::stop_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardLocationTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationTrack, stop));
}
inline CardLocationTrack& CardLocationTrack::sync_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardLocationTrack*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationTrack, sync));
}
#pragma GCC diagnostic pop


} // namespace note::api
