// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once

#include <note/dyn_field.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/notecard.hpp>
#include <note/safety.hpp>
#include <note/types.hpp>
#include <note/units.hpp>
#include <note/voltage_variable.hpp>
#include <note/target.hpp>

namespace note::api {







struct HubSet {
    static constexpr string_view notecard_request = "hub.set";
    static constexpr bool supports_cmd = true;
    static constexpr Safety safety = Safety::Idempotent;
    static constexpr Skus skus{};

    Notecard* nc_ = nullptr;

    /// Use `true` to align syncs on a regular time-periodic cycle.
    struct align_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        HubSet& operator()(bool v);
    } align{};
#if NOTE_API_VERSION >= NOTE_VERSION(6, 2, 3) || !defined(NOTE_API_STRICT)
    /// When using Notecard LoRa you can use this argument to provide
    /// information about an alternative LoRaWAN server or service you would
    /// like the Notecard to use. The argument you provide must be a JSON object
    /// with three keys, "deveui", "appeui", and "appkey", all of which are
    /// hexadecimal strings with no leading 0x. For example:
    ///
    /// `{"deveui":"0080E11500088B37","appeui":"6E6F746563617264","appkey":"0008
    /// 8B37"}`
    ///
    /// The LoRaWAN details you send to a Notecard become part of its permanent
    /// configuration, and survive factory resets. You can reset a Notecard's
    /// LoRaWAN details to its default values by providing a `"-"` for the
    /// details argument.
    ///
    /// @since firmware 6.2.3
#if NOTE_API_VERSION < NOTE_VERSION(6, 2, 3)
    [[deprecated("requires firmware >= 6.2.3")]]
#endif
    struct details_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        HubSet& operator()(note::string_view v);
    } details{};
#endif
    /// When in `continuous` mode, the amount of time, in minutes, of each
    /// session (the minimum allowed value is `15`). When this time elapses, the
    /// Notecard gracefully ends the current session and starts a new one in
    /// order to sync session-specific data to Notehub.
    struct duration_t : Field<note::Minutes> {
        static constexpr note::Minutes reset{ -1 };
        using Field<note::Minutes>::Field;
        using Field<note::Minutes>::operator=;
        HubSet& operator()(note::Minutes v);
    } duration{};
    /// The URL of the Notehub service. Use `"-"` to reset to the default value.
    struct host_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        HubSet& operator()(note::string_view v);
    } host{};
    /// The max wait time, in minutes, to sync inbound data from Notehub.
    /// Explicit syncs (e.g. using `hub.sync`) do not affect this cadence.
    ///
    /// When in `periodic` or `continuous` mode this argument is required,
    /// otherwise the Notecard will function as if it is in `minimum` mode as it
    /// pertains to syncing behavior.
    ///
    /// Use `-1` to reset the value back to its default of `0`.
    ///
    /// A value of `0` means that the Notecard will never sync inbound data
    /// unless explicitly told to do so (e.g. using `hub.sync`).
    struct inbound_t : Field<note::Minutes> {
        static constexpr note::Minutes reset{ -1 };
        static constexpr note::Minutes manual{ 0 };
        using Field<note::Minutes>::Field;
        using Field<note::Minutes>::operator=;
        HubSet& operator()(note::Minutes v);
    } inbound{};
    /// The Notecard's synchronization mode.
    ///
    /// **NOTE:** The Notecard must be in `periodic` or `continuous` mode to use
    /// the onboard GPS module.
    // mode: periodic | continuous | minimum | off | dfu
    struct mode_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        HubSet& operator()(note::string_view v);
    } mode{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
    /// Set to `true` to manually instruct the Notecard to resume periodic mode
    /// after a web transaction has completed.
    ///
    /// @since firmware 3.4.1
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
    [[deprecated("requires firmware >= 3.4.1")]]
#endif
    struct off_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        HubSet& operator()(bool v);
    } off{};
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
    /// If in `periodic` mode, used to temporarily switch the Notecard to
    /// `continuous` mode to perform a web transaction.
    ///
    /// Ignored if the Notecard is already in `continuous` mode or if the
    /// Notecard is NOT performing a web transaction.
    ///
    /// @since firmware 3.4.1
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
    [[deprecated("requires firmware >= 3.4.1")]]
#endif
    struct on_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        HubSet& operator()(bool v);
    } on{};
#endif
    /// The max wait time, in minutes, to sync outbound data from the Notecard.
    /// Explicit syncs (e.g. using `hub.sync`) do not affect this cadence.
    ///
    /// When in `periodic` or `continuous` mode this argument is required,
    /// otherwise the Notecard will function as if it is in `minimum` mode as it
    /// pertains to syncing behavior.
    ///
    /// Use `-1` to reset the value back to its default of `0`.
    ///
    /// A value of `0` means that the Notecard will never sync outbound data
    /// unless explicitly told to do so (e.g. using `hub.sync`).
    struct outbound_t : Field<note::Minutes> {
        static constexpr note::Minutes reset{ -1 };
        static constexpr note::Minutes manual{ 0 };
        using Field<note::Minutes>::Field;
        using Field<note::Minutes>::operator=;
        HubSet& operator()(note::Minutes v);
    } outbound{};
    /// A Notehub-managed unique identifier that is used to match Devices with
    /// Projects. This string is used during a device's auto-provisioning to
    /// find the Notehub Project that, once provisioned, will securely manage
    /// the device and its data.
    struct product_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        HubSet& operator()(note::string_view v);
    } product{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
    /// If in `periodic` mode and using `on` above, the number of seconds to run
    /// in continuous mode before switching back to periodic mode. If not set, a
    /// default of 300 seconds is used. Ignored if the Notecard is already in
    /// continuous mode.
    ///
    /// @since firmware 3.4.1
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
    [[deprecated("requires firmware >= 3.4.1")]]
#endif
    struct seconds_t : Field<note::Seconds> {
        using Field<note::Seconds>::Field;
        using Field<note::Seconds>::operator=;
        HubSet& operator()(note::Seconds v);
    } seconds{};
#endif
    /// The end product's serial number.
    struct sn_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        HubSet& operator()(note::string_view v);
    } sn{};
    /// If in `continuous` mode, automatically and immediately sync each time an
    /// inbound Notefile change is detected on Notehub.
    ///
    /// **NOTE:** The `sync` argument is not supported when a Notecard is in NTN
    /// mode.
    struct sync_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        HubSet& operator()(bool v);
    } sync{};
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
    /// Set to `true` to use USB/line power variable sync behavior, enabling the
    /// Notecard to stay in `continuous` mode when connected to USB/line power
    /// and fallback to `minimum` mode when disconnected.
    ///
    /// @since firmware 4.1.1
#if NOTE_API_VERSION < NOTE_VERSION(4, 1, 1)
    [[deprecated("requires firmware >= 4.1.1")]]
#endif
    struct umin_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        HubSet& operator()(bool v);
    } umin{};
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
    /// Set to `true` to use USB/line power variable sync behavior, enabling the
    /// Notecard to stay in `continuous` mode when connected to USB/line power
    /// and fallback to `off` mode when disconnected.
    ///
    /// @since firmware 4.1.1
#if NOTE_API_VERSION < NOTE_VERSION(4, 1, 1)
    [[deprecated("requires firmware >= 4.1.1")]]
#endif
    struct uoff_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        HubSet& operator()(bool v);
    } uoff{};
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
    /// Set to `true` to use USB/line power variable sync behavior, enabling the
    /// Notecard to stay in `continuous` mode when connected to USB/line power
    /// and fallback to `periodic` mode when disconnected.
    ///
    /// @since firmware 4.1.1
#if NOTE_API_VERSION < NOTE_VERSION(4, 1, 1)
    [[deprecated("requires firmware >= 4.1.1")]]
#endif
    struct uperiodic_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        HubSet& operator()(bool v);
    } uperiodic{};
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(7, 3, 1) || !defined(NOTE_API_STRICT)
    /// The version of your host firmware. The value provided will appear on
    /// your device in Notehub under the "Host Firmware" tab.
    ///
    /// You may pass a simple version number string (e.g. "1.0.0.0"), or an
    /// object with detailed information about the firmware image. If you
    /// provide an object it must take the following form.
    ///
    /// `{"org":"my-organization","product":"My Product","description":"A
    /// description of the image","version":"1.2.4","built":"Jan 01 2025
    /// 01:02:03","ver_major":1,"ver_minor":2,"ver_patch":4,"ver_build":
    /// 5,"builder":"The Builder"}`
    ///
    /// If your project uses [Notecard Outboard Firmware Update](/notehub/host-
    /// firmware-updates/notecard-outboard-firmware-update), you can
    /// alternatively use the [`dfu.status` request](/api-reference/notecard-
    /// api/dfu-requests/latest/#dfu-status) to set your host firmware version.
    ///
    /// @since firmware 7.3.1
#if NOTE_API_VERSION < NOTE_VERSION(7, 3, 1)
    [[deprecated("requires firmware >= 7.3.1")]]
#endif
    struct version_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        HubSet& operator()(note::string_view v);
    } version{};
#endif
    /// Overrides `inbound` with a voltage-variable value. Use `"-"` to clear
    /// this value.
    ///
    /// **NOTE:** Setting voltage-variable values is not supported on Notecard
    /// XP.
    struct vinbound_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        HubSet& operator()(note::string_view v);
        HubSet& operator()(const note::VoltageVariable& v);
        vinbound_t& usb(int32_t minutes);
        vinbound_t& high(int32_t minutes);
        vinbound_t& normal(int32_t minutes);
        vinbound_t& low(int32_t minutes);
        vinbound_t& dead(int32_t minutes);
        note::VoltageVariable vv_{};
    } vinbound{};
    /// Overrides `outbound` with a voltage-variable value. Use `"-"` to clear
    /// this value.
    ///
    /// **NOTE:** Setting voltage-variable values is not supported on Notecard
    /// XP.
    struct voutbound_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        HubSet& operator()(note::string_view v);
        HubSet& operator()(const note::VoltageVariable& v);
        voutbound_t& usb(int32_t minutes);
        voutbound_t& high(int32_t minutes);
        voutbound_t& normal(int32_t minutes);
        voutbound_t& low(int32_t minutes);
        voutbound_t& dead(int32_t minutes);
        note::VoltageVariable vv_{};
    } voutbound{};

    // consteval: only callable at compile time
    static consteval note::string_view validatedMode(const char* v) {
        note::string_view sv{v};
        if (sv != "periodic" && sv != "continuous" && sv != "minimum" && sv != "off" && sv != "dfu")
            throw "hub.set: invalid value for 'mode'";
        return sv;
    }

    template<typename T>
    auto& extra(note::string_view key, T value) {
        if (extras_count_ < NOTE_EXTRAS_MAX)
            extras_[extras_count_++] = {key, note::DynValue{value}};
        return *this;
    }
    auto& extra(note::string_view key, const char* value) {
        return extra(key, note::string_view{value});
    }

    note::DynField operator[](note::string_view k_) {
        if (k_ == "align") return note::dyn_field_for(align);
#if NOTE_API_VERSION >= NOTE_VERSION(6, 2, 3) || !defined(NOTE_API_STRICT)
        if (k_ == "details") return note::dyn_field_for(details);
#endif
        if (k_ == "duration") return note::dyn_field_for(duration);
        if (k_ == "host") return note::dyn_field_for(host);
        if (k_ == "inbound") return note::dyn_field_for(inbound);
        if (k_ == "mode") return note::dyn_field_for(mode);
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        if (k_ == "off") return note::dyn_field_for(off);
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        if (k_ == "on") return note::dyn_field_for(on);
#endif
        if (k_ == "outbound") return note::dyn_field_for(outbound);
        if (k_ == "product") return note::dyn_field_for(product);
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        if (k_ == "seconds") return note::dyn_field_for(seconds);
#endif
        if (k_ == "sn") return note::dyn_field_for(sn);
        if (k_ == "sync") return note::dyn_field_for(sync);
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
        if (k_ == "umin") return note::dyn_field_for(umin);
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
        if (k_ == "uoff") return note::dyn_field_for(uoff);
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
        if (k_ == "uperiodic") return note::dyn_field_for(uperiodic);
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(7, 3, 1) || !defined(NOTE_API_STRICT)
        if (k_ == "version") return note::dyn_field_for(version);
#endif
        if (k_ == "vinbound") return note::dyn_field_for(vinbound);
        if (k_ == "voutbound") return note::dyn_field_for(voutbound);
        if (extras_count_ < NOTE_EXTRAS_MAX) {
            auto& slot = extras_[extras_count_++];
            slot.key = k_;
            return note::dyn_field_for(slot.value);
        }
        return {};
    }

    std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
    uint8_t extras_count_ = 0;

    using Response = void;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
    void build(JsonBuilder& b) const {
        if (align) b.add("align", *align);
#if NOTE_API_VERSION >= NOTE_VERSION(6, 2, 3) || !defined(NOTE_API_STRICT)
        if (details) b.add("details", *details);
#endif
        if (duration) b.add("duration", *duration);
        if (host) b.add("host", *host);
        if (inbound) b.add("inbound", *inbound);
        if (mode) b.add("mode", *mode);
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        if (off) b.add("off", *off);
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        if (on) b.add("on", *on);
#endif
        if (outbound) b.add("outbound", *outbound);
        if (product) b.add("product", *product);
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        if (seconds) b.add("seconds", *seconds);
#endif
        if (sn) b.add("sn", *sn);
        if (sync) b.add("sync", *sync);
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
        if (umin) b.add("umin", *umin);
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
        if (uoff) b.add("uoff", *uoff);
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
        if (uperiodic) b.add("uperiodic", *uperiodic);
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(7, 3, 1) || !defined(NOTE_API_STRICT)
        if (version) b.add("version", *version);
#endif
        if (vinbound) b.add("vinbound", *vinbound);
        if (voutbound) b.add("voutbound", *voutbound);
        for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
            std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                       extras_[i_].value);
    }
#pragma GCC diagnostic pop

    auto execute() const { return nc_->execute(*this); }
    auto execute(Notecard& nc) const { return nc.execute(*this); }
    Result<void> command() const { return nc_->command_typed(*this); }
    Result<void> command(Notecard& nc) const { return nc.command_typed(*this); }

};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline HubSet& HubSet::align_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, align));
}
#if NOTE_API_VERSION >= NOTE_VERSION(6, 2, 3) || !defined(NOTE_API_STRICT)
inline HubSet& HubSet::details_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, details));
}
#endif
inline HubSet& HubSet::duration_t::operator()(note::Minutes v) {
    Field<note::Minutes>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, duration));
}
inline HubSet& HubSet::host_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, host));
}
inline HubSet& HubSet::inbound_t::operator()(note::Minutes v) {
    Field<note::Minutes>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, inbound));
}
inline HubSet& HubSet::mode_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, mode));
}
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
inline HubSet& HubSet::off_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, off));
}
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
inline HubSet& HubSet::on_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, on));
}
#endif
inline HubSet& HubSet::outbound_t::operator()(note::Minutes v) {
    Field<note::Minutes>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, outbound));
}
inline HubSet& HubSet::product_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, product));
}
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
inline HubSet& HubSet::seconds_t::operator()(note::Seconds v) {
    Field<note::Seconds>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, seconds));
}
#endif
inline HubSet& HubSet::sn_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, sn));
}
inline HubSet& HubSet::sync_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, sync));
}
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
inline HubSet& HubSet::umin_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, umin));
}
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
inline HubSet& HubSet::uoff_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, uoff));
}
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
inline HubSet& HubSet::uperiodic_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, uperiodic));
}
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(7, 3, 1) || !defined(NOTE_API_STRICT)
inline HubSet& HubSet::version_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, version));
}
#endif
inline HubSet& HubSet::vinbound_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, vinbound));
}
inline HubSet& HubSet::vinbound_t::operator()(const note::VoltageVariable& v) {
    vv_ = v;
    Field<note::string_view>::operator=(vv_.str());
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, vinbound));
}
inline HubSet::vinbound_t& HubSet::vinbound_t::usb(int32_t minutes) {
    vv_.usb(minutes);
    Field<note::string_view>::operator=(vv_.str());
    return *this;
}
inline HubSet::vinbound_t& HubSet::vinbound_t::high(int32_t minutes) {
    vv_.high(minutes);
    Field<note::string_view>::operator=(vv_.str());
    return *this;
}
inline HubSet::vinbound_t& HubSet::vinbound_t::normal(int32_t minutes) {
    vv_.normal(minutes);
    Field<note::string_view>::operator=(vv_.str());
    return *this;
}
inline HubSet::vinbound_t& HubSet::vinbound_t::low(int32_t minutes) {
    vv_.low(minutes);
    Field<note::string_view>::operator=(vv_.str());
    return *this;
}
inline HubSet::vinbound_t& HubSet::vinbound_t::dead(int32_t minutes) {
    vv_.dead(minutes);
    Field<note::string_view>::operator=(vv_.str());
    return *this;
}
inline HubSet& HubSet::voutbound_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, voutbound));
}
inline HubSet& HubSet::voutbound_t::operator()(const note::VoltageVariable& v) {
    vv_ = v;
    Field<note::string_view>::operator=(vv_.str());
    return *reinterpret_cast<HubSet*>(
        reinterpret_cast<char*>(this) - offsetof(HubSet, voutbound));
}
inline HubSet::voutbound_t& HubSet::voutbound_t::usb(int32_t minutes) {
    vv_.usb(minutes);
    Field<note::string_view>::operator=(vv_.str());
    return *this;
}
inline HubSet::voutbound_t& HubSet::voutbound_t::high(int32_t minutes) {
    vv_.high(minutes);
    Field<note::string_view>::operator=(vv_.str());
    return *this;
}
inline HubSet::voutbound_t& HubSet::voutbound_t::normal(int32_t minutes) {
    vv_.normal(minutes);
    Field<note::string_view>::operator=(vv_.str());
    return *this;
}
inline HubSet::voutbound_t& HubSet::voutbound_t::low(int32_t minutes) {
    vv_.low(minutes);
    Field<note::string_view>::operator=(vv_.str());
    return *this;
}
inline HubSet::voutbound_t& HubSet::voutbound_t::dead(int32_t minutes) {
    vv_.dead(minutes);
    Field<note::string_view>::operator=(vv_.str());
    return *this;
}
#pragma GCC diagnostic pop


} // namespace note::api
