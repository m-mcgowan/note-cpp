// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once
#include <note/note_config.hpp>

// NOTE_EXTRAS default is set in note_config.hpp.
#if NOTE_EXTRAS
#include <note/dyn_field.hpp>
#endif
#include <note/generic_builder.hpp>
#include <note/generic_sink.hpp>
#include <note/notecard.hpp>
#include <note/arena.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/binary_request.hpp>
#include <note/print.hpp>
#include <note/safety.hpp>
#include <note/string_pool.hpp>
#include <note/types.hpp>
#include <note/progmem.hpp>
#include <note/target.hpp>

namespace note::api {









/// Used to configure a Notecard for Notecard Outboard Firmware Update.
///
/// @skus{CELL,CELL+WIFI,SKYLO,WIFI}
struct CardDfu {
    struct keys_ {
        static constexpr char req[] NOTE_FLASH_ATTR = "card.dfu";
        static constexpr char mode[] NOTE_FLASH_ATTR = "mode";
        static constexpr char name[] NOTE_FLASH_ATTR = "name";
        static constexpr char off[] NOTE_FLASH_ATTR = "off";
        static constexpr char on[] NOTE_FLASH_ATTR = "on";
        static constexpr char seconds[] NOTE_FLASH_ATTR = "seconds";
        static constexpr char start[] NOTE_FLASH_ATTR = "start";
        static constexpr char stop[] NOTE_FLASH_ATTR = "stop";
        static constexpr char rsp_name[] NOTE_FLASH_ATTR = "name";
    };

    static constexpr string_view notecard_request = "card.dfu";
    static constexpr bool supports_cmd = true;
    static constexpr Safety safety = Safety::Idempotent;
    static constexpr RadiosSupport radios = RadiosSupport::from(Radios::Cell, Radios::CellWifi, Radios::Skylo, Radios::WiFi);
    static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
    void* nc_ = nullptr;
#endif


    /// The `mode` argument allows you to control whether a Notecard's `AUX`
    /// pins (default) or `ALT_DFU` pins are used for Notecard Outboard Firmware
    /// Update. This argument is only supported on Notecards that have `ALT_DFU`
    /// pins, which includes all versions of Notecard Cell+WiFi, non-legacy
    /// versions of Notecard Cellular, and Notecard WiFi v2.
    // mode: altdfu | aux
    struct mode_t : Field<note::string_view> {
#if __cplusplus >= 202002L && !defined(__clang__)
        constexpr mode_t() = default;
        template<std::size_t N>
        consteval mode_t(const char (&s)[N])
            : Field<note::string_view>(note::string_view(s, N - 1)) {
            note::string_view sv(s, N - 1);
            if (sv != "altdfu" && sv != "aux")
                throw "card.dfu: invalid value for 'mode'";
        }
        template<typename U>
            requires std::is_convertible_v<U, note::string_view>
                  && (!std::is_array_v<std::remove_reference_t<U>>)
                  && (!std::is_same_v<std::decay_t<U>, mode_t>)
        constexpr mode_t(U&& v) : Field<note::string_view>(note::string_view(std::forward<U>(v))) {}
        template<typename U>
            requires std::is_convertible_v<U, note::string_view>
                  && (!std::is_array_v<std::remove_reference_t<U>>)
                  && (!std::is_same_v<std::decay_t<U>, mode_t>)
        mode_t& operator=(U&& v) {
            Field<note::string_view>::operator=(note::string_view(std::forward<U>(v)));
            return *this;
        }
        mode_t& operator=(std::nullopt_t) { Field<note::string_view>::reset(); return *this; }
        mode_t(const mode_t&) = default;
        mode_t& operator=(const mode_t&) = default;
        mode_t(mode_t&&) = default;
        mode_t& operator=(mode_t&&) = default;
#else
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
#endif
        static constexpr note::string_view altdfu{"altdfu"};
        static constexpr note::string_view aux{"aux"};
        /// The `mode` argument allows you to control whether a Notecard's `AUX`
        /// pins (default) or `ALT_DFU` pins are used for Notecard Outboard
        /// Firmware Update. This argument is only supported on Notecards that
        /// have `ALT_DFU` pins, which includes all versions of Notecard
        /// Cell+WiFi, non-legacy versions of Notecard Cellular, and Notecard
        /// WiFi v2.
        CardDfu& operator()(note::string_view v);
    } mode{};
    /// One of the supported classes of host MCU. Supported MCU classes are
    /// `"esp32"`, `"stm32"`, `"stm32-bi"`, `"mcuboot"` (added in v5.3.1), and
    /// `"-"`, which resets the configuration. The "bi" in `"stm32-bi"` stands
    /// for "boot inverted", and the `"stm32-bi"` option should be used on STM32
    /// family boards where the hardware boot pin is assumed to be active low,
    /// instead of active high. Supported MCUs can be found on the Notecarrier F
    /// datasheet.
    // name: esp32 | stm32 | stm32-bi | mcuboot | -
    struct name_t : Field<note::string_view> {
#if __cplusplus >= 202002L && !defined(__clang__)
        constexpr name_t() = default;
        template<std::size_t N>
        consteval name_t(const char (&s)[N])
            : Field<note::string_view>(note::string_view(s, N - 1)) {
            note::string_view sv(s, N - 1);
            if (sv != "esp32" && sv != "stm32" && sv != "stm32-bi" && sv != "mcuboot" && sv != "-")
                throw "card.dfu: invalid value for 'name'";
        }
        template<typename U>
            requires std::is_convertible_v<U, note::string_view>
                  && (!std::is_array_v<std::remove_reference_t<U>>)
                  && (!std::is_same_v<std::decay_t<U>, name_t>)
        constexpr name_t(U&& v) : Field<note::string_view>(note::string_view(std::forward<U>(v))) {}
        template<typename U>
            requires std::is_convertible_v<U, note::string_view>
                  && (!std::is_array_v<std::remove_reference_t<U>>)
                  && (!std::is_same_v<std::decay_t<U>, name_t>)
        name_t& operator=(U&& v) {
            Field<note::string_view>::operator=(note::string_view(std::forward<U>(v)));
            return *this;
        }
        name_t& operator=(std::nullopt_t) { Field<note::string_view>::reset(); return *this; }
        name_t(const name_t&) = default;
        name_t& operator=(const name_t&) = default;
        name_t(name_t&&) = default;
        name_t& operator=(name_t&&) = default;
#else
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
#endif
        static constexpr note::string_view esp32{"esp32"};
        static constexpr note::string_view stm32{"stm32"};
        static constexpr note::string_view stm32_bi{"stm32-bi"};
        static constexpr note::string_view mcuboot{"mcuboot"};
        static constexpr note::string_view _{"-"};
        /// One of the supported classes of host MCU. Supported MCU classes are
        /// `"esp32"`, `"stm32"`, `"stm32-bi"`, `"mcuboot"` (added in v5.3.1),
        /// and `"-"`, which resets the configuration. The "bi" in `"stm32-bi"`
        /// stands for "boot inverted", and the `"stm32-bi"` option should be
        /// used on STM32 family boards where the hardware boot pin is assumed
        /// to be active low, instead of active high. Supported MCUs can be
        /// found on the Notecarrier F datasheet.
        CardDfu& operator()(note::string_view v);
    } name{};
    /// Set to `true` to disable Notecard Outboard Firmware Update from
    /// occurring.
    struct off_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        /// Set to `true` to disable Notecard Outboard Firmware Update from
        /// occurring.
        CardDfu& operator()(bool v);
    } off{};
    /// Set to `true` to enable Notecard Outboard Firmware Update.
    struct on_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        /// Set to `true` to enable Notecard Outboard Firmware Update.
        CardDfu& operator()(bool v);
    } on{};
    /// When used with `"off":true`, disable Notecard Outboard Firmware Update
    /// operations for the specified number of `seconds`.
    struct seconds_t : Field<note::json_int_t> {
        using Field<note::json_int_t>::Field;
        using Field<note::json_int_t>::operator=;
        /// When used with `"off":true`, disable Notecard Outboard Firmware
        /// Update operations for the specified number of `seconds`.
        CardDfu& operator()(note::json_int_t v);
    } seconds{};
    /// Set to `true` to enable the host RESET if previously disabled with
    /// `"stop":true`.
    struct start_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        /// Set to `true` to enable the host RESET if previously disabled with
        /// `"stop":true`.
        CardDfu& operator()(bool v);
    } start{};
    /// Set to `true` to disable the host RESET that is normally performed on
    /// the host MCU when the Notecard starts up (in order to ensure a clean
    /// startup), and also when the Notecard wakes up the host MCU after the
    /// expiration of a `card.attn` "sleep" operation. If `true`, the host MCU
    /// will not be reset in these two conditions.
    struct stop_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        /// Set to `true` to disable the host RESET that is normally performed
        /// on the host MCU when the Notecard starts up (in order to ensure a
        /// clean startup), and also when the Notecard wakes up the host MCU
        /// after the expiration of a `card.attn` "sleep" operation. If `true`,
        /// the host MCU will not be reset in these two conditions.
        CardDfu& operator()(bool v);
    } stop{};

    // Valid values for 'mode':
    //   "altdfu" — Enable the Notecard's `ALT_DFU` pins (instead of the `AUX` pins) for ...
    //   "aux" — Return the Notecard's `ALT_DFU` pins to their default state of `AUX`.
    // consteval: only callable at compile time (C++20)
#if __cplusplus >= 202002L
    static consteval note::string_view validatedMode(const char* v) {
        note::string_view sv{v};
        if (sv != "altdfu" && sv != "aux")
            throw "card.dfu: invalid value for 'mode'";
        return sv;
    }
#endif
    // Valid values for 'name':
    //   "esp32" — ESP32 microcontroller family.
    //   "stm32" — STM32 microcontroller family.
    //   "stm32-bi" — STM32 microcontroller family with boot inverted (boot pin active low).
    //   "mcuboot" — MCUboot compatible microcontroller (added in v5.3.1).
    //   "-" — Resets the configuration.
    // consteval: only callable at compile time (C++20)
#if __cplusplus >= 202002L
    static consteval note::string_view validatedName(const char* v) {
        note::string_view sv{v};
        if (sv != "esp32" && sv != "stm32" && sv != "stm32-bi" && sv != "mcuboot" && sv != "-")
            throw "card.dfu: invalid value for 'name'";
        return sv;
    }
#endif

    // Semantic convenience methods — generated from x-toggle / x-action metadata
    // (method names that match a field accessor are skipped to avoid redefinition)
    auto& enableUpdate() { on = true; return *this; }
    auto& disableUpdate() { off = true; return *this; }
    auto& enableUpdate(bool v_) { if (v_) on = true; else off = true; return *this; }
    auto& enableHostReset() { start = true; return *this; }
    auto& disableHostReset() { stop = true; return *this; }
    auto& enableHostReset(bool v_) { if (v_) start = true; else stop = true; return *this; }
#if NOTE_EXTRAS
    /// Add an arbitrary key/value pair to the request, beyond the typed fields
    /// declared above. Useful for fields the schema doesn't yet model.
    /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
    template<typename T>
    auto& extra(note::string_view k_, T v_) {
        if (extras_count_ < NOTE_EXTRAS_MAX)
            extras_[extras_count_++] = {k_, note::DynValue{v_}};
        return *this;
    }
    /// String-literal overload of extra().
    auto& extra(note::string_view k_, const char* v_) {
        return extra(k_, note::string_view{v_});
    }

    /// Index-style access to fields by wire name. Returns a DynField proxy
    /// usable for assignment; unknown keys are added as extras (subject to
    /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
    note::DynField operator[](note::string_view k_) {
        if (k_ == "mode") return note::dyn_field_for(mode);
        if (k_ == "name") return note::dyn_field_for(name);
        if (k_ == "off") return note::dyn_field_for(off);
        if (k_ == "on") return note::dyn_field_for(on);
        if (k_ == "seconds") return note::dyn_field_for(seconds);
        if (k_ == "start") return note::dyn_field_for(start);
        if (k_ == "stop") return note::dyn_field_for(stop);
        if (extras_count_ < NOTE_EXTRAS_MAX) {
            auto& slot = extras_[extras_count_++];
            slot.key = k_;
            return note::dyn_field_for(slot.value);
        }
        return {};
    }

    std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
    uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

    /// Successful response
    struct Response {
        /// Compile-time arena budget for this response type.
        static constexpr size_t max_arena_size =
            ::note::detail::arena_cost(49) +
            ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

        /// The class of MCU that the Notecard is currently configured to
        /// support for Outboard DFU.
        note::ResponseField<note::string_view> name{};

#if !NOTE_NO_JSON_TREE
        static Response parse(std::unique_ptr<JsonReader> reader_) {
            Response rsp;
            if (reader_->has("name")) rsp.name = reader_->get_string("name");
            rsp.reader_ = std::move(reader_);
            return rsp;
        }

        // Non-owning parse: string_views point into the reader's data.
        // The reader (and its underlying JSON buffer) must outlive the Response,
        // or the caller must consume all string fields before the reader is reused.
        static Response parse(const JsonReader& reader_) {
            Response rsp;
            if (reader_.has("name")) rsp.name = reader_.get_string("name");
            return rsp;
        }
#endif // !NOTE_NO_JSON_TREE

        // SAX sink — zero-allocation streaming parse into Response fields.
        // String fields are interned into the StringPool immediately, so
        // string_views survive after the parser's scratch buffer is reused.
        struct Sink : ::note::DefaultSink {
            Response& rsp;
            ::note::StringPool& pool_;
            Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {}
            NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                v_ = pool_.intern(v_);
                if (note::flash(keys_::rsp_name) == k_) { rsp.name = v_; return; }
            }
            NOTE_SINK_NOINLINE void reset() {
                rsp = Response{};
            }
        };

        void intern_strings(::note::StringPool& pool) {
            if (!name.empty()) name = pool.intern(name);
        }

#ifdef ARDUINO
        /// Arduino Printable: prints response fields to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{");
            bool first_ = true;
            if (!first_) n += p.print(",");
            first_ = false;
            n += p.print("\"name\":");
            n += note::detail::print_json_value(p, name.value());
            n += p.print("}");
            return n;
        }
#endif

#if !NOTE_NO_JSON_TREE
    private:
        std::unique_ptr<JsonReader> reader_;
#endif
    };

#if NOTE_SINGLETON
    /// Send this request to the Notecard and wait for a response.
    /// Returns an ApiResult<Response> — boolean-convertible to true on success;
    /// dereference (or use member-of-pointer ->) to read response fields,
    /// or call .error() to inspect the ErrorInfo on failure.
    ApiResult<Response> execute() const;
    /// Send this request as a fire-and-forget command (cmd) — the Notecard
    /// processes it without sending a response. Lower power and bandwidth
    /// than execute() when you don't need the result.
    Result<void> command() const;
#else
    ApiResult<Response>(*execute_fn_)(void*, const CardDfu&) = nullptr;
    Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
    /// Send this request to the Notecard and wait for a response.
    auto execute() const { return execute_fn_(nc_, *this); }
    /// Send this request as a fire-and-forget command (cmd) — the Notecard
    /// processes it without sending a response. Lower power and bandwidth
    /// than execute() when you don't need the result.
    Result<void> command() const {
        auto build_ = [&](JsonBuilder& b_) {
            b_.add("cmd", notecard_request);
            this->build(b_);
        };
        BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
            (*static_cast<decltype(build_)*>(p_))(b_);
        };
        return send_fn_(nc_, fn_, &build_);
    }
#endif

    private:
    void build(JsonBuilder& b) const;
    public:


#ifdef ARDUINO
    /// Arduino Printable: prints the JSON request to Serial or any Print stream.
    size_t printTo(Print& p) const {
        size_t n = p.print("{\"req\":\"");
        n += p.print(notecard_request.data());
        n += p.print("\"");
        if (mode) {
            n += p.print(",\"mode\":");
            n += note::detail::print_json_value(p, *mode);
        }
        if (name) {
            n += p.print(",\"name\":");
            n += note::detail::print_json_value(p, *name);
        }
        if (off) {
            n += p.print(",\"off\":");
            n += note::detail::print_json_value(p, *off);
        }
        if (on) {
            n += p.print(",\"on\":");
            n += note::detail::print_json_value(p, *on);
        }
        if (seconds) {
            n += p.print(",\"seconds\":");
            n += note::detail::print_json_value(p, *seconds);
        }
        if (start) {
            n += p.print(",\"start\":");
            n += note::detail::print_json_value(p, *start);
        }
        if (stop) {
            n += p.print(",\"stop\":");
            n += note::detail::print_json_value(p, *stop);
        }
        n += p.print("}");
        return n;
    }
#endif

    private:
    friend class ::note::Notecard;
    template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
    template<typename> friend class ::note::Api;
#else
    template<typename, typename> friend class ::note::Api;
#endif
    public:

};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardDfu& CardDfu::mode_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardDfu*>(
        reinterpret_cast<char*>(this) - offsetof(CardDfu, mode));
}
inline CardDfu& CardDfu::name_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardDfu*>(
        reinterpret_cast<char*>(this) - offsetof(CardDfu, name));
}
inline CardDfu& CardDfu::off_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardDfu*>(
        reinterpret_cast<char*>(this) - offsetof(CardDfu, off));
}
inline CardDfu& CardDfu::on_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardDfu*>(
        reinterpret_cast<char*>(this) - offsetof(CardDfu, on));
}
inline CardDfu& CardDfu::seconds_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardDfu*>(
        reinterpret_cast<char*>(this) - offsetof(CardDfu, seconds));
}
inline CardDfu& CardDfu::start_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardDfu*>(
        reinterpret_cast<char*>(this) - offsetof(CardDfu, start));
}
inline CardDfu& CardDfu::stop_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardDfu*>(
        reinterpret_cast<char*>(this) - offsetof(CardDfu, stop));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardDfu> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardDfu::keys_::rsp_name, static_cast<uint16_t>(offsetof(::note::api::CardDfu::Response, name)), ::note::FieldType::String},
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardDfu::keys_::mode, static_cast<uint16_t>(offsetof(::note::api::CardDfu, mode)), ::note::ReqFieldType::String},
            {::note::api::CardDfu::keys_::name, static_cast<uint16_t>(offsetof(::note::api::CardDfu, name)), ::note::ReqFieldType::String},
            {::note::api::CardDfu::keys_::off, static_cast<uint16_t>(offsetof(::note::api::CardDfu, off)), ::note::ReqFieldType::Bool},
            {::note::api::CardDfu::keys_::on, static_cast<uint16_t>(offsetof(::note::api::CardDfu, on)), ::note::ReqFieldType::Bool},
            {::note::api::CardDfu::keys_::seconds, static_cast<uint16_t>(offsetof(::note::api::CardDfu, seconds)), ::note::ReqFieldType::Int},
            {::note::api::CardDfu::keys_::start, static_cast<uint16_t>(offsetof(::note::api::CardDfu, start)), ::note::ReqFieldType::Bool},
            {::note::api::CardDfu::keys_::stop, static_cast<uint16_t>(offsetof(::note::api::CardDfu, stop)), ::note::ReqFieldType::Bool},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardDfu::build(JsonBuilder& b) const {
    using meta_ = ::note::detail::request_traits<CardDfu>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<typename CardDfu::Response> CardDfu::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardDfu>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardDfu::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardDfu>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif


} // namespace note::api
