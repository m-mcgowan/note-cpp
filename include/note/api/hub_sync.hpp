// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once

#include <note/dyn_field.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/notecard.hpp>
#include <note/safety.hpp>
#include <note/string_pool.hpp>
#include <note/types.hpp>
#include <note/target.hpp>

namespace note::api {








/// Manually initiates a sync with Notehub.
///
/// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
struct HubSync {
    static constexpr string_view notecard_request = "hub.sync";
    static constexpr bool supports_cmd = true;
    static constexpr Safety safety = Safety::NonIdempotent;
    static constexpr Skus skus{};

    Notecard* nc_ = nullptr;

    /// Set to `true` to remove the Notecard from certain types of penalty boxes
    /// (the default is `false`).
    struct allow_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        HubSync& operator()(bool v);
    } allow{};
    /// Set to `true` to only sync pending inbound Notefiles. Required when
    /// using NTN mode with Starnote to check for inbound Notefiles.
    struct in_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        HubSync& operator()(bool v);
    } in{};
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
    /// Set to `true` to only sync pending outbound Notefiles.
    ///
    /// @since{7.2.1}
#if NOTE_API_VERSION < NOTE_VERSION(7, 2, 1)
    [[deprecated("requires firmware >= 7.2.1")]]
#endif
    struct out_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        HubSync& operator()(bool v);
    } out{};
#endif


    template<typename T>
    auto& extra(note::string_view key, T value) {
        if (extras_count_ < NOTE_EXTRAS_MAX)
            extras_[extras_count_++] = {key, note::DynValue{value}};
        return *this;
    }
    auto& extra(note::string_view key, const char* value) {
        return extra(key, note::string_view{value});
    }

    note::DynField operator[](note::string_view k_) {
        if (k_ == "allow") return note::dyn_field_for(allow);
        if (k_ == "in") return note::dyn_field_for(in);
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
        if (k_ == "out") return note::dyn_field_for(out);
#endif
        if (extras_count_ < NOTE_EXTRAS_MAX) {
            auto& slot = extras_[extras_count_++];
            slot.key = k_;
            return note::dyn_field_for(slot.value);
        }
        return {};
    }

    std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
    uint8_t extras_count_ = 0;

    using Response = void;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
    void build(JsonBuilder& b) const {
        if (allow) b.add("allow", *allow);
        if (in) b.add("in", *in);
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
        if (out) b.add("out", *out);
#endif
        for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
            std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                       extras_[i_].value);
    }
#pragma GCC diagnostic pop

    auto execute() const { return nc_->execute(*this); }
    auto execute(Notecard& nc) const { return nc.execute(*this); }
    Result<void> command() const { return nc_->command_typed(*this); }
    Result<void> command(Notecard& nc) const { return nc.command_typed(*this); }

};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline HubSync& HubSync::allow_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<HubSync*>(
        reinterpret_cast<char*>(this) - offsetof(HubSync, allow));
}
inline HubSync& HubSync::in_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<HubSync*>(
        reinterpret_cast<char*>(this) - offsetof(HubSync, in));
}
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
inline HubSync& HubSync::out_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<HubSync*>(
        reinterpret_cast<char*>(this) - offsetof(HubSync, out));
}
#endif
#pragma GCC diagnostic pop


} // namespace note::api
