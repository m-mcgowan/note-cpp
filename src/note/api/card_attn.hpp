// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once
#include <note/note_config.hpp>

// NOTE_EXTRAS default is set in note_config.hpp.
#if NOTE_EXTRAS
#include <note/dyn_field.hpp>
#endif
#include <note/generic_builder.hpp>
#include <note/generic_sink.hpp>
#include <note/notecard.hpp>
#include <note/arena.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/binary_request.hpp>
#include <note/print.hpp>
#include <note/safety.hpp>
#include <note/string_pool.hpp>
#include <note/types.hpp>
#include <note/flag_set.hpp>
#include <note/array_field.hpp>
#include <note/response_array.hpp>
#include <note/progmem.hpp>
#include <note/target.hpp>

namespace note::attn {
    /// When armed, causes ATTN to fire if an AUX GPIO input changes. Disable by
    /// using `-auxgpio`.
    inline constexpr uint32_t auxgpio = 1u << 0;
    /// When armed, will cause ATTN to fire whenever the module connects to
    /// cellular. Disable with `-connected`.
    inline constexpr uint32_t connected = 1u << 1;
    /// When armed, causes ATTN to fire if an environment variable changes on
    /// the Notecard. Disable by using `-env`.
    inline constexpr uint32_t env = 1u << 2;
    /// When armed, will cause ATTN to fire if any of the "files" are modified.
    /// Disable by using `-files`.
    inline constexpr uint32_t files = 1u << 3;
    /// When armed, will cause ATTN to fire whenever the Notecard GPS module
    /// makes a position fix. Disable by using `-location`.
    inline constexpr uint32_t location = 1u << 4;
    /// When armed, will cause ATTN to fire whenever the accelerometer detects
    /// module motion. Disable with `-motion`.
    inline constexpr uint32_t motion = 1u << 5;
    /// When armed, will cause ATTN to fire whenever the `card.motion.mode`
    /// changes from "moving" to "stopped" (or vice versa). Learn how to
    /// configure this feature in this guide.
    inline constexpr uint32_t motionchange = 1u << 6;
    /// When armed, will cause ATTN to fire whenever the Notecard receives a
    /// signal.
    inline constexpr uint32_t signal = 1u << 7;
    /// When armed, will enable USB power events firing the ATTN pin. Disable
    /// with `-usb`.
    inline constexpr uint32_t usb = 1u << 8;
    /// Instruct the Notecard to fire the ATTN pin whenever the `card.wireless`
    /// status changes.
    inline constexpr uint32_t wireless = 1u << 9;
} // namespace note::attn

namespace note::api {









/// Configure hardware notifications from a Notecard to a host MCU.
///
/// NOTE: Requires a connection between the Notecard ATTN pin and a GPIO pin on
/// the host MCU.
///
/// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
struct CardAttn {

    struct Request {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.attn";
            static constexpr char files[] NOTE_FLASH_ATTR = "files";
            static constexpr char mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char off[] NOTE_FLASH_ATTR = "off";
            static constexpr char on[] NOTE_FLASH_ATTR = "on";
            static constexpr char payload[] NOTE_FLASH_ATTR = "payload";
            static constexpr char seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char start[] NOTE_FLASH_ATTR = "start";
            static constexpr char verify[] NOTE_FLASH_ATTR = "verify";
            static constexpr char rsp_files[] NOTE_FLASH_ATTR = "files";
            static constexpr char rsp_off[] NOTE_FLASH_ATTR = "off";
            static constexpr char rsp_payload[] NOTE_FLASH_ATTR = "payload";
            static constexpr char rsp_set[] NOTE_FLASH_ATTR = "set";
            static constexpr char rsp_time[] NOTE_FLASH_ATTR = "time";
        };

        static constexpr string_view notecard_request = "card.attn";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// A list of Notefiles to watch for file-based interrupts.
        /// A list of Notefiles to watch for file-based interrupts.
        note::ArrayField<note::string_view, 8> files{};
        /// A comma-separated list of one or more of the following keywords.
        /// Some keywords are only supported on certain types of Notecards.
        struct mode_t : Field<note::string_view> {
#if __cplusplus >= 202002L && !defined(__clang__)
            constexpr mode_t() = default;
            template<std::size_t N_>
            consteval mode_t(const char (&s)[N_])
                : Field<note::string_view>(note::string_view(s, N_ - 1)) {
                validate_flags(note::string_view(s, N_ - 1));
            }
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, mode_t>)
            constexpr mode_t(U&& v) : Field<note::string_view>(note::string_view(std::forward<U>(v))) {}
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, mode_t>)
            mode_t& operator=(U&& v) {
                Field<note::string_view>::operator=(note::string_view(std::forward<U>(v)));
                return *this;
            }
            mode_t& operator=(std::nullopt_t) { Field<note::string_view>::reset(); return *this; }
            mode_t(const mode_t& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            mode_t& operator=(const mode_t& o) { flags_ = o.flags_; fixup_(o); return *this; }
            mode_t(mode_t&& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            mode_t& operator=(mode_t&& o) { flags_ = o.flags_; fixup_(o); return *this; }
#else
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            mode_t(const mode_t& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            mode_t& operator=(const mode_t& o) { flags_ = o.flags_; fixup_(o); return *this; }
            mode_t(mode_t&& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            mode_t& operator=(mode_t&& o) { flags_ = o.flags_; fixup_(o); return *this; }
#endif
            /// A comma-separated list of one or more of the following keywords.
            /// Some keywords are only supported on certain types of Notecards.
            CardAttn::Request& operator()(note::string_view v);
            /// A comma-separated list of one or more of the following keywords.
            /// Some keywords are only supported on certain types of Notecards.
            CardAttn::Request& operator=(uint32_t flags);
            /// A comma-separated list of one or more of the following keywords.
            /// Some keywords are only supported on certain types of Notecards.
            CardAttn::Request& operator()(uint32_t flags);
            mode_t& add(uint32_t flag);
            mode_t& operator|=(uint32_t flag);
            /// When armed, causes ATTN to fire if an AUX GPIO input changes.
            /// Disable by using `-auxgpio`.
            mode_t& auxgpio();
            /// When armed, will cause ATTN to fire whenever the module connects
            /// to cellular. Disable with `-connected`.
            mode_t& connected();
            /// When armed, causes ATTN to fire if an environment variable
            /// changes on the Notecard. Disable by using `-env`.
            mode_t& env();
            /// When armed, will cause ATTN to fire if any of the "files" are
            /// modified. Disable by using `-files`.
            mode_t& files();
            /// When armed, will cause ATTN to fire whenever the Notecard GPS
            /// module makes a position fix. Disable by using `-location`.
            mode_t& location();
            /// When armed, will cause ATTN to fire whenever the accelerometer
            /// detects module motion. Disable with `-motion`.
            mode_t& motion();
            /// When armed, will cause ATTN to fire whenever the
            /// `card.motion.mode` changes from "moving" to "stopped" (or vice
            /// versa). Learn how to configure this feature in this guide.
            mode_t& motionchange();
            /// When armed, will cause ATTN to fire whenever the Notecard
            /// receives a signal.
            mode_t& signal();
            /// When armed, will enable USB power events firing the ATTN pin.
            /// Disable with `-usb`.
            mode_t& usb();
            /// Instruct the Notecard to fire the ATTN pin whenever the
            /// `card.wireless` status changes.
            mode_t& wireless();
            static constexpr note::FlagDef flag_defs_[] = {
                { note::attn::auxgpio, "auxgpio" },
                { note::attn::connected, "connected" },
                { note::attn::env, "env" },
                { note::attn::files, "files" },
                { note::attn::location, "location" },
                { note::attn::motion, "motion" },
                { note::attn::motionchange, "motionchange" },
                { note::attn::signal, "signal" },
                { note::attn::usb, "usb" },
                { note::attn::wireless, "wireless" },
            };
            note::FlagSet<10, 77> flags_{flag_defs_};
            void fixup_(const mode_t& o) {
                if (flags_) Field<note::string_view>::operator=(flags_.str());
                else if (o.has_value()) Field<note::string_view>::operator=(*o);
                else Field<note::string_view>::reset();
            }
#if __cplusplus >= 202002L
            // consteval: validates that a string literal contains only known flags.
            static consteval bool validate_flags(note::string_view sv) {
                while (!sv.empty()) {
                    auto comma = sv.find(',');
                    auto token = (comma == sv.npos) ? sv : sv.substr(0, comma);
                    if (token != "arm" && token != "rearm" && token != "disarm" && token != "sleep" && token != "watchdog" && token != "-all" && token != "-env" && token != "-files" && token != "-location" && token != "-motion" && token != "-usb" && token != "auxgpio" && token != "connected" && token != "env" && token != "files" && token != "location" && token != "motion" && token != "motionchange" && token != "signal" && token != "usb" && token != "wireless")
                        throw "card.attn: invalid flag";
                    sv = (comma == sv.npos) ? note::string_view{} : sv.substr(comma + 1);
                }
                return true;
            }
#endif
        } mode{};
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
        /// When `true`, completely disables ATTN processing and sets the pin
        /// OFF. This setting is retained across device restarts.
        ///
        /// @since{7.2.1}
#if NOTE_API_VERSION < NOTE_VERSION(7, 2, 1)
        [[deprecated("requires firmware >= 7.2.1")]]
#endif
        struct off_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// When `true`, completely disables ATTN processing and sets the
            /// pin OFF. This setting is retained across device restarts.
            ///
            /// @since{7.2.1}
            CardAttn::Request& operator()(bool v);
        } off{};
#endif
        /// When `true`, enables ATTN processing. This setting is retained
        /// across device restarts.
        struct on_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// When `true`, enables ATTN processing. This setting is retained
            /// across device restarts.
            CardAttn::Request& operator()(bool v);
        } on{};
        /// When using `sleep` mode, a payload of data from the host that the
        /// Notecard should hold in memory until retrieved by the host.
        struct payload_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// When using `sleep` mode, a payload of data from the host that
            /// the Notecard should hold in memory until retrieved by the host.
            CardAttn::Request& operator()(note::string_view v);
        } payload{};
        /// To set an ATTN timeout when arming, or when using `sleep`.
        ///
        /// NOTE: When the Notecard is in `continuous` mode, the `seconds`
        /// timeout is serviced by a routine that wakes every 15 seconds. You
        /// can predict when the device will wake, by rounding up to the nearest
        /// 15 second interval.
        struct seconds_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// To set an ATTN timeout when arming, or when using `sleep`.
            ///
            /// NOTE: When the Notecard is in `continuous` mode, the `seconds`
            /// timeout is serviced by a routine that wakes every 15 seconds.
            /// You can predict when the device will wake, by rounding up to the
            /// nearest 15 second interval.
            CardAttn::Request& operator()(note::json_int_t v);
        } seconds{};
        /// When using `sleep` mode and the host has reawakened, request the
        /// Notecard to return the stored `payload`.
        struct start_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// When using `sleep` mode and the host has reawakened, request the
            /// Notecard to return the stored `payload`.
            CardAttn::Request& operator()(bool v);
        } start{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
        /// When `true`, returns the current attention mode configuration, if
        /// any.
        ///
        /// @since{3.2.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 2, 1)
        [[deprecated("requires firmware >= 3.2.1")]]
#endif
        struct verify_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// When `true`, returns the current attention mode configuration,
            /// if any.
            ///
            /// @since{3.2.1}
            CardAttn::Request& operator()(bool v);
        } verify{};
#endif


    // Semantic convenience methods — generated from x-toggle / x-action metadata
    // (method names that match a field accessor are skipped to avoid redefinition)
        auto& persist() { on = true; return *this; }
        auto& persist(bool v_) { on = v_; return *this; }
        auto& auxgpio() { mode.auxgpio(); return *this; }
        auto& connected() { mode.connected(); return *this; }
        auto& env() { mode.env(); return *this; }
        auto& location() { mode.location(); return *this; }
        auto& motion() { mode.motion(); return *this; }
        auto& motionchange() { mode.motionchange(); return *this; }
        auto& signal() { mode.signal(); return *this; }
        auto& usb() { mode.usb(); return *this; }
        auto& wireless() { mode.wireless(); return *this; }
#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "mode") return note::dyn_field_for(mode);
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
            if (k_ == "off") return note::dyn_field_for(off);
#endif
            if (k_ == "on") return note::dyn_field_for(on);
            if (k_ == "payload") return note::dyn_field_for(payload);
            if (k_ == "seconds") return note::dyn_field_for(seconds);
            if (k_ == "start") return note::dyn_field_for(start);
#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
            if (k_ == "verify") return note::dyn_field_for(verify);
#endif
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Successful response
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(392) +
                ::note::detail::arena_cost(257) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// A list of files changed since `file` attention mode was set. In
            /// addition, this field will include keywords to signify the
            /// occurrence of other attention mode triggers:
            note::ResponseArray<note::printable_string_view, 8> files{};
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
            /// This field is present and set to `true` if ATTN processing has
            /// been disabled with the `off` argument.
            ///
            /// @since{7.2.1}
#if NOTE_API_VERSION < NOTE_VERSION(7, 2, 1)
            [[deprecated("requires firmware >= 7.2.1")]]
#endif
            note::ResponseField<bool> off{};
#endif
            /// When using `sleep` mode with a `payload`, the payload provided
            /// by the host to the Notecard.
            note::ResponseField<note::string_view> payload{};
            /// Reflects the state of the attention pin. The `set` field is
            /// `true` when the attention pin is `HIGH`, otherwise the `set`
            /// field will not be present when the attention pin is `LOW`.
            note::ResponseField<bool> set{};
            /// When using `sleep` mode with a `payload`, the time (UNIX Epoch
            /// time) that the payload was stored by the Notecard.
            note::ResponseField<note::json_int_t> time{};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                { note::string_view arr_[8]; auto n_ = reader_->get_string_array("files", arr_, 8); for (size_t i_ = 0; i_ < n_; ++i_) rsp.files.add(arr_[i_]); }
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
                if (reader_->has("off")) rsp.off = reader_->get_bool("off");
#endif
                if (reader_->has("payload")) rsp.payload = reader_->get_string("payload");
                if (reader_->has("set")) rsp.set = reader_->get_bool("set");
                if (reader_->has("time")) rsp.time = reader_->get_int("time");
                rsp.reader_ = std::move(reader_);
                return rsp;
            }
#pragma GCC diagnostic pop

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                { note::string_view arr_[8]; auto n_ = reader_.get_string_array("files", arr_, 8); for (size_t i_ = 0; i_ < n_; ++i_) rsp.files.add(arr_[i_]); }
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
                if (reader_.has("off")) rsp.off = reader_.get_bool("off");
#endif
                if (reader_.has("payload")) rsp.payload = reader_.get_string("payload");
                if (reader_.has("set")) rsp.set = reader_.get_bool("set");
                if (reader_.has("time")) rsp.time = reader_.get_int("time");
                return rsp;
            }
#pragma GCC diagnostic pop
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                enum class ArrayCtx_ : uint8_t { none, files } array_ctx_{};
                NOTE_SINK_NOINLINE void on_array_begin(::note::string_view k_) {
                    if (note::flash(keys_::rsp_files) == k_) { array_ctx_ = ArrayCtx_::files; return; }
                }
                NOTE_SINK_NOINLINE void on_array_end(::note::string_view k_) {
                    (void)k_; array_ctx_ = ArrayCtx_::none;
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (array_ctx_ != ArrayCtx_::none) {
                        // Array element — dispatch based on current array context
                        if (array_ctx_ == ArrayCtx_::files) { rsp.files.add(v_); return; }
                        return;
                    }
                    if (note::flash(keys_::rsp_payload) == k_) { rsp.payload = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_bool(::note::string_view k_, bool v_) {
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_off) == k_) { rsp.off = v_; return; }
#endif
                    if (note::flash(keys_::rsp_set) == k_) { rsp.set = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_number(::note::string_view k_, ::note::string_view raw_) {
                    if (note::flash(keys_::rsp_time) == k_) { rsp.time = ::note::parse_int(raw_); return; }
                }
                NOTE_SINK_NOINLINE void on_int(::note::string_view k_, ::note::json_int_t v_) {
                    if (note::flash(keys_::rsp_time) == k_) { rsp.time = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };
#pragma GCC diagnostic pop

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            void intern_strings(::note::StringPool& pool) {
                for (auto& v_ : files) if (!v_.empty()) v_ = pool.intern(v_);
                if (!payload.empty()) payload = pool.intern(payload);
            }
#pragma GCC diagnostic pop

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"files\":");
                n += p.print("[");
                for (size_t i_ = 0; i_ < files.size(); ++i_) {
                    if (i_) n += p.print(",");
                    n += note::detail::print_json_value(p, files[i_]);
                }
                n += p.print("]");
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"off\":");
                n += note::detail::print_json_value(p, off.value());
#endif
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"payload\":");
                n += note::detail::print_json_value(p, payload.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"set\":");
                n += note::detail::print_json_value(p, set.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"time\":");
                n += note::detail::print_json_value(p, time.value());
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardAttn::Request&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (mode) {
                n += p.print(",\"mode\":");
                n += note::detail::print_json_value(p, *mode);
            }
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
            if (off) {
                n += p.print(",\"off\":");
                n += note::detail::print_json_value(p, *off);
            }
#endif
            if (on) {
                n += p.print(",\"on\":");
                n += note::detail::print_json_value(p, *on);
            }
            if (payload) {
                n += p.print(",\"payload\":");
                n += note::detail::print_json_value(p, *payload);
            }
            if (seconds) {
                n += p.print(",\"seconds\":");
                n += note::detail::print_json_value(p, *seconds);
            }
            if (start) {
                n += p.print(",\"start\":");
                n += note::detail::print_json_value(p, *start);
            }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
            if (verify) {
                n += p.print(",\"verify\":");
                n += note::detail::print_json_value(p, *verify);
            }
#endif
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Arm ATTN pin for interrupt on an event trigger.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Arm {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.attn";
            static constexpr char files[] NOTE_FLASH_ATTR = "files";
            static constexpr char triggers[] NOTE_FLASH_ATTR = "mode";
            static constexpr char on[] NOTE_FLASH_ATTR = "on";
            static constexpr char seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char rsp_set[] NOTE_FLASH_ATTR = "set";
        };

        static constexpr string_view notecard_request = "card.attn";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// A list of Notefiles to watch for file-based interrupts.
        /// A list of Notefiles to watch for file-based interrupts.
        note::ArrayField<note::string_view, 8> files{};
        /// A comma-separated list of one or more of the following keywords.
        /// Some keywords are only supported on certain types of Notecards.
        struct triggers_t : Field<note::string_view> {
#if __cplusplus >= 202002L && !defined(__clang__)
            constexpr triggers_t() = default;
            template<std::size_t N_>
            consteval triggers_t(const char (&s)[N_])
                : Field<note::string_view>(note::string_view(s, N_ - 1)) {
                validate_flags(note::string_view(s, N_ - 1));
            }
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, triggers_t>)
            constexpr triggers_t(U&& v) : Field<note::string_view>(note::string_view(std::forward<U>(v))) {}
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, triggers_t>)
            triggers_t& operator=(U&& v) {
                Field<note::string_view>::operator=(note::string_view(std::forward<U>(v)));
                return *this;
            }
            triggers_t& operator=(std::nullopt_t) { Field<note::string_view>::reset(); return *this; }
            triggers_t(const triggers_t& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            triggers_t& operator=(const triggers_t& o) { flags_ = o.flags_; fixup_(o); return *this; }
            triggers_t(triggers_t&& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            triggers_t& operator=(triggers_t&& o) { flags_ = o.flags_; fixup_(o); return *this; }
#else
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            triggers_t(const triggers_t& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            triggers_t& operator=(const triggers_t& o) { flags_ = o.flags_; fixup_(o); return *this; }
            triggers_t(triggers_t&& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            triggers_t& operator=(triggers_t&& o) { flags_ = o.flags_; fixup_(o); return *this; }
#endif
            /// A comma-separated list of one or more of the following keywords.
            /// Some keywords are only supported on certain types of Notecards.
            CardAttn::Arm& operator()(note::string_view v);
            /// A comma-separated list of one or more of the following keywords.
            /// Some keywords are only supported on certain types of Notecards.
            CardAttn::Arm& operator=(uint32_t flags);
            /// A comma-separated list of one or more of the following keywords.
            /// Some keywords are only supported on certain types of Notecards.
            CardAttn::Arm& operator()(uint32_t flags);
            triggers_t& add(uint32_t flag);
            triggers_t& operator|=(uint32_t flag);
            /// When armed, causes ATTN to fire if an AUX GPIO input changes.
            /// Disable by using `-auxgpio`.
            triggers_t& auxgpio();
            /// When armed, will cause ATTN to fire whenever the module connects
            /// to cellular. Disable with `-connected`.
            triggers_t& connected();
            /// When armed, causes ATTN to fire if an environment variable
            /// changes on the Notecard. Disable by using `-env`.
            triggers_t& env();
            /// When armed, will cause ATTN to fire if any of the "files" are
            /// modified. Disable by using `-files`.
            triggers_t& files();
            /// When armed, will cause ATTN to fire whenever the Notecard GPS
            /// module makes a position fix. Disable by using `-location`.
            triggers_t& location();
            /// When armed, will cause ATTN to fire whenever the accelerometer
            /// detects module motion. Disable with `-motion`.
            triggers_t& motion();
            /// When armed, will cause ATTN to fire whenever the
            /// `card.motion.mode` changes from "moving" to "stopped" (or vice
            /// versa). Learn how to configure this feature in this guide.
            triggers_t& motionchange();
            /// When armed, will cause ATTN to fire whenever the Notecard
            /// receives a signal.
            triggers_t& signal();
            /// When armed, will enable USB power events firing the ATTN pin.
            /// Disable with `-usb`.
            triggers_t& usb();
            /// Instruct the Notecard to fire the ATTN pin whenever the
            /// `card.wireless` status changes.
            triggers_t& wireless();
            static constexpr note::FlagDef flag_defs_[] = {
                { note::attn::auxgpio, "auxgpio" },
                { note::attn::connected, "connected" },
                { note::attn::env, "env" },
                { note::attn::files, "files" },
                { note::attn::location, "location" },
                { note::attn::motion, "motion" },
                { note::attn::motionchange, "motionchange" },
                { note::attn::signal, "signal" },
                { note::attn::usb, "usb" },
                { note::attn::wireless, "wireless" },
            };
            note::FlagSet<10, 77> flags_{flag_defs_};
            void fixup_(const triggers_t& o) {
                if (flags_) Field<note::string_view>::operator=(flags_.str());
                else if (o.has_value()) Field<note::string_view>::operator=(*o);
                else Field<note::string_view>::reset();
            }
#if __cplusplus >= 202002L
            // consteval: validates that a string literal contains only known flags.
            static consteval bool validate_flags(note::string_view sv) {
                while (!sv.empty()) {
                    auto comma = sv.find(',');
                    auto token = (comma == sv.npos) ? sv : sv.substr(0, comma);
                    if (token != "arm" && token != "rearm" && token != "disarm" && token != "sleep" && token != "watchdog" && token != "-all" && token != "-env" && token != "-files" && token != "-location" && token != "-motion" && token != "-usb" && token != "auxgpio" && token != "connected" && token != "env" && token != "files" && token != "location" && token != "motion" && token != "motionchange" && token != "signal" && token != "usb" && token != "wireless")
                        throw "card.attn: invalid flag";
                    sv = (comma == sv.npos) ? note::string_view{} : sv.substr(comma + 1);
                }
                return true;
            }
#endif
        } triggers{};
        /// When `true`, enables ATTN processing. This setting is retained
        /// across device restarts.
        struct on_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// When `true`, enables ATTN processing. This setting is retained
            /// across device restarts.
            CardAttn::Arm& operator()(bool v);
        } on{};
        /// To set an ATTN timeout when arming, or when using `sleep`.
        ///
        /// NOTE: When the Notecard is in `continuous` mode, the `seconds`
        /// timeout is serviced by a routine that wakes every 15 seconds. You
        /// can predict when the device will wake, by rounding up to the nearest
        /// 15 second interval.
        struct seconds_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// To set an ATTN timeout when arming, or when using `sleep`.
            ///
            /// NOTE: When the Notecard is in `continuous` mode, the `seconds`
            /// timeout is serviced by a routine that wakes every 15 seconds.
            /// You can predict when the device will wake, by rounding up to the
            /// nearest 15 second interval.
            CardAttn::Arm& operator()(note::json_int_t v);
        } seconds{};


    // Semantic convenience methods — generated from x-toggle / x-action metadata
    // (method names that match a field accessor are skipped to avoid redefinition)
        auto& persist() { on = true; return *this; }
        auto& persist(bool v_) { on = v_; return *this; }
        auto& auxgpio() { triggers.auxgpio(); return *this; }
        auto& connected() { triggers.connected(); return *this; }
        auto& env() { triggers.env(); return *this; }
        auto& location() { triggers.location(); return *this; }
        auto& motion() { triggers.motion(); return *this; }
        auto& motionchange() { triggers.motionchange(); return *this; }
        auto& signal() { triggers.signal(); return *this; }
        auto& usb() { triggers.usb(); return *this; }
        auto& wireless() { triggers.wireless(); return *this; }
#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "mode") return note::dyn_field_for(triggers);
            if (k_ == "on") return note::dyn_field_for(on);
            if (k_ == "seconds") return note::dyn_field_for(seconds);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Arm ATTN pin for interrupt on an event trigger.
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// Reflects the state of the attention pin. The `set` field is
            /// `true` when the attention pin is `HIGH`, otherwise the `set`
            /// field will not be present when the attention pin is `LOW`.
            note::ResponseField<bool> set{};

#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("set")) rsp.set = reader_->get_bool("set");
                rsp.reader_ = std::move(reader_);
                return rsp;
            }

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("set")) rsp.set = reader_.get_bool("set");
                return rsp;
            }
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_bool(::note::string_view k_, bool v_) {
                    if (note::flash(keys_::rsp_set) == k_) { rsp.set = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };

            void intern_strings(::note::StringPool&) {}

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"set\":");
                n += note::detail::print_json_value(p, set.value());
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardAttn::Arm&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (triggers) {
                n += p.print(",\"mode\":");
                n += note::detail::print_json_value(p, *triggers);
            }
            if (on) {
                n += p.print(",\"on\":");
                n += note::detail::print_json_value(p, *on);
            }
            if (seconds) {
                n += p.print(",\"seconds\":");
                n += note::detail::print_json_value(p, *seconds);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Idempotent arm — arms ATTN if not already armed, or resets
    /// mode/files/seconds to the values given. Safe to call repeatedly (unlike
    /// arm which briefly disarms).
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Rearm {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.attn";
            static constexpr char files[] NOTE_FLASH_ATTR = "files";
            static constexpr char triggers[] NOTE_FLASH_ATTR = "mode";
            static constexpr char on[] NOTE_FLASH_ATTR = "on";
            static constexpr char seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char rsp_set[] NOTE_FLASH_ATTR = "set";
        };

        static constexpr string_view notecard_request = "card.attn";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// A list of Notefiles to watch for file-based interrupts.
        /// A list of Notefiles to watch for file-based interrupts.
        note::ArrayField<note::string_view, 8> files{};
        /// A comma-separated list of one or more of the following keywords.
        /// Some keywords are only supported on certain types of Notecards.
        struct triggers_t : Field<note::string_view> {
#if __cplusplus >= 202002L && !defined(__clang__)
            constexpr triggers_t() = default;
            template<std::size_t N_>
            consteval triggers_t(const char (&s)[N_])
                : Field<note::string_view>(note::string_view(s, N_ - 1)) {
                validate_flags(note::string_view(s, N_ - 1));
            }
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, triggers_t>)
            constexpr triggers_t(U&& v) : Field<note::string_view>(note::string_view(std::forward<U>(v))) {}
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, triggers_t>)
            triggers_t& operator=(U&& v) {
                Field<note::string_view>::operator=(note::string_view(std::forward<U>(v)));
                return *this;
            }
            triggers_t& operator=(std::nullopt_t) { Field<note::string_view>::reset(); return *this; }
            triggers_t(const triggers_t& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            triggers_t& operator=(const triggers_t& o) { flags_ = o.flags_; fixup_(o); return *this; }
            triggers_t(triggers_t&& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            triggers_t& operator=(triggers_t&& o) { flags_ = o.flags_; fixup_(o); return *this; }
#else
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            triggers_t(const triggers_t& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            triggers_t& operator=(const triggers_t& o) { flags_ = o.flags_; fixup_(o); return *this; }
            triggers_t(triggers_t&& o) : Field<note::string_view>(), flags_(o.flags_) { fixup_(o); }
            triggers_t& operator=(triggers_t&& o) { flags_ = o.flags_; fixup_(o); return *this; }
#endif
            /// A comma-separated list of one or more of the following keywords.
            /// Some keywords are only supported on certain types of Notecards.
            CardAttn::Rearm& operator()(note::string_view v);
            /// A comma-separated list of one or more of the following keywords.
            /// Some keywords are only supported on certain types of Notecards.
            CardAttn::Rearm& operator=(uint32_t flags);
            /// A comma-separated list of one or more of the following keywords.
            /// Some keywords are only supported on certain types of Notecards.
            CardAttn::Rearm& operator()(uint32_t flags);
            triggers_t& add(uint32_t flag);
            triggers_t& operator|=(uint32_t flag);
            /// When armed, causes ATTN to fire if an AUX GPIO input changes.
            /// Disable by using `-auxgpio`.
            triggers_t& auxgpio();
            /// When armed, will cause ATTN to fire whenever the module connects
            /// to cellular. Disable with `-connected`.
            triggers_t& connected();
            /// When armed, causes ATTN to fire if an environment variable
            /// changes on the Notecard. Disable by using `-env`.
            triggers_t& env();
            /// When armed, will cause ATTN to fire if any of the "files" are
            /// modified. Disable by using `-files`.
            triggers_t& files();
            /// When armed, will cause ATTN to fire whenever the Notecard GPS
            /// module makes a position fix. Disable by using `-location`.
            triggers_t& location();
            /// When armed, will cause ATTN to fire whenever the accelerometer
            /// detects module motion. Disable with `-motion`.
            triggers_t& motion();
            /// When armed, will cause ATTN to fire whenever the
            /// `card.motion.mode` changes from "moving" to "stopped" (or vice
            /// versa). Learn how to configure this feature in this guide.
            triggers_t& motionchange();
            /// When armed, will cause ATTN to fire whenever the Notecard
            /// receives a signal.
            triggers_t& signal();
            /// When armed, will enable USB power events firing the ATTN pin.
            /// Disable with `-usb`.
            triggers_t& usb();
            /// Instruct the Notecard to fire the ATTN pin whenever the
            /// `card.wireless` status changes.
            triggers_t& wireless();
            static constexpr note::FlagDef flag_defs_[] = {
                { note::attn::auxgpio, "auxgpio" },
                { note::attn::connected, "connected" },
                { note::attn::env, "env" },
                { note::attn::files, "files" },
                { note::attn::location, "location" },
                { note::attn::motion, "motion" },
                { note::attn::motionchange, "motionchange" },
                { note::attn::signal, "signal" },
                { note::attn::usb, "usb" },
                { note::attn::wireless, "wireless" },
            };
            note::FlagSet<10, 77> flags_{flag_defs_};
            void fixup_(const triggers_t& o) {
                if (flags_) Field<note::string_view>::operator=(flags_.str());
                else if (o.has_value()) Field<note::string_view>::operator=(*o);
                else Field<note::string_view>::reset();
            }
#if __cplusplus >= 202002L
            // consteval: validates that a string literal contains only known flags.
            static consteval bool validate_flags(note::string_view sv) {
                while (!sv.empty()) {
                    auto comma = sv.find(',');
                    auto token = (comma == sv.npos) ? sv : sv.substr(0, comma);
                    if (token != "arm" && token != "rearm" && token != "disarm" && token != "sleep" && token != "watchdog" && token != "-all" && token != "-env" && token != "-files" && token != "-location" && token != "-motion" && token != "-usb" && token != "auxgpio" && token != "connected" && token != "env" && token != "files" && token != "location" && token != "motion" && token != "motionchange" && token != "signal" && token != "usb" && token != "wireless")
                        throw "card.attn: invalid flag";
                    sv = (comma == sv.npos) ? note::string_view{} : sv.substr(comma + 1);
                }
                return true;
            }
#endif
        } triggers{};
        /// When `true`, enables ATTN processing. This setting is retained
        /// across device restarts.
        struct on_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// When `true`, enables ATTN processing. This setting is retained
            /// across device restarts.
            CardAttn::Rearm& operator()(bool v);
        } on{};
        /// To set an ATTN timeout when arming, or when using `sleep`.
        ///
        /// NOTE: When the Notecard is in `continuous` mode, the `seconds`
        /// timeout is serviced by a routine that wakes every 15 seconds. You
        /// can predict when the device will wake, by rounding up to the nearest
        /// 15 second interval.
        struct seconds_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// To set an ATTN timeout when arming, or when using `sleep`.
            ///
            /// NOTE: When the Notecard is in `continuous` mode, the `seconds`
            /// timeout is serviced by a routine that wakes every 15 seconds.
            /// You can predict when the device will wake, by rounding up to the
            /// nearest 15 second interval.
            CardAttn::Rearm& operator()(note::json_int_t v);
        } seconds{};


    // Semantic convenience methods — generated from x-toggle / x-action metadata
    // (method names that match a field accessor are skipped to avoid redefinition)
        auto& persist() { on = true; return *this; }
        auto& persist(bool v_) { on = v_; return *this; }
        auto& auxgpio() { triggers.auxgpio(); return *this; }
        auto& connected() { triggers.connected(); return *this; }
        auto& env() { triggers.env(); return *this; }
        auto& location() { triggers.location(); return *this; }
        auto& motion() { triggers.motion(); return *this; }
        auto& motionchange() { triggers.motionchange(); return *this; }
        auto& signal() { triggers.signal(); return *this; }
        auto& usb() { triggers.usb(); return *this; }
        auto& wireless() { triggers.wireless(); return *this; }
#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "mode") return note::dyn_field_for(triggers);
            if (k_ == "on") return note::dyn_field_for(on);
            if (k_ == "seconds") return note::dyn_field_for(seconds);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Idempotent arm — arms ATTN if not already armed, or resets
        /// mode/files/seconds to the values given. Safe to call repeatedly
        /// (unlike arm which briefly disarms).
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// Reflects the state of the attention pin. The `set` field is
            /// `true` when the attention pin is `HIGH`, otherwise the `set`
            /// field will not be present when the attention pin is `LOW`.
            note::ResponseField<bool> set{};

#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("set")) rsp.set = reader_->get_bool("set");
                rsp.reader_ = std::move(reader_);
                return rsp;
            }

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("set")) rsp.set = reader_.get_bool("set");
                return rsp;
            }
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_bool(::note::string_view k_, bool v_) {
                    if (note::flash(keys_::rsp_set) == k_) { rsp.set = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };

            void intern_strings(::note::StringPool&) {}

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"set\":");
                n += note::detail::print_json_value(p, set.value());
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardAttn::Rearm&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (triggers) {
                n += p.print(",\"mode\":");
                n += note::detail::print_json_value(p, *triggers);
            }
            if (on) {
                n += p.print(",\"on\":");
                n += note::detail::print_json_value(p, *on);
            }
            if (seconds) {
                n += p.print(",\"seconds\":");
                n += note::detail::print_json_value(p, *seconds);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Configure ATTN as a watchdog timer.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Watchdog {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.attn";
            static constexpr char seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char mode[] NOTE_FLASH_ATTR = "mode";
        };

        static constexpr string_view notecard_request = "card.attn";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// To set an ATTN timeout when arming, or when using `sleep`.
        ///
        /// NOTE: When the Notecard is in `continuous` mode, the `seconds`
        /// timeout is serviced by a routine that wakes every 15 seconds. You
        /// can predict when the device will wake, by rounding up to the nearest
        /// 15 second interval.
        struct seconds_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// To set an ATTN timeout when arming, or when using `sleep`.
            ///
            /// NOTE: When the Notecard is in `continuous` mode, the `seconds`
            /// timeout is serviced by a routine that wakes every 15 seconds.
            /// You can predict when the device will wake, by rounding up to the
            /// nearest 15 second interval.
            CardAttn::Watchdog& operator()(note::json_int_t v);
        } seconds{};


#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "seconds") return note::dyn_field_for(seconds);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        using Response = void;

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<void> — boolean-convertible to true on success;
        /// call .error() to inspect the ErrorInfo on failure.
        ApiResult<void> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardAttn::Watchdog&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (seconds) {
                n += p.print(",\"seconds\":");
                n += note::detail::print_json_value(p, *seconds);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Instruct host MCU to sleep with optional payload.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Sleep {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.attn";
            static constexpr char payload[] NOTE_FLASH_ATTR = "payload";
            static constexpr char seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char mode[] NOTE_FLASH_ATTR = "mode";
        };

        static constexpr string_view notecard_request = "card.attn";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// When using `sleep` mode, a payload of data from the host that the
        /// Notecard should hold in memory until retrieved by the host.
        struct payload_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// When using `sleep` mode, a payload of data from the host that
            /// the Notecard should hold in memory until retrieved by the host.
            CardAttn::Sleep& operator()(note::string_view v);
        } payload{};
        /// To set an ATTN timeout when arming, or when using `sleep`.
        ///
        /// NOTE: When the Notecard is in `continuous` mode, the `seconds`
        /// timeout is serviced by a routine that wakes every 15 seconds. You
        /// can predict when the device will wake, by rounding up to the nearest
        /// 15 second interval.
        struct seconds_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// To set an ATTN timeout when arming, or when using `sleep`.
            ///
            /// NOTE: When the Notecard is in `continuous` mode, the `seconds`
            /// timeout is serviced by a routine that wakes every 15 seconds.
            /// You can predict when the device will wake, by rounding up to the
            /// nearest 15 second interval.
            CardAttn::Sleep& operator()(note::json_int_t v);
        } seconds{};


#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "payload") return note::dyn_field_for(payload);
            if (k_ == "seconds") return note::dyn_field_for(seconds);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        using Response = void;

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<void> — boolean-convertible to true on success;
        /// call .error() to inspect the ErrorInfo on failure.
        ApiResult<void> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardAttn::Sleep&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (payload) {
                n += p.print(",\"payload\":");
                n += note::detail::print_json_value(p, *payload);
            }
            if (seconds) {
                n += p.print(",\"seconds\":");
                n += note::detail::print_json_value(p, *seconds);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Retrieve stored payload after sleep.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Retrieve {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.attn";
            static constexpr char start[] NOTE_FLASH_ATTR = "start";
            static constexpr char rsp_payload[] NOTE_FLASH_ATTR = "payload";
            static constexpr char rsp_time[] NOTE_FLASH_ATTR = "time";
        };

        static constexpr string_view notecard_request = "card.attn";
        static constexpr bool supports_cmd = false;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif



#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Retrieve stored payload after sleep.
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(257) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// When using `sleep` mode with a `payload`, the payload provided
            /// by the host to the Notecard.
            note::ResponseField<note::string_view> payload{};
            /// When using `sleep` mode with a `payload`, the time (UNIX Epoch
            /// time) that the payload was stored by the Notecard.
            note::ResponseField<note::json_int_t> time{};

#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("payload")) rsp.payload = reader_->get_string("payload");
                if (reader_->has("time")) rsp.time = reader_->get_int("time");
                rsp.reader_ = std::move(reader_);
                return rsp;
            }

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("payload")) rsp.payload = reader_.get_string("payload");
                if (reader_.has("time")) rsp.time = reader_.get_int("time");
                return rsp;
            }
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_payload) == k_) { rsp.payload = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_number(::note::string_view k_, ::note::string_view raw_) {
                    if (note::flash(keys_::rsp_time) == k_) { rsp.time = ::note::parse_int(raw_); return; }
                }
                NOTE_SINK_NOINLINE void on_int(::note::string_view k_, ::note::json_int_t v_) {
                    if (note::flash(keys_::rsp_time) == k_) { rsp.time = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };

            void intern_strings(::note::StringPool& pool) {
                if (!payload.empty()) payload = pool.intern(payload);
            }

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"payload\":");
                n += note::detail::print_json_value(p, payload.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"time\":");
                n += note::detail::print_json_value(p, time.value());
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardAttn::Retrieve&) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Disarm all attention interrupts.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Disarm {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.attn";
            static constexpr char mode[] NOTE_FLASH_ATTR = "mode";
        };

        static constexpr string_view notecard_request = "card.attn";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif



#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        using Response = void;

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<void> — boolean-convertible to true on success;
        /// call .error() to inspect the ErrorInfo on failure.
        ApiResult<void> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardAttn::Disarm&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Disable all ATTN processing. Unlike disarm, this completely stops ATTN
    /// monitoring rather than just clearing triggers.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Off {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.attn";
            static constexpr char off[] NOTE_FLASH_ATTR = "off";
        };

        static constexpr string_view notecard_request = "card.attn";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif



#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        using Response = void;

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<void> — boolean-convertible to true on success;
        /// call .error() to inspect the ErrorInfo on failure.
        ApiResult<void> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardAttn::Off&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Re-enable ATTN processing after it was disabled with off(). Retained
    /// across device restarts.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct On {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.attn";
            static constexpr char on[] NOTE_FLASH_ATTR = "on";
        };

        static constexpr string_view notecard_request = "card.attn";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif



#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        using Response = void;

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<void> — boolean-convertible to true on success;
        /// call .error() to inspect the ErrorInfo on failure.
        ApiResult<void> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardAttn::On&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Query current ATTN state and configuration.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Query {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.attn";
            static constexpr char verify[] NOTE_FLASH_ATTR = "verify";
            static constexpr char rsp_files[] NOTE_FLASH_ATTR = "files";
            static constexpr char rsp_off[] NOTE_FLASH_ATTR = "off";
            static constexpr char rsp_set[] NOTE_FLASH_ATTR = "set";
        };

        static constexpr string_view notecard_request = "card.attn";
        static constexpr bool supports_cmd = false;
        static constexpr Safety safety = Safety::ReadOnly;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
        /// When `true`, returns the current attention mode configuration, if
        /// any.
        ///
        /// @since{3.2.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 2, 1)
        [[deprecated("requires firmware >= 3.2.1")]]
#endif
        struct verify_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// When `true`, returns the current attention mode configuration,
            /// if any.
            ///
            /// @since{3.2.1}
            CardAttn::Query& operator()(bool v);
        } verify{};
#endif


#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
            if (k_ == "verify") return note::dyn_field_for(verify);
#endif
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Query current ATTN state and configuration.
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(392) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// A list of files changed since `file` attention mode was set. In
            /// addition, this field will include keywords to signify the
            /// occurrence of other attention mode triggers:
            note::ResponseArray<note::printable_string_view, 8> files{};
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
            /// This field is present and set to `true` if ATTN processing has
            /// been disabled with the `off` argument.
            ///
            /// @since{7.2.1}
#if NOTE_API_VERSION < NOTE_VERSION(7, 2, 1)
            [[deprecated("requires firmware >= 7.2.1")]]
#endif
            note::ResponseField<bool> off{};
#endif
            /// Reflects the state of the attention pin. The `set` field is
            /// `true` when the attention pin is `HIGH`, otherwise the `set`
            /// field will not be present when the attention pin is `LOW`.
            note::ResponseField<bool> set{};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                { note::string_view arr_[8]; auto n_ = reader_->get_string_array("files", arr_, 8); for (size_t i_ = 0; i_ < n_; ++i_) rsp.files.add(arr_[i_]); }
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
                if (reader_->has("off")) rsp.off = reader_->get_bool("off");
#endif
                if (reader_->has("set")) rsp.set = reader_->get_bool("set");
                rsp.reader_ = std::move(reader_);
                return rsp;
            }
#pragma GCC diagnostic pop

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                { note::string_view arr_[8]; auto n_ = reader_.get_string_array("files", arr_, 8); for (size_t i_ = 0; i_ < n_; ++i_) rsp.files.add(arr_[i_]); }
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
                if (reader_.has("off")) rsp.off = reader_.get_bool("off");
#endif
                if (reader_.has("set")) rsp.set = reader_.get_bool("set");
                return rsp;
            }
#pragma GCC diagnostic pop
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                enum class ArrayCtx_ : uint8_t { none, files } array_ctx_{};
                NOTE_SINK_NOINLINE void on_array_begin(::note::string_view k_) {
                    if (note::flash(keys_::rsp_files) == k_) { array_ctx_ = ArrayCtx_::files; return; }
                }
                NOTE_SINK_NOINLINE void on_array_end(::note::string_view k_) {
                    (void)k_; array_ctx_ = ArrayCtx_::none;
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    (void)k_;
                    if (array_ctx_ != ArrayCtx_::none) {
                        // Array element — dispatch based on current array context
                        if (array_ctx_ == ArrayCtx_::files) { rsp.files.add(v_); return; }
                        return;
                    }
                }
                NOTE_SINK_NOINLINE void on_bool(::note::string_view k_, bool v_) {
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_off) == k_) { rsp.off = v_; return; }
#endif
                    if (note::flash(keys_::rsp_set) == k_) { rsp.set = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };
#pragma GCC diagnostic pop

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            void intern_strings(::note::StringPool& pool) {
                for (auto& v_ : files) if (!v_.empty()) v_ = pool.intern(v_);
            }
#pragma GCC diagnostic pop

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"files\":");
                n += p.print("[");
                for (size_t i_ = 0; i_ < files.size(); ++i_) {
                    if (i_) n += p.print(",");
                    n += note::detail::print_json_value(p, files[i_]);
                }
                n += p.print("]");
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"off\":");
                n += note::detail::print_json_value(p, off.value());
#endif
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"set\":");
                n += note::detail::print_json_value(p, set.value());
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardAttn::Query&) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
            if (verify) {
                n += p.print(",\"verify\":");
                n += note::detail::print_json_value(p, *verify);
            }
#endif
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };
};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline CardAttn::Request& CardAttn::Request::mode_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardAttn::Request*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Request, mode));
}
inline CardAttn::Request& CardAttn::Request::mode_t::operator=(uint32_t flags) {
    flags_.set(flags);
    Field<note::string_view>::operator=(flags_.str());
    return *reinterpret_cast<CardAttn::Request*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Request, mode));
}
inline CardAttn::Request& CardAttn::Request::mode_t::operator()(uint32_t flags) {
    return operator=(flags);
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::add(uint32_t flag) {
    flags_.add(flag);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::operator|=(uint32_t flag) {
    flags_ |= flag;
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::auxgpio() {
    flags_.add(note::attn::auxgpio);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::connected() {
    flags_.add(note::attn::connected);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::env() {
    flags_.add(note::attn::env);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::files() {
    flags_.add(note::attn::files);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::location() {
    flags_.add(note::attn::location);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::motion() {
    flags_.add(note::attn::motion);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::motionchange() {
    flags_.add(note::attn::motionchange);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::signal() {
    flags_.add(note::attn::signal);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::usb() {
    flags_.add(note::attn::usb);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Request::mode_t& CardAttn::Request::mode_t::wireless() {
    flags_.add(note::attn::wireless);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
inline CardAttn::Request& CardAttn::Request::off_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardAttn::Request*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Request, off));
}
#endif
inline CardAttn::Request& CardAttn::Request::on_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardAttn::Request*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Request, on));
}
inline CardAttn::Request& CardAttn::Request::payload_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardAttn::Request*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Request, payload));
}
inline CardAttn::Request& CardAttn::Request::seconds_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardAttn::Request*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Request, seconds));
}
inline CardAttn::Request& CardAttn::Request::start_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardAttn::Request*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Request, start));
}
#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
inline CardAttn::Request& CardAttn::Request::verify_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardAttn::Request*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Request, verify));
}
#endif
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardAttn::Request> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardAttn::Request::keys_::rsp_files, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request::Response, files)), ::note::FieldType::StringArray},
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
        {::note::api::CardAttn::Request::keys_::rsp_off, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request::Response, off)), ::note::FieldType::Bool},
#endif
        {::note::api::CardAttn::Request::keys_::rsp_payload, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request::Response, payload)), ::note::FieldType::String},
        {::note::api::CardAttn::Request::keys_::rsp_set, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request::Response, set)), ::note::FieldType::Bool},
        {::note::api::CardAttn::Request::keys_::rsp_time, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request::Response, time)), ::note::FieldType::Int},
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardAttn::Request::keys_::files, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request, files)), ::note::ReqFieldType::Array},
            {::note::api::CardAttn::Request::keys_::mode, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request, mode)), ::note::ReqFieldType::String},
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
            {::note::api::CardAttn::Request::keys_::off, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request, off)), ::note::ReqFieldType::Bool},
#endif
            {::note::api::CardAttn::Request::keys_::on, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request, on)), ::note::ReqFieldType::Bool},
            {::note::api::CardAttn::Request::keys_::payload, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request, payload)), ::note::ReqFieldType::String},
            {::note::api::CardAttn::Request::keys_::seconds, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request, seconds)), ::note::ReqFieldType::Int},
            {::note::api::CardAttn::Request::keys_::start, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request, start)), ::note::ReqFieldType::Bool},
#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
            {::note::api::CardAttn::Request::keys_::verify, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Request, verify)), ::note::ReqFieldType::Bool},
#endif
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline void CardAttn::Request::build(JsonBuilder& b) const {
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
#endif
#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
#endif
    using meta_ = ::note::detail::request_traits<CardAttn::Request>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}
#pragma GCC diagnostic pop

#if NOTE_SINGLETON
inline ApiResult<typename CardAttn::Request::Response> CardAttn::Request::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardAttn::Request>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardAttn::Request::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardAttn::Request>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardAttn::Arm& CardAttn::Arm::triggers_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardAttn::Arm*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Arm, triggers));
}
inline CardAttn::Arm& CardAttn::Arm::triggers_t::operator=(uint32_t flags) {
    flags_.set(flags);
    Field<note::string_view>::operator=(flags_.str());
    return *reinterpret_cast<CardAttn::Arm*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Arm, triggers));
}
inline CardAttn::Arm& CardAttn::Arm::triggers_t::operator()(uint32_t flags) {
    return operator=(flags);
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::add(uint32_t flag) {
    flags_.add(flag);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::operator|=(uint32_t flag) {
    flags_ |= flag;
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::auxgpio() {
    flags_.add(note::attn::auxgpio);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::connected() {
    flags_.add(note::attn::connected);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::env() {
    flags_.add(note::attn::env);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::files() {
    flags_.add(note::attn::files);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::location() {
    flags_.add(note::attn::location);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::motion() {
    flags_.add(note::attn::motion);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::motionchange() {
    flags_.add(note::attn::motionchange);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::signal() {
    flags_.add(note::attn::signal);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::usb() {
    flags_.add(note::attn::usb);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm::triggers_t& CardAttn::Arm::triggers_t::wireless() {
    flags_.add(note::attn::wireless);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Arm& CardAttn::Arm::on_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardAttn::Arm*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Arm, on));
}
inline CardAttn::Arm& CardAttn::Arm::seconds_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardAttn::Arm*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Arm, seconds));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardAttn::Arm> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardAttn::Arm::keys_::rsp_set, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Arm::Response, set)), ::note::FieldType::Bool},
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardAttn::Arm::keys_::files, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Arm, files)), ::note::ReqFieldType::Array},
            {::note::api::CardAttn::Arm::keys_::on, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Arm, on)), ::note::ReqFieldType::Bool},
            {::note::api::CardAttn::Arm::keys_::seconds, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Arm, seconds)), ::note::ReqFieldType::Int},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardAttn::Arm::build(JsonBuilder& b) const {
    if (triggers) {
        char mp_[96];
        std::snprintf(mp_, sizeof(mp_), "arm,%.*s",
                     static_cast<int>((*triggers).size()), (*triggers).data());
        note::add_flash(b, note::flash(keys_::triggers), note::string_view{mp_});
    } else {
        note::add_flash(b, note::flash(keys_::triggers), "arm");
    }
    using meta_ = ::note::detail::request_traits<CardAttn::Arm>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<typename CardAttn::Arm::Response> CardAttn::Arm::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardAttn::Arm>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardAttn::Arm::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardAttn::Arm>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardAttn::Rearm& CardAttn::Rearm::triggers_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardAttn::Rearm*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Rearm, triggers));
}
inline CardAttn::Rearm& CardAttn::Rearm::triggers_t::operator=(uint32_t flags) {
    flags_.set(flags);
    Field<note::string_view>::operator=(flags_.str());
    return *reinterpret_cast<CardAttn::Rearm*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Rearm, triggers));
}
inline CardAttn::Rearm& CardAttn::Rearm::triggers_t::operator()(uint32_t flags) {
    return operator=(flags);
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::add(uint32_t flag) {
    flags_.add(flag);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::operator|=(uint32_t flag) {
    flags_ |= flag;
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::auxgpio() {
    flags_.add(note::attn::auxgpio);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::connected() {
    flags_.add(note::attn::connected);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::env() {
    flags_.add(note::attn::env);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::files() {
    flags_.add(note::attn::files);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::location() {
    flags_.add(note::attn::location);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::motion() {
    flags_.add(note::attn::motion);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::motionchange() {
    flags_.add(note::attn::motionchange);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::signal() {
    flags_.add(note::attn::signal);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::usb() {
    flags_.add(note::attn::usb);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm::triggers_t& CardAttn::Rearm::triggers_t::wireless() {
    flags_.add(note::attn::wireless);
    Field<note::string_view>::operator=(flags_.str());
    return *this;
}
inline CardAttn::Rearm& CardAttn::Rearm::on_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardAttn::Rearm*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Rearm, on));
}
inline CardAttn::Rearm& CardAttn::Rearm::seconds_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardAttn::Rearm*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Rearm, seconds));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardAttn::Rearm> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardAttn::Rearm::keys_::rsp_set, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Rearm::Response, set)), ::note::FieldType::Bool},
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardAttn::Rearm::keys_::files, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Rearm, files)), ::note::ReqFieldType::Array},
            {::note::api::CardAttn::Rearm::keys_::on, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Rearm, on)), ::note::ReqFieldType::Bool},
            {::note::api::CardAttn::Rearm::keys_::seconds, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Rearm, seconds)), ::note::ReqFieldType::Int},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardAttn::Rearm::build(JsonBuilder& b) const {
    if (triggers) {
        char mp_[96];
        std::snprintf(mp_, sizeof(mp_), "rearm,%.*s",
                     static_cast<int>((*triggers).size()), (*triggers).data());
        note::add_flash(b, note::flash(keys_::triggers), note::string_view{mp_});
    } else {
        note::add_flash(b, note::flash(keys_::triggers), "rearm");
    }
    using meta_ = ::note::detail::request_traits<CardAttn::Rearm>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<typename CardAttn::Rearm::Response> CardAttn::Rearm::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardAttn::Rearm>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardAttn::Rearm::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardAttn::Rearm>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardAttn::Watchdog& CardAttn::Watchdog::seconds_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardAttn::Watchdog*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Watchdog, seconds));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardAttn::Watchdog> {
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardAttn::Watchdog::keys_::seconds, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Watchdog, seconds)), ::note::ReqFieldType::Int},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_void_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, ::note::detail::NcErrorCapture&, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardAttn::Watchdog::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::mode), "watchdog");
    using meta_ = ::note::detail::request_traits<CardAttn::Watchdog>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<void> CardAttn::Watchdog::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    ::note::detail::NcErrorCapture nc_err_;
    using meta_ = ::note::detail::request_traits<CardAttn::Watchdog>;
    auto rv_ = meta_::execute_void_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, nc_err_, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<void>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    return ApiResult<void>{};
}
inline Result<void> CardAttn::Watchdog::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardAttn::Watchdog>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardAttn::Sleep& CardAttn::Sleep::payload_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardAttn::Sleep*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Sleep, payload));
}
inline CardAttn::Sleep& CardAttn::Sleep::seconds_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardAttn::Sleep*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Sleep, seconds));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardAttn::Sleep> {
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardAttn::Sleep::keys_::payload, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Sleep, payload)), ::note::ReqFieldType::String},
            {::note::api::CardAttn::Sleep::keys_::seconds, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Sleep, seconds)), ::note::ReqFieldType::Int},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_void_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, ::note::detail::NcErrorCapture&, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardAttn::Sleep::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::mode), "sleep");
    using meta_ = ::note::detail::request_traits<CardAttn::Sleep>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<void> CardAttn::Sleep::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    ::note::detail::NcErrorCapture nc_err_;
    using meta_ = ::note::detail::request_traits<CardAttn::Sleep>;
    auto rv_ = meta_::execute_void_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, nc_err_, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<void>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    return ApiResult<void>{};
}
inline Result<void> CardAttn::Sleep::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardAttn::Sleep>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif


} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardAttn::Retrieve> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardAttn::Retrieve::keys_::rsp_payload, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Retrieve::Response, payload)), ::note::FieldType::String},
        {::note::api::CardAttn::Retrieve::keys_::rsp_time, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Retrieve::Response, time)), ::note::FieldType::Int},
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardAttn::Retrieve::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::start), true);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<typename CardAttn::Retrieve::Response> CardAttn::Retrieve::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardAttn::Retrieve>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    return ApiResult<Response>(std::move(rsp_));
}
#endif


} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardAttn::Disarm> {
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_void_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, ::note::detail::NcErrorCapture&, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardAttn::Disarm::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::mode), "disarm,-all");
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<void> CardAttn::Disarm::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    ::note::detail::NcErrorCapture nc_err_;
    using meta_ = ::note::detail::request_traits<CardAttn::Disarm>;
    auto rv_ = meta_::execute_void_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, nc_err_, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<void>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    return ApiResult<void>{};
}
inline Result<void> CardAttn::Disarm::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardAttn::Disarm>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif


} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardAttn::Off> {
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_void_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, ::note::detail::NcErrorCapture&, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardAttn::Off::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::off), true);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<void> CardAttn::Off::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    ::note::detail::NcErrorCapture nc_err_;
    using meta_ = ::note::detail::request_traits<CardAttn::Off>;
    auto rv_ = meta_::execute_void_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, nc_err_, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<void>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    return ApiResult<void>{};
}
inline Result<void> CardAttn::Off::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardAttn::Off>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif


} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardAttn::On> {
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_void_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, ::note::detail::NcErrorCapture&, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardAttn::On::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::on), true);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<void> CardAttn::On::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    ::note::detail::NcErrorCapture nc_err_;
    using meta_ = ::note::detail::request_traits<CardAttn::On>;
    auto rv_ = meta_::execute_void_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, nc_err_, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<void>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    return ApiResult<void>{};
}
inline Result<void> CardAttn::On::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardAttn::On>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
inline CardAttn::Query& CardAttn::Query::verify_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardAttn::Query*>(
        reinterpret_cast<char*>(this) - offsetof(CardAttn::Query, verify));
}
#endif
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardAttn::Query> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardAttn::Query::keys_::rsp_files, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Query::Response, files)), ::note::FieldType::StringArray},
#if NOTE_API_VERSION >= NOTE_VERSION(7, 2, 1) || !defined(NOTE_API_STRICT)
        {::note::api::CardAttn::Query::keys_::rsp_off, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Query::Response, off)), ::note::FieldType::Bool},
#endif
        {::note::api::CardAttn::Query::keys_::rsp_set, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Query::Response, set)), ::note::FieldType::Bool},
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
            {::note::api::CardAttn::Query::keys_::verify, static_cast<uint16_t>(offsetof(::note::api::CardAttn::Query, verify)), ::note::ReqFieldType::Bool},
#endif
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline void CardAttn::Query::build(JsonBuilder& b) const {
#if NOTE_API_VERSION >= NOTE_VERSION(3, 2, 1) || !defined(NOTE_API_STRICT)
#endif
    using meta_ = ::note::detail::request_traits<CardAttn::Query>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}
#pragma GCC diagnostic pop

#if NOTE_SINGLETON
inline ApiResult<typename CardAttn::Query::Response> CardAttn::Query::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardAttn::Query>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    return ApiResult<Response>(std::move(rsp_));
}
#endif


} // namespace note::api
