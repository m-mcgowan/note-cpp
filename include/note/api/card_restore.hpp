// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once

#include <note/dyn_field.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/notecard.hpp>
#include <note/safety.hpp>
#include <note/types.hpp>
#include <note/target.hpp>

namespace note::api {







struct CardRestore {
    static constexpr string_view notecard_request = "card.restore";
    static constexpr bool supports_cmd = true;
    static constexpr Safety safety = Safety::Destructive;
    static constexpr Skus skus{};

    Notecard* nc_ = nullptr;

    /// Set to `true` to reset the Notecard on Notehub. This will delete and
    /// deprovision the Notecard from Notehub the next time the Notecard
    /// connects. This also removes any Notefile templates used by this device.
    ///
    /// Conversely, if `connected` is `false` (or omitted), the Notecard's
    /// settings and data will be restored from Notehub the next time the
    /// Notecard connects to the previously used Notehub project.
    struct connected_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardRestore& operator()(bool v);
    } connected{};
    /// Set to `true` to reset most Notecard configuration settings. Note that
    /// this does not reset stored WiFi credentials or the [alternate I2C
    /// address](/notecard/notecard-walkthrough/advanced-notecard-
    /// configuration/#change-the-notecard-i2c-address) (if previously set) so
    /// the Notecard can still contact the network after a reset.
    ///
    /// *The Notecard will be unable to sync with Notehub until the `ProductUID`
    /// is set again.*
    struct delete_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardRestore& operator()(bool v);
    } delete_{};


    template<typename T>
    auto& extra(note::string_view key, T value) {
        if (extras_count_ < NOTE_EXTRAS_MAX)
            extras_[extras_count_++] = {key, note::DynValue{value}};
        return *this;
    }
    auto& extra(note::string_view key, const char* value) {
        return extra(key, note::string_view{value});
    }

    note::DynField operator[](note::string_view k_) {
        if (k_ == "connected") return note::dyn_field_for(connected);
        if (k_ == "delete") return note::dyn_field_for(delete_);
        if (extras_count_ < NOTE_EXTRAS_MAX) {
            auto& slot = extras_[extras_count_++];
            slot.key = k_;
            return note::dyn_field_for(slot.value);
        }
        return {};
    }

    std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
    uint8_t extras_count_ = 0;

    struct Response {

        static Response parse(std::unique_ptr<JsonReader> reader_) {
            (void)reader_;
            return {};
        }

        // Non-owning parse: string_views point into the reader's data.
        // The reader (and its underlying JSON buffer) must outlive the Response,
        // or the caller must consume all string fields before the reader is reused.
        static Response parse(const JsonReader& reader_) {
            (void)reader_;
            return {};
        }
    };

    void build(JsonBuilder& b) const {
        if (connected) b.add("connected", *connected);
        if (delete_) b.add("delete", *delete_);
        for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
            std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                       extras_[i_].value);
    }

    auto execute() const { return nc_->execute(*this); }
    auto execute(Notecard& nc) const { return nc.execute(*this); }
    Result<void> command() const { return nc_->command_typed(*this); }
    Result<void> command(Notecard& nc) const { return nc.command_typed(*this); }

};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardRestore& CardRestore::connected_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardRestore*>(
        reinterpret_cast<char*>(this) - offsetof(CardRestore, connected));
}
inline CardRestore& CardRestore::delete_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardRestore*>(
        reinterpret_cast<char*>(this) - offsetof(CardRestore, delete_));
}
#pragma GCC diagnostic pop


} // namespace note::api
