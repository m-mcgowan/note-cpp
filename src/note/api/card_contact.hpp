// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once
#include <note/note_config.hpp>

#ifndef NOTE_EXTRAS
#define NOTE_EXTRAS 1
#endif
#if NOTE_EXTRAS
#include <note/dyn_field.hpp>
#endif
#include <note/generic_sink.hpp>
#include <note/notecard.hpp>
#include <note/arena.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/binary_request.hpp>
#include <note/print.hpp>
#include <note/safety.hpp>
#include <note/string_pool.hpp>
#include <note/types.hpp>
#include <note/progmem.hpp>
#include <note/target.hpp>

namespace note::api {









/// Used to set or retrieve information about the Notecard maintainer. Once set,
/// this information is synced to Notehub.
///
/// @skus{CELL,CELL+WIFI,SKYLO,WIFI}
struct CardContact {

    struct Get {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.contact";
            static constexpr char email[] NOTE_FLASH_ATTR = "email";
            static constexpr char name[] NOTE_FLASH_ATTR = "name";
            static constexpr char org[] NOTE_FLASH_ATTR = "org";
            static constexpr char role[] NOTE_FLASH_ATTR = "role";
            static constexpr char rsp_email[] NOTE_FLASH_ATTR = "email";
            static constexpr char rsp_name[] NOTE_FLASH_ATTR = "name";
            static constexpr char rsp_org[] NOTE_FLASH_ATTR = "org";
            static constexpr char rsp_role[] NOTE_FLASH_ATTR = "role";
        };

        static constexpr string_view notecard_request = "card.contact";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::ReadOnly;
        static constexpr Skus skus = Skus::from(Product::Cell, Product::CellWifi, Product::Skylo, Product::WiFi);

#if NOTE_SINGLETON
        static inline void* nc_;
#else
        void* nc_ = nullptr;
#endif


        /// Set the email address of the Notecard maintainer.
        struct email_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            CardContact::Get& operator()(note::string_view v);
        } email{};
        /// Set the name of the Notecard maintainer.
        struct name_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            CardContact::Get& operator()(note::string_view v);
        } name{};
        /// Set the organization name of the Notecard maintainer.
        struct org_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            CardContact::Get& operator()(note::string_view v);
        } org{};
        /// Set the role of the Notecard maintainer.
        struct role_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            CardContact::Get& operator()(note::string_view v);
        } role{};


#if NOTE_EXTRAS
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        note::DynField operator[](note::string_view k_) {
            if (k_ == "email") return note::dyn_field_for(email);
            if (k_ == "name") return note::dyn_field_for(name);
            if (k_ == "org") return note::dyn_field_for(org);
            if (k_ == "role") return note::dyn_field_for(role);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Successful response
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(64) +
                ::note::detail::arena_cost(48) +
                ::note::detail::arena_cost(48) +
                ::note::detail::arena_cost(32) +
                ::note::detail::arena_cost(64);  // error reserve

            /// Email address of the Notecard maintainer.
            note::ResponseField<note::string_view> email{};
            /// Name of the Notecard maintainer.
            note::ResponseField<note::string_view> name{};
            /// Organization name of the Notecard maintainer.
            note::ResponseField<note::string_view> org{};
            /// Role of the Notecard maintainer.
            note::ResponseField<note::string_view> role{};

            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                rsp.email = reader_->get_string("email");
                rsp.name = reader_->get_string("name");
                rsp.org = reader_->get_string("org");
                rsp.role = reader_->get_string("role");
                rsp.reader_ = std::move(reader_);
                return rsp;
            }

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                rsp.email = reader_.get_string("email");
                rsp.name = reader_.get_string("name");
                rsp.org = reader_.get_string("org");
                rsp.role = reader_.get_string("role");
                return rsp;
            }

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {}
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_email) == k_) { rsp.email = v_; return; }
                    if (note::flash(keys_::rsp_name) == k_) { rsp.name = v_; return; }
                    if (note::flash(keys_::rsp_org) == k_) { rsp.org = v_; return; }
                    if (note::flash(keys_::rsp_role) == k_) { rsp.role = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };

            void intern_strings(::note::StringPool& pool) {
                if (!email.empty()) email = pool.intern(email);
                if (!name.empty()) name = pool.intern(name);
                if (!org.empty()) org = pool.intern(org);
                if (!role.empty()) role = pool.intern(role);
            }

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"email\":");
                n += note::detail::print_json_value(p, email.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"name\":");
                n += note::detail::print_json_value(p, name.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"org\":");
                n += note::detail::print_json_value(p, org.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"role\":");
                n += note::detail::print_json_value(p, role.value());
                n += p.print("}");
                return n;
            }
#endif

        private:
            std::unique_ptr<JsonReader> reader_;
        };
        static constexpr uint8_t field_count = 4;
        static const ::note::FieldDesc* field_descs_ptr() {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
            static constexpr ::note::FieldDesc table[] = {
                {"email", static_cast<uint16_t>(offsetof(Response, email)), ::note::FieldType::String},
                {"name", static_cast<uint16_t>(offsetof(Response, name)), ::note::FieldType::String},
                {"org", static_cast<uint16_t>(offsetof(Response, org)), ::note::FieldType::String},
                {"role", static_cast<uint16_t>(offsetof(Response, role)), ::note::FieldType::String},
            };
#pragma GCC diagnostic pop
            return table;
        }

#if NOTE_SINGLETON
        /// Singleton: type-erased execute — one static fn ptr per type, set by Api.
        static inline ApiResult<Response>(*execute_fn_)(void*, const CardContact::Get&);
        static inline Result<void>(*send_fn_)(void*, BuildFn, void*);
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardContact::Get&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
#endif
        auto execute() const { return execute_fn_(nc_, *this); }
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }

        void build(JsonBuilder& b) const {
            if (email) note::add_flash(b, note::flash(keys_::email), *email);
            if (name) note::add_flash(b, note::flash(keys_::name), *name);
            if (org) note::add_flash(b, note::flash(keys_::org), *org);
            if (role) note::add_flash(b, note::flash(keys_::role), *role);
#if NOTE_EXTRAS
            for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
                std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                           extras_[i_].value);
#endif
        }


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (email) {
                n += p.print(",\"email\":");
                n += note::detail::print_json_value(p, *email);
            }
            if (name) {
                n += p.print(",\"name\":");
                n += note::detail::print_json_value(p, *name);
            }
            if (org) {
                n += p.print(",\"org\":");
                n += note::detail::print_json_value(p, *org);
            }
            if (role) {
                n += p.print(",\"role\":");
                n += note::detail::print_json_value(p, *role);
            }
            n += p.print("}");
            return n;
        }
#endif

    };

    /// Used to set or retrieve information about the Notecard maintainer. Once
    /// set, this information is synced to Notehub.
    ///
    /// @skus{CELL,CELL+WIFI,SKYLO,WIFI}
    struct Set {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.contact";
            static constexpr char email[] NOTE_FLASH_ATTR = "email";
            static constexpr char name[] NOTE_FLASH_ATTR = "name";
            static constexpr char org[] NOTE_FLASH_ATTR = "org";
            static constexpr char role[] NOTE_FLASH_ATTR = "role";
            static constexpr char rsp_email[] NOTE_FLASH_ATTR = "email";
            static constexpr char rsp_name[] NOTE_FLASH_ATTR = "name";
            static constexpr char rsp_org[] NOTE_FLASH_ATTR = "org";
            static constexpr char rsp_role[] NOTE_FLASH_ATTR = "role";
        };

        static constexpr string_view notecard_request = "card.contact";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr Skus skus = Skus::from(Product::Cell, Product::CellWifi, Product::Skylo, Product::WiFi);

#if NOTE_SINGLETON
        static inline void* nc_;
#else
        void* nc_ = nullptr;
#endif


        /// Set the email address of the Notecard maintainer.
        struct email_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            CardContact::Set& operator()(note::string_view v);
        } email{};
        /// Set the name of the Notecard maintainer.
        struct name_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            CardContact::Set& operator()(note::string_view v);
        } name{};
        /// Set the organization name of the Notecard maintainer.
        struct org_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            CardContact::Set& operator()(note::string_view v);
        } org{};
        /// Set the role of the Notecard maintainer.
        struct role_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            CardContact::Set& operator()(note::string_view v);
        } role{};


#if NOTE_EXTRAS
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        note::DynField operator[](note::string_view k_) {
            if (k_ == "email") return note::dyn_field_for(email);
            if (k_ == "name") return note::dyn_field_for(name);
            if (k_ == "org") return note::dyn_field_for(org);
            if (k_ == "role") return note::dyn_field_for(role);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Successful response
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(64) +
                ::note::detail::arena_cost(48) +
                ::note::detail::arena_cost(48) +
                ::note::detail::arena_cost(32) +
                ::note::detail::arena_cost(64);  // error reserve

            /// Email address of the Notecard maintainer.
            note::ResponseField<note::string_view> email{};
            /// Name of the Notecard maintainer.
            note::ResponseField<note::string_view> name{};
            /// Organization name of the Notecard maintainer.
            note::ResponseField<note::string_view> org{};
            /// Role of the Notecard maintainer.
            note::ResponseField<note::string_view> role{};

            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                rsp.email = reader_->get_string("email");
                rsp.name = reader_->get_string("name");
                rsp.org = reader_->get_string("org");
                rsp.role = reader_->get_string("role");
                rsp.reader_ = std::move(reader_);
                return rsp;
            }

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                rsp.email = reader_.get_string("email");
                rsp.name = reader_.get_string("name");
                rsp.org = reader_.get_string("org");
                rsp.role = reader_.get_string("role");
                return rsp;
            }

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {}
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_email) == k_) { rsp.email = v_; return; }
                    if (note::flash(keys_::rsp_name) == k_) { rsp.name = v_; return; }
                    if (note::flash(keys_::rsp_org) == k_) { rsp.org = v_; return; }
                    if (note::flash(keys_::rsp_role) == k_) { rsp.role = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };

            void intern_strings(::note::StringPool& pool) {
                if (!email.empty()) email = pool.intern(email);
                if (!name.empty()) name = pool.intern(name);
                if (!org.empty()) org = pool.intern(org);
                if (!role.empty()) role = pool.intern(role);
            }

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"email\":");
                n += note::detail::print_json_value(p, email.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"name\":");
                n += note::detail::print_json_value(p, name.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"org\":");
                n += note::detail::print_json_value(p, org.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"role\":");
                n += note::detail::print_json_value(p, role.value());
                n += p.print("}");
                return n;
            }
#endif

        private:
            std::unique_ptr<JsonReader> reader_;
        };
        static constexpr uint8_t field_count = 4;
        static const ::note::FieldDesc* field_descs_ptr() {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
            static constexpr ::note::FieldDesc table[] = {
                {"email", static_cast<uint16_t>(offsetof(Response, email)), ::note::FieldType::String},
                {"name", static_cast<uint16_t>(offsetof(Response, name)), ::note::FieldType::String},
                {"org", static_cast<uint16_t>(offsetof(Response, org)), ::note::FieldType::String},
                {"role", static_cast<uint16_t>(offsetof(Response, role)), ::note::FieldType::String},
            };
#pragma GCC diagnostic pop
            return table;
        }

#if NOTE_SINGLETON
        /// Singleton: type-erased execute — one static fn ptr per type, set by Api.
        static inline ApiResult<Response>(*execute_fn_)(void*, const CardContact::Set&);
        static inline Result<void>(*send_fn_)(void*, BuildFn, void*);
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardContact::Set&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
#endif
        auto execute() const { return execute_fn_(nc_, *this); }
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }

        void build(JsonBuilder& b) const {
            if (email) note::add_flash(b, note::flash(keys_::email), *email);
            if (name) note::add_flash(b, note::flash(keys_::name), *name);
            if (org) note::add_flash(b, note::flash(keys_::org), *org);
            if (role) note::add_flash(b, note::flash(keys_::role), *role);
#if NOTE_EXTRAS
            for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
                std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                           extras_[i_].value);
#endif
        }


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (email) {
                n += p.print(",\"email\":");
                n += note::detail::print_json_value(p, *email);
            }
            if (name) {
                n += p.print(",\"name\":");
                n += note::detail::print_json_value(p, *name);
            }
            if (org) {
                n += p.print(",\"org\":");
                n += note::detail::print_json_value(p, *org);
            }
            if (role) {
                n += p.print(",\"role\":");
                n += note::detail::print_json_value(p, *role);
            }
            n += p.print("}");
            return n;
        }
#endif

    };
};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardContact::Get& CardContact::Get::email_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Get, email));
}
inline CardContact::Get& CardContact::Get::name_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Get, name));
}
inline CardContact::Get& CardContact::Get::org_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Get, org));
}
inline CardContact::Get& CardContact::Get::role_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Get, role));
}
#pragma GCC diagnostic pop


#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardContact::Set& CardContact::Set::email_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Set, email));
}
inline CardContact::Set& CardContact::Set::name_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Set, name));
}
inline CardContact::Set& CardContact::Set::org_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Set, org));
}
inline CardContact::Set& CardContact::Set::role_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardContact::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardContact::Set, role));
}
#pragma GCC diagnostic pop



} // namespace note::api
