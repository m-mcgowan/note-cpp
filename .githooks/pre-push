#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"

# Run the docs verification subset of ci.sh.
# The full CI also compiles examples and runs tests — pre-push
# only checks the quick doc-related verifications.

echo "pre-push: verifying docs..."
"$ROOT/tools/verify-docs.sh"
