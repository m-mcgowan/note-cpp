// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once

#include <note/dyn_field.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/binary_request.hpp>
#include <note/notecard.hpp>
#include <note/print.hpp>
#include <note/safety.hpp>
#include <note/string_pool.hpp>
#include <note/types.hpp>
#include <note/target.hpp>

namespace note::api {








/// Sets up a Notecard WiFi to connect to a WiFi access point.
///
/// @skus{CELL+WIFI,SKYLO,WIFI}
struct CardWifi {
    static constexpr string_view notecard_request = "card.wifi";
    static constexpr bool supports_cmd = true;
    static constexpr Safety safety = Safety::Idempotent;
    static constexpr Skus skus = Skus::from(Product::CellWifi, Product::Skylo, Product::WiFi);

    Notecard* nc_ = nullptr;

    /// By default, the Notecard creates a SoftAP (software enabled access
    /// point) under the name "Notecard". You can use the `name` argument to
    /// change the name of the SoftAP to a custom name.
    ///
    /// If you include a `-` at the end of the `name` (for example `"name":
    /// "acme-"`), the Notecard will append the last four digits of the
    /// network's MAC address (for example `acme-025c`). This allows you to
    /// distinguish between multiple Notecards in SoftAP mode.
    struct name_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        CardWifi& operator()(note::string_view v);
    } name{};
    /// If specified, replaces the Blues logo on the SoftAP page with the
    /// provided name.
    struct org_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        CardWifi& operator()(note::string_view v);
    } org{};
    /// The network password of the WiFi access point. Alternatively, use `-` to
    /// clear an already set password or to connect to an open access point.
    struct password_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        CardWifi& operator()(note::string_view v);
    } password{};
    /// The SSID of the WiFi access point. Alternatively, use `-` to clear an
    /// already set SSID.
    struct ssid_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        CardWifi& operator()(note::string_view v);
    } ssid{};
    /// Specify `true` to activate SoftAP mode on the Notecard programmatically.
    struct start_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardWifi& operator()(bool v);
    } start{};
#if NOTE_API_VERSION >= NOTE_VERSION(7, 5, 1) || !defined(NOTE_API_STRICT)
    /// A string containing an array of access points the Notecard should
    /// attempt to use. The access points should be provided in the following
    /// format:
    ///
    /// `["FIRST-SSID","FIRST-PASSWORD"],["SECOND-SSID","SECOND-PASSWORD"]`.
    ///
    /// You may need to escape any quotes used in this argument before passing
    /// it to the Notecard. For example, the following is a valid request to
    /// pass to a Notecard through the In-Browser Terminal.
    ///
    /// `{"req":"card.wifi", "text":"[\"FIRST-SSID\",\"FIRST-PASSWORD\"]"}`
    ///
    /// @since{7.5.1}
#if NOTE_API_VERSION < NOTE_VERSION(7, 5, 1)
    [[deprecated("requires firmware >= 7.5.1")]]
#endif
    struct text_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        CardWifi& operator()(note::string_view v);
    } text{};
#endif


    // Semantic convenience methods — generated from x-toggle / x-action metadata
    // (method names that match a field accessor are skipped to avoid redefinition)
    auto& activateSoftAP() { start = true; return *this; }
    auto& activateSoftAP(bool v_) { start = v_; return *this; }
    template<typename T>
    auto& extra(note::string_view k_, T v_) {
        if (extras_count_ < NOTE_EXTRAS_MAX)
            extras_[extras_count_++] = {k_, note::DynValue{v_}};
        return *this;
    }
    auto& extra(note::string_view k_, const char* v_) {
        return extra(k_, note::string_view{v_});
    }

    note::DynField operator[](note::string_view k_) {
        if (k_ == "name") return note::dyn_field_for(name);
        if (k_ == "org") return note::dyn_field_for(org);
        if (k_ == "password") return note::dyn_field_for(password);
        if (k_ == "ssid") return note::dyn_field_for(ssid);
        if (k_ == "start") return note::dyn_field_for(start);
#if NOTE_API_VERSION >= NOTE_VERSION(7, 5, 1) || !defined(NOTE_API_STRICT)
        if (k_ == "text") return note::dyn_field_for(text);
#endif
        if (extras_count_ < NOTE_EXTRAS_MAX) {
            auto& slot = extras_[extras_count_++];
            slot.key = k_;
            return note::dyn_field_for(slot.value);
        }
        return {};
    }

    std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
    uint8_t extras_count_ = 0;

    /// Response containing WiFi connection status and configuration information
    /// from the Notecard.
    struct Response {
        /// `true` means that the WiFi access point is using Management Frame
        /// Protection.
        note::ResponseField<bool> secure{};
        /// The security protocol the WiFi access point uses.
        note::ResponseField<note::string_view> security{};
        /// The SSID of the WiFi access point.
        note::ResponseField<note::string_view> ssid{};
        /// The Silicon Labs WF200 WiFi Transceiver binary version.
        note::ResponseField<note::string_view> version{};

        static Response parse(std::unique_ptr<JsonReader> reader_) {
            Response rsp;
            rsp.secure = reader_->get_bool("secure");
            rsp.security = reader_->get_string("security");
            rsp.ssid = reader_->get_string("ssid");
            rsp.version = reader_->get_string("version");
            rsp.reader_ = std::move(reader_);
            return rsp;
        }

        // Non-owning parse: string_views point into the reader's data.
        // The reader (and its underlying JSON buffer) must outlive the Response,
        // or the caller must consume all string fields before the reader is reused.
        static Response parse(const JsonReader& reader_) {
            Response rsp;
            rsp.secure = reader_.get_bool("secure");
            rsp.security = reader_.get_string("security");
            rsp.ssid = reader_.get_string("ssid");
            rsp.version = reader_.get_string("version");
            return rsp;
        }

        // SAX sink — zero-allocation streaming parse into Response fields.
        // String fields are interned into the StringPool immediately, so
        // string_views survive after the parser's scratch buffer is reused.
        struct Sink : ::note::JsonSink {
            Response& rsp;
            ::note::StringPool& pool_;
            Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {}
            void on_string(::note::string_view k_, ::note::string_view v_) override {
                v_ = pool_.intern(v_);
                if (k_ == "security") { rsp.security = v_; return; }
                if (k_ == "ssid") { rsp.ssid = v_; return; }
                if (k_ == "version") { rsp.version = v_; return; }
            }
            void on_bool(::note::string_view k_, bool v_) override {
                if (k_ == "secure") { rsp.secure = v_; return; }
            }
            void reset() override { rsp = Response{}; }
        };

        void intern_strings(::note::StringPool& pool) {
            if (!security.empty()) security = pool.intern(security);
            if (!ssid.empty()) ssid = pool.intern(ssid);
            if (!version.empty()) version = pool.intern(version);
        }

#ifdef ARDUINO
        /// Arduino Printable: prints response fields to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{");
            bool first_ = true;
            if (!first_) n += p.print(",");
            first_ = false;
            n += p.print("\"secure\":");
            n += note::detail::print_json_value(p, secure.value());
            if (!first_) n += p.print(",");
            first_ = false;
            n += p.print("\"security\":");
            n += note::detail::print_json_value(p, security.value());
            if (!first_) n += p.print(",");
            first_ = false;
            n += p.print("\"ssid\":");
            n += note::detail::print_json_value(p, ssid.value());
            if (!first_) n += p.print(",");
            first_ = false;
            n += p.print("\"version\":");
            n += note::detail::print_json_value(p, version.value());
            n += p.print("}");
            return n;
        }
#endif

    private:
        std::unique_ptr<JsonReader> reader_;
    };

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
    void build(JsonBuilder& b) const {
        if (name) b.add("name", *name);
        if (org) b.add("org", *org);
        if (password) b.add("password", *password);
        if (ssid) b.add("ssid", *ssid);
        if (start) b.add("start", *start);
#if NOTE_API_VERSION >= NOTE_VERSION(7, 5, 1) || !defined(NOTE_API_STRICT)
        if (text) b.add("text", *text);
#endif
        for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
            std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                       extras_[i_].value);
    }
#pragma GCC diagnostic pop

    auto execute() const { return nc_->execute(*this); }
    auto execute(Notecard& nc) const { return nc.execute(*this); }
    Result<void> command() const { return nc_->command_typed(*this); }
    Result<void> command(Notecard& nc) const { return nc.command_typed(*this); }

#ifdef ARDUINO
    /// Arduino Printable: prints the JSON request to Serial or any Print stream.
    size_t printTo(Print& p) const {
        size_t n = p.print("{\"req\":\"");
        n += p.print(notecard_request.data());
        n += p.print("\"");
        if (name) {
            n += p.print(",\"name\":");
            n += note::detail::print_json_value(p, *name);
        }
        if (org) {
            n += p.print(",\"org\":");
            n += note::detail::print_json_value(p, *org);
        }
        if (password) {
            n += p.print(",\"password\":");
            n += note::detail::print_json_value(p, *password);
        }
        if (ssid) {
            n += p.print(",\"ssid\":");
            n += note::detail::print_json_value(p, *ssid);
        }
        if (start) {
            n += p.print(",\"start\":");
            n += note::detail::print_json_value(p, *start);
        }
#if NOTE_API_VERSION >= NOTE_VERSION(7, 5, 1) || !defined(NOTE_API_STRICT)
        if (text) {
            n += p.print(",\"text\":");
            n += note::detail::print_json_value(p, *text);
        }
#endif
        n += p.print("}");
        return n;
    }
#endif

};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline CardWifi& CardWifi::name_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardWifi*>(
        reinterpret_cast<char*>(this) - offsetof(CardWifi, name));
}
inline CardWifi& CardWifi::org_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardWifi*>(
        reinterpret_cast<char*>(this) - offsetof(CardWifi, org));
}
inline CardWifi& CardWifi::password_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardWifi*>(
        reinterpret_cast<char*>(this) - offsetof(CardWifi, password));
}
inline CardWifi& CardWifi::ssid_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardWifi*>(
        reinterpret_cast<char*>(this) - offsetof(CardWifi, ssid));
}
inline CardWifi& CardWifi::start_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardWifi*>(
        reinterpret_cast<char*>(this) - offsetof(CardWifi, start));
}
#if NOTE_API_VERSION >= NOTE_VERSION(7, 5, 1) || !defined(NOTE_API_STRICT)
inline CardWifi& CardWifi::text_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardWifi*>(
        reinterpret_cast<char*>(this) - offsetof(CardWifi, text));
}
#endif
#pragma GCC diagnostic pop


} // namespace note::api
