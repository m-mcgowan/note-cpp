// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once

#include <note/field.hpp>
#include <note/json.hpp>
#include <note/notecard.hpp>
#include <note/safety.hpp>
#include <note/types.hpp>

namespace note::api {

struct CardUsageTest {
    static constexpr string_view notecard_request = "card.usage.test";
    static constexpr bool supports_cmd = true;
    static constexpr Safety safety = Safety::ReadOnly;

    Notecard* nc_ = nullptr;

    Field<int32_t> days{};
    Field<int32_t> hours{};
    Field<int32_t> megabytes{};

#if __cpp_explicit_this_parameter >= 202110L
    auto&& set_days(this auto&& self, int32_t v) { self.days = v; return std::forward<decltype(self)>(self); }
#else
    auto& set_days(int32_t v) { days = v; return *this; }
#endif
#if __cpp_explicit_this_parameter >= 202110L
    auto&& set_hours(this auto&& self, int32_t v) { self.hours = v; return std::forward<decltype(self)>(self); }
#else
    auto& set_hours(int32_t v) { hours = v; return *this; }
#endif
#if __cpp_explicit_this_parameter >= 202110L
    auto&& set_megabytes(this auto&& self, int32_t v) { self.megabytes = v; return std::forward<decltype(self)>(self); }
#else
    auto& set_megabytes(int32_t v) { megabytes = v; return *this; }
#endif

    struct Response {
        int32_t bytes_per_day{};
        int32_t bytes_received{};
        int32_t bytes_sent{};
        int32_t days{};
        int32_t max{};
        int32_t notes_received{};
        int32_t notes_sent{};
        int32_t seconds{};
        int32_t sessions_secure{};
        int32_t sessions_standard{};
        int32_t time{};

        static Response parse(std::unique_ptr<JsonReader> reader_) {
            Response rsp;
            rsp.bytes_per_day = reader_->get_int("bytes_per_day");
            rsp.bytes_received = reader_->get_int("bytes_received");
            rsp.bytes_sent = reader_->get_int("bytes_sent");
            rsp.days = reader_->get_int("days");
            rsp.max = reader_->get_int("max");
            rsp.notes_received = reader_->get_int("notes_received");
            rsp.notes_sent = reader_->get_int("notes_sent");
            rsp.seconds = reader_->get_int("seconds");
            rsp.sessions_secure = reader_->get_int("sessions_secure");
            rsp.sessions_standard = reader_->get_int("sessions_standard");
            rsp.time = reader_->get_int("time");
            rsp.reader_ = std::move(reader_);
            return rsp;
        }

    private:
        std::unique_ptr<JsonReader> reader_;
    };

    void build(JsonBuilder& b) const {
        if (days) b.add("days", *days);
        if (hours) b.add("hours", *hours);
        if (megabytes) b.add("megabytes", *megabytes);
    }

    auto execute() const { return nc_->execute(*this); }
    auto execute(Notecard& nc) const { return nc.execute(*this); }
    Result<void> command() const { return nc_->command_typed(*this); }
    Result<void> command(Notecard& nc) const { return nc.command_typed(*this); }

};

} // namespace note::api
