// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once
#include <note/note_config.hpp>

// NOTE_EXTRAS default is set in note_config.hpp.
#if NOTE_EXTRAS
#include <note/dyn_field.hpp>
#endif
#include <note/generic_builder.hpp>
#include <note/generic_sink.hpp>
#include <note/notecard.hpp>
#include <note/arena.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/binary_request.hpp>
#include <note/print.hpp>
#include <note/response_release.hpp>
#include <note/safety.hpp>
#include <note/string_pool.hpp>
#include <note/types.hpp>
#include <note/progmem.hpp>
#include <note/target.hpp>

namespace note::api {









/// View the current state of a Notecard Penalty Box, manually remove the
/// Notecard from a penalty box, or override penalty box defaults.
///
/// @skus{CELL,CELL+WIFI,SKYLO,WIFI}
struct CardWirelessPenalty {

    struct Check {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.wireless.penalty";
            static constexpr char add[] NOTE_FLASH_ATTR = "add";
            static constexpr char max[] NOTE_FLASH_ATTR = "max";
            static constexpr char min[] NOTE_FLASH_ATTR = "min";
            static constexpr char rate[] NOTE_FLASH_ATTR = "rate";
            static constexpr char reset[] NOTE_FLASH_ATTR = "reset";
            static constexpr char set[] NOTE_FLASH_ATTR = "set";
            static constexpr char rsp_status[] NOTE_FLASH_ATTR = "status";
            static constexpr char rsp_count[] NOTE_FLASH_ATTR = "count";
            static constexpr char rsp_minutes[] NOTE_FLASH_ATTR = "minutes";
            static constexpr char rsp_seconds[] NOTE_FLASH_ATTR = "seconds";
        };

        static constexpr string_view notecard_request = "card.wireless.penalty";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::ReadOnly;
        static constexpr RadiosSupport radios = RadiosSupport::from(Radios::Cell, Radios::CellWifi, Radios::Skylo, Radios::WiFi);
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// The number of minutes to add to successive retries. Used with the
        /// `set` argument to override the Network Registration Failure Penalty
        /// Box defaults.
        struct add_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// The number of minutes to add to successive retries. Used with
            /// the `set` argument to override the Network Registration Failure
            /// Penalty Box defaults.
            CardWirelessPenalty::Check& operator()(note::json_int_t v);
        } add{};
        /// The maximum number of minutes that a device can be in a Network
        /// Registration Failure Penalty Box. Used with the `set` argument to
        /// override the Network Registration Failure Penalty Box defaults.
        struct max_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// The maximum number of minutes that a device can be in a Network
            /// Registration Failure Penalty Box. Used with the `set` argument
            /// to override the Network Registration Failure Penalty Box
            /// defaults.
            CardWirelessPenalty::Check& operator()(note::json_int_t v);
        } max{};
        /// The number of minutes of the first retry interval of a Network
        /// Registration Failure Penalty Box. Used with the `set` argument to
        /// override the Network Registration Failure Penalty Box defaults.
        struct min_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// The number of minutes of the first retry interval of a Network
            /// Registration Failure Penalty Box. Used with the `set` argument
            /// to override the Network Registration Failure Penalty Box
            /// defaults.
            CardWirelessPenalty::Check& operator()(note::json_int_t v);
        } min{};
        /// The rate at which the penalty box time multiplier is increased over
        /// successive retries. Used with the `set` argument to override the
        /// Network Registration Failure Penalty Box defaults.
        struct rate_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// The rate at which the penalty box time multiplier is increased
            /// over successive retries. Used with the `set` argument to
            /// override the Network Registration Failure Penalty Box defaults.
            CardWirelessPenalty::Check& operator()(double v);
        } rate{};
        /// Set to `true` to remove the Notecard from certain types of penalty
        /// boxes.
        struct reset_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// Set to `true` to remove the Notecard from certain types of
            /// penalty boxes.
            CardWirelessPenalty::Check& operator()(bool v);
        } reset{};
        /// Set to `true` to override the default settings of the Network
        /// Registration Failure Penalty Box.
        struct set_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// Set to `true` to override the default settings of the Network
            /// Registration Failure Penalty Box.
            CardWirelessPenalty::Check& operator()(bool v);
        } set{};


    // Semantic convenience methods — generated from x-toggle / x-action metadata
    // (method names that match a field accessor are skipped to avoid redefinition)
        auto& clearAll() { reset = true; return *this; }
        auto& clearAll(bool v_) { reset = v_; return *this; }
#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "add") return note::dyn_field_for(add);
            if (k_ == "max") return note::dyn_field_for(max);
            if (k_ == "min") return note::dyn_field_for(min);
            if (k_ == "rate") return note::dyn_field_for(rate);
            if (k_ == "reset") return note::dyn_field_for(reset);
            if (k_ == "set") return note::dyn_field_for(set);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Response containing penalty box status information, including
        /// failure counts, duration, and current penalty conditions from the
        /// Notecard.
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(81) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// If the Notecard is in a Penalty Box, this field provides the
            /// associated Error and Status Codes.
            note::ResponseField<note::string_view> status{};
            /// The number of consecutive network registration failures.
            note::ResponseField<note::json_int_t> count{};
            /// The time since the first network registration failure.
            note::ResponseField<note::json_int_t> minutes{};
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
            /// If the Notecard is in a Penalty Box, the number of seconds until
            /// the penalty condition ends.
            ///
            /// @since{4.1.1}
#if NOTE_API_VERSION < NOTE_VERSION(4, 1, 1)
            [[deprecated("requires firmware >= 4.1.1")]]
#endif
            note::ResponseField<note::json_int_t> seconds{};
#endif

#if !NOTE_NO_RESPONSE_RAII
            /// Allocator that minted this Response's interned string fields,
            /// attached by Notecard execute paths when the Response is parsed.
            /// Empty == no cleanup needed (default-constructed Response, or a
            /// tree-mode parse with no `set_allocator` configured).
            ::note::AllocatorRef alloc_;

            ~Response() {
                if (!alloc_) return;
#if NOTE_RESPONSE_RELEASE_LOOP
                ::note::detail::release_string_fields(*alloc_,
                    &status,
                    1);
#else
                ::note::detail::deallocate_if_present(*alloc_, status.value());
#endif
            }

            // Move-only: copying a Response would alias the interned-string
            // ownership and double-free on the second destruction. AllocatorRef
            // nulls the source's pointer so only the live owner runs cleanup.
            Response() = default;
            Response(Response&&) noexcept = default;
            Response& operator=(Response&& o) noexcept {
                if (this != &o) {
                    this->~Response();
                    ::new (this) Response(::std::move(o));
                }
                return *this;
            }
            Response(const Response&) = delete;
            Response& operator=(const Response&) = delete;
#endif // !NOTE_NO_RESPONSE_RAII

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("status")) rsp.status = reader_->get_string("status");
                if (reader_->has("count")) rsp.count = reader_->get_int("count");
                if (reader_->has("minutes")) rsp.minutes = reader_->get_int("minutes");
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                if (reader_->has("seconds")) rsp.seconds = reader_->get_int("seconds");
#endif
                rsp.reader_ = std::move(reader_);
                return rsp;
            }
#pragma GCC diagnostic pop

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("status")) rsp.status = reader_.get_string("status");
                if (reader_.has("count")) rsp.count = reader_.get_int("count");
                if (reader_.has("minutes")) rsp.minutes = reader_.get_int("minutes");
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                if (reader_.has("seconds")) rsp.seconds = reader_.get_int("seconds");
#endif
                return rsp;
            }
#pragma GCC diagnostic pop
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_status) == k_) { rsp.status = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_number(::note::string_view k_, ::note::string_view raw_) {
                    if (note::flash(keys_::rsp_count) == k_) { rsp.count = ::note::parse_int(raw_); return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = ::note::parse_int(raw_); return; }
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = ::note::parse_int(raw_); return; }
#endif
                }
                NOTE_SINK_NOINLINE void on_int(::note::string_view k_, ::note::json_int_t v_) {
                    if (note::flash(keys_::rsp_count) == k_) { rsp.count = v_; return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = v_; return; }
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = v_; return; }
#endif
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };
#pragma GCC diagnostic pop

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            void intern_strings(::note::StringPool& pool) {
                if (!status.empty()) status = pool.intern(status);
            }
#pragma GCC diagnostic pop

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"status\":");
                n += note::detail::print_json_value(p, status.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"count\":");
                n += note::detail::print_json_value(p, count.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"minutes\":");
                n += note::detail::print_json_value(p, minutes.value());
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"seconds\":");
                n += note::detail::print_json_value(p, seconds.value());
#endif
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardWirelessPenalty::Check&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (add) {
                n += p.print(",\"add\":");
                n += note::detail::print_json_value(p, *add);
            }
            if (max) {
                n += p.print(",\"max\":");
                n += note::detail::print_json_value(p, *max);
            }
            if (min) {
                n += p.print(",\"min\":");
                n += note::detail::print_json_value(p, *min);
            }
            if (rate) {
                n += p.print(",\"rate\":");
                n += note::detail::print_json_value(p, *rate);
            }
            if (reset) {
                n += p.print(",\"reset\":");
                n += note::detail::print_json_value(p, *reset);
            }
            if (set) {
                n += p.print(",\"set\":");
                n += note::detail::print_json_value(p, *set);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        friend struct ::note::test::JsonbWireProbe;
        public:

    };
    using Get = Check;  // legacy alias

    /// View the current state of a Notecard Penalty Box, manually remove the
    /// Notecard from a penalty box, or override penalty box defaults.
    ///
    /// @skus{CELL,CELL+WIFI,SKYLO,WIFI}
    struct Set {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.wireless.penalty";
            static constexpr char add[] NOTE_FLASH_ATTR = "add";
            static constexpr char max[] NOTE_FLASH_ATTR = "max";
            static constexpr char min[] NOTE_FLASH_ATTR = "min";
            static constexpr char rate[] NOTE_FLASH_ATTR = "rate";
            static constexpr char reset[] NOTE_FLASH_ATTR = "reset";
            static constexpr char set[] NOTE_FLASH_ATTR = "set";
            static constexpr char rsp_status[] NOTE_FLASH_ATTR = "status";
            static constexpr char rsp_count[] NOTE_FLASH_ATTR = "count";
            static constexpr char rsp_minutes[] NOTE_FLASH_ATTR = "minutes";
            static constexpr char rsp_seconds[] NOTE_FLASH_ATTR = "seconds";
        };

        static constexpr string_view notecard_request = "card.wireless.penalty";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios = RadiosSupport::from(Radios::Cell, Radios::CellWifi, Radios::Skylo, Radios::WiFi);
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// The number of minutes to add to successive retries. Used with the
        /// `set` argument to override the Network Registration Failure Penalty
        /// Box defaults.
        struct add_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// The number of minutes to add to successive retries. Used with
            /// the `set` argument to override the Network Registration Failure
            /// Penalty Box defaults.
            CardWirelessPenalty::Set& operator()(note::json_int_t v);
        } add{};
        /// The maximum number of minutes that a device can be in a Network
        /// Registration Failure Penalty Box. Used with the `set` argument to
        /// override the Network Registration Failure Penalty Box defaults.
        struct max_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// The maximum number of minutes that a device can be in a Network
            /// Registration Failure Penalty Box. Used with the `set` argument
            /// to override the Network Registration Failure Penalty Box
            /// defaults.
            CardWirelessPenalty::Set& operator()(note::json_int_t v);
        } max{};
        /// The number of minutes of the first retry interval of a Network
        /// Registration Failure Penalty Box. Used with the `set` argument to
        /// override the Network Registration Failure Penalty Box defaults.
        struct min_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// The number of minutes of the first retry interval of a Network
            /// Registration Failure Penalty Box. Used with the `set` argument
            /// to override the Network Registration Failure Penalty Box
            /// defaults.
            CardWirelessPenalty::Set& operator()(note::json_int_t v);
        } min{};
        /// The rate at which the penalty box time multiplier is increased over
        /// successive retries. Used with the `set` argument to override the
        /// Network Registration Failure Penalty Box defaults.
        struct rate_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// The rate at which the penalty box time multiplier is increased
            /// over successive retries. Used with the `set` argument to
            /// override the Network Registration Failure Penalty Box defaults.
            CardWirelessPenalty::Set& operator()(double v);
        } rate{};
        /// Set to `true` to remove the Notecard from certain types of penalty
        /// boxes.
        struct reset_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// Set to `true` to remove the Notecard from certain types of
            /// penalty boxes.
            CardWirelessPenalty::Set& operator()(bool v);
        } reset{};


    // Semantic convenience methods — generated from x-toggle / x-action metadata
    // (method names that match a field accessor are skipped to avoid redefinition)
        auto& clearAll() { reset = true; return *this; }
        auto& clearAll(bool v_) { reset = v_; return *this; }
#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "add") return note::dyn_field_for(add);
            if (k_ == "max") return note::dyn_field_for(max);
            if (k_ == "min") return note::dyn_field_for(min);
            if (k_ == "rate") return note::dyn_field_for(rate);
            if (k_ == "reset") return note::dyn_field_for(reset);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Response containing penalty box status information, including
        /// failure counts, duration, and current penalty conditions from the
        /// Notecard.
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(81) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// If the Notecard is in a Penalty Box, this field provides the
            /// associated Error and Status Codes.
            note::ResponseField<note::string_view> status{};
            /// The number of consecutive network registration failures.
            note::ResponseField<note::json_int_t> count{};
            /// The time since the first network registration failure.
            note::ResponseField<note::json_int_t> minutes{};
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
            /// If the Notecard is in a Penalty Box, the number of seconds until
            /// the penalty condition ends.
            ///
            /// @since{4.1.1}
#if NOTE_API_VERSION < NOTE_VERSION(4, 1, 1)
            [[deprecated("requires firmware >= 4.1.1")]]
#endif
            note::ResponseField<note::json_int_t> seconds{};
#endif

#if !NOTE_NO_RESPONSE_RAII
            /// Allocator that minted this Response's interned string fields,
            /// attached by Notecard execute paths when the Response is parsed.
            /// Empty == no cleanup needed (default-constructed Response, or a
            /// tree-mode parse with no `set_allocator` configured).
            ::note::AllocatorRef alloc_;

            ~Response() {
                if (!alloc_) return;
#if NOTE_RESPONSE_RELEASE_LOOP
                ::note::detail::release_string_fields(*alloc_,
                    &status,
                    1);
#else
                ::note::detail::deallocate_if_present(*alloc_, status.value());
#endif
            }

            // Move-only: copying a Response would alias the interned-string
            // ownership and double-free on the second destruction. AllocatorRef
            // nulls the source's pointer so only the live owner runs cleanup.
            Response() = default;
            Response(Response&&) noexcept = default;
            Response& operator=(Response&& o) noexcept {
                if (this != &o) {
                    this->~Response();
                    ::new (this) Response(::std::move(o));
                }
                return *this;
            }
            Response(const Response&) = delete;
            Response& operator=(const Response&) = delete;
#endif // !NOTE_NO_RESPONSE_RAII

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("status")) rsp.status = reader_->get_string("status");
                if (reader_->has("count")) rsp.count = reader_->get_int("count");
                if (reader_->has("minutes")) rsp.minutes = reader_->get_int("minutes");
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                if (reader_->has("seconds")) rsp.seconds = reader_->get_int("seconds");
#endif
                rsp.reader_ = std::move(reader_);
                return rsp;
            }
#pragma GCC diagnostic pop

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("status")) rsp.status = reader_.get_string("status");
                if (reader_.has("count")) rsp.count = reader_.get_int("count");
                if (reader_.has("minutes")) rsp.minutes = reader_.get_int("minutes");
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                if (reader_.has("seconds")) rsp.seconds = reader_.get_int("seconds");
#endif
                return rsp;
            }
#pragma GCC diagnostic pop
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_status) == k_) { rsp.status = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_number(::note::string_view k_, ::note::string_view raw_) {
                    if (note::flash(keys_::rsp_count) == k_) { rsp.count = ::note::parse_int(raw_); return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = ::note::parse_int(raw_); return; }
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = ::note::parse_int(raw_); return; }
#endif
                }
                NOTE_SINK_NOINLINE void on_int(::note::string_view k_, ::note::json_int_t v_) {
                    if (note::flash(keys_::rsp_count) == k_) { rsp.count = v_; return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = v_; return; }
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = v_; return; }
#endif
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };
#pragma GCC diagnostic pop

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            void intern_strings(::note::StringPool& pool) {
                if (!status.empty()) status = pool.intern(status);
            }
#pragma GCC diagnostic pop

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"status\":");
                n += note::detail::print_json_value(p, status.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"count\":");
                n += note::detail::print_json_value(p, count.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"minutes\":");
                n += note::detail::print_json_value(p, minutes.value());
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"seconds\":");
                n += note::detail::print_json_value(p, seconds.value());
#endif
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardWirelessPenalty::Set&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (add) {
                n += p.print(",\"add\":");
                n += note::detail::print_json_value(p, *add);
            }
            if (max) {
                n += p.print(",\"max\":");
                n += note::detail::print_json_value(p, *max);
            }
            if (min) {
                n += p.print(",\"min\":");
                n += note::detail::print_json_value(p, *min);
            }
            if (rate) {
                n += p.print(",\"rate\":");
                n += note::detail::print_json_value(p, *rate);
            }
            if (reset) {
                n += p.print(",\"reset\":");
                n += note::detail::print_json_value(p, *reset);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        friend struct ::note::test::JsonbWireProbe;
        public:

    };

    /// View the current state of a Notecard Penalty Box, manually remove the
    /// Notecard from a penalty box, or override penalty box defaults.
    ///
    /// @skus{CELL,CELL+WIFI,SKYLO,WIFI}
    struct Clear {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.wireless.penalty";
            static constexpr char add[] NOTE_FLASH_ATTR = "add";
            static constexpr char max[] NOTE_FLASH_ATTR = "max";
            static constexpr char min[] NOTE_FLASH_ATTR = "min";
            static constexpr char rate[] NOTE_FLASH_ATTR = "rate";
            static constexpr char reset[] NOTE_FLASH_ATTR = "reset";
            static constexpr char set[] NOTE_FLASH_ATTR = "set";
            static constexpr char rsp_status[] NOTE_FLASH_ATTR = "status";
            static constexpr char rsp_count[] NOTE_FLASH_ATTR = "count";
            static constexpr char rsp_minutes[] NOTE_FLASH_ATTR = "minutes";
            static constexpr char rsp_seconds[] NOTE_FLASH_ATTR = "seconds";
        };

        static constexpr string_view notecard_request = "card.wireless.penalty";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Destructive;
        static constexpr RadiosSupport radios = RadiosSupport::from(Radios::Cell, Radios::CellWifi, Radios::Skylo, Radios::WiFi);
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// The number of minutes to add to successive retries. Used with the
        /// `set` argument to override the Network Registration Failure Penalty
        /// Box defaults.
        struct add_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// The number of minutes to add to successive retries. Used with
            /// the `set` argument to override the Network Registration Failure
            /// Penalty Box defaults.
            CardWirelessPenalty::Clear& operator()(note::json_int_t v);
        } add{};
        /// The maximum number of minutes that a device can be in a Network
        /// Registration Failure Penalty Box. Used with the `set` argument to
        /// override the Network Registration Failure Penalty Box defaults.
        struct max_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// The maximum number of minutes that a device can be in a Network
            /// Registration Failure Penalty Box. Used with the `set` argument
            /// to override the Network Registration Failure Penalty Box
            /// defaults.
            CardWirelessPenalty::Clear& operator()(note::json_int_t v);
        } max{};
        /// The number of minutes of the first retry interval of a Network
        /// Registration Failure Penalty Box. Used with the `set` argument to
        /// override the Network Registration Failure Penalty Box defaults.
        struct min_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// The number of minutes of the first retry interval of a Network
            /// Registration Failure Penalty Box. Used with the `set` argument
            /// to override the Network Registration Failure Penalty Box
            /// defaults.
            CardWirelessPenalty::Clear& operator()(note::json_int_t v);
        } min{};
        /// The rate at which the penalty box time multiplier is increased over
        /// successive retries. Used with the `set` argument to override the
        /// Network Registration Failure Penalty Box defaults.
        struct rate_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// The rate at which the penalty box time multiplier is increased
            /// over successive retries. Used with the `set` argument to
            /// override the Network Registration Failure Penalty Box defaults.
            CardWirelessPenalty::Clear& operator()(double v);
        } rate{};
        /// Set to `true` to override the default settings of the Network
        /// Registration Failure Penalty Box.
        struct set_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// Set to `true` to override the default settings of the Network
            /// Registration Failure Penalty Box.
            CardWirelessPenalty::Clear& operator()(bool v);
        } set{};


#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "add") return note::dyn_field_for(add);
            if (k_ == "max") return note::dyn_field_for(max);
            if (k_ == "min") return note::dyn_field_for(min);
            if (k_ == "rate") return note::dyn_field_for(rate);
            if (k_ == "set") return note::dyn_field_for(set);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Response containing penalty box status information, including
        /// failure counts, duration, and current penalty conditions from the
        /// Notecard.
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(81) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// If the Notecard is in a Penalty Box, this field provides the
            /// associated Error and Status Codes.
            note::ResponseField<note::string_view> status{};
            /// The number of consecutive network registration failures.
            note::ResponseField<note::json_int_t> count{};
            /// The time since the first network registration failure.
            note::ResponseField<note::json_int_t> minutes{};
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
            /// If the Notecard is in a Penalty Box, the number of seconds until
            /// the penalty condition ends.
            ///
            /// @since{4.1.1}
#if NOTE_API_VERSION < NOTE_VERSION(4, 1, 1)
            [[deprecated("requires firmware >= 4.1.1")]]
#endif
            note::ResponseField<note::json_int_t> seconds{};
#endif

#if !NOTE_NO_RESPONSE_RAII
            /// Allocator that minted this Response's interned string fields,
            /// attached by Notecard execute paths when the Response is parsed.
            /// Empty == no cleanup needed (default-constructed Response, or a
            /// tree-mode parse with no `set_allocator` configured).
            ::note::AllocatorRef alloc_;

            ~Response() {
                if (!alloc_) return;
#if NOTE_RESPONSE_RELEASE_LOOP
                ::note::detail::release_string_fields(*alloc_,
                    &status,
                    1);
#else
                ::note::detail::deallocate_if_present(*alloc_, status.value());
#endif
            }

            // Move-only: copying a Response would alias the interned-string
            // ownership and double-free on the second destruction. AllocatorRef
            // nulls the source's pointer so only the live owner runs cleanup.
            Response() = default;
            Response(Response&&) noexcept = default;
            Response& operator=(Response&& o) noexcept {
                if (this != &o) {
                    this->~Response();
                    ::new (this) Response(::std::move(o));
                }
                return *this;
            }
            Response(const Response&) = delete;
            Response& operator=(const Response&) = delete;
#endif // !NOTE_NO_RESPONSE_RAII

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("status")) rsp.status = reader_->get_string("status");
                if (reader_->has("count")) rsp.count = reader_->get_int("count");
                if (reader_->has("minutes")) rsp.minutes = reader_->get_int("minutes");
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                if (reader_->has("seconds")) rsp.seconds = reader_->get_int("seconds");
#endif
                rsp.reader_ = std::move(reader_);
                return rsp;
            }
#pragma GCC diagnostic pop

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("status")) rsp.status = reader_.get_string("status");
                if (reader_.has("count")) rsp.count = reader_.get_int("count");
                if (reader_.has("minutes")) rsp.minutes = reader_.get_int("minutes");
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                if (reader_.has("seconds")) rsp.seconds = reader_.get_int("seconds");
#endif
                return rsp;
            }
#pragma GCC diagnostic pop
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_status) == k_) { rsp.status = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_number(::note::string_view k_, ::note::string_view raw_) {
                    if (note::flash(keys_::rsp_count) == k_) { rsp.count = ::note::parse_int(raw_); return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = ::note::parse_int(raw_); return; }
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = ::note::parse_int(raw_); return; }
#endif
                }
                NOTE_SINK_NOINLINE void on_int(::note::string_view k_, ::note::json_int_t v_) {
                    if (note::flash(keys_::rsp_count) == k_) { rsp.count = v_; return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = v_; return; }
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = v_; return; }
#endif
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };
#pragma GCC diagnostic pop

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            void intern_strings(::note::StringPool& pool) {
                if (!status.empty()) status = pool.intern(status);
            }
#pragma GCC diagnostic pop

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"status\":");
                n += note::detail::print_json_value(p, status.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"count\":");
                n += note::detail::print_json_value(p, count.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"minutes\":");
                n += note::detail::print_json_value(p, minutes.value());
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"seconds\":");
                n += note::detail::print_json_value(p, seconds.value());
#endif
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardWirelessPenalty::Clear&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (add) {
                n += p.print(",\"add\":");
                n += note::detail::print_json_value(p, *add);
            }
            if (max) {
                n += p.print(",\"max\":");
                n += note::detail::print_json_value(p, *max);
            }
            if (min) {
                n += p.print(",\"min\":");
                n += note::detail::print_json_value(p, *min);
            }
            if (rate) {
                n += p.print(",\"rate\":");
                n += note::detail::print_json_value(p, *rate);
            }
            if (set) {
                n += p.print(",\"set\":");
                n += note::detail::print_json_value(p, *set);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        friend struct ::note::test::JsonbWireProbe;
        public:

    };
    using Delete = Clear;  // legacy alias
};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardWirelessPenalty::Check& CardWirelessPenalty::Check::add_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Check*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Check, add));
}
inline CardWirelessPenalty::Check& CardWirelessPenalty::Check::max_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Check*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Check, max));
}
inline CardWirelessPenalty::Check& CardWirelessPenalty::Check::min_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Check*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Check, min));
}
inline CardWirelessPenalty::Check& CardWirelessPenalty::Check::rate_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Check*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Check, rate));
}
inline CardWirelessPenalty::Check& CardWirelessPenalty::Check::reset_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Check*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Check, reset));
}
inline CardWirelessPenalty::Check& CardWirelessPenalty::Check::set_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Check*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Check, set));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardWirelessPenalty::Check> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardWirelessPenalty::Check::keys_::rsp_status, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Check::Response, status)), ::note::FieldType::String},
        {::note::api::CardWirelessPenalty::Check::keys_::rsp_count, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Check::Response, count)), ::note::FieldType::Int},
        {::note::api::CardWirelessPenalty::Check::keys_::rsp_minutes, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Check::Response, minutes)), ::note::FieldType::Int},
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
        {::note::api::CardWirelessPenalty::Check::keys_::rsp_seconds, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Check::Response, seconds)), ::note::FieldType::Int},
#endif
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardWirelessPenalty::Check::keys_::add, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Check, add)), ::note::ReqFieldType::Int},
            {::note::api::CardWirelessPenalty::Check::keys_::max, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Check, max)), ::note::ReqFieldType::Int},
            {::note::api::CardWirelessPenalty::Check::keys_::min, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Check, min)), ::note::ReqFieldType::Int},
            {::note::api::CardWirelessPenalty::Check::keys_::rate, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Check, rate)), ::note::ReqFieldType::Double},
            {::note::api::CardWirelessPenalty::Check::keys_::reset, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Check, reset)), ::note::ReqFieldType::Bool},
            {::note::api::CardWirelessPenalty::Check::keys_::set, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Check, set)), ::note::ReqFieldType::Bool},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardWirelessPenalty::Check::build(JsonBuilder& b) const {
    using meta_ = ::note::detail::request_traits<CardWirelessPenalty::Check>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<typename CardWirelessPenalty::Check::Response> CardWirelessPenalty::Check::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardWirelessPenalty::Check>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
#if !NOTE_NO_RESPONSE_RAII
    // The type-erased thunk couldn't reach `attach_allocator`. Mark the
    // Response as owning its interned strings so its dtor frees them on
    // the SINGLETON path (parity with the Notecard::execute template path,
    // which attaches via `detail::attach_allocator`). The dtor still gates
    // the actual free on `g_singleton_allocator_present`, so marking when
    // no allocator is configured stays safe.
    rsp_.alloc_.reset(::note::detail::g_singleton_allocator);
#endif
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardWirelessPenalty::Check::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardWirelessPenalty::Check>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardWirelessPenalty::Set& CardWirelessPenalty::Set::add_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Set, add));
}
inline CardWirelessPenalty::Set& CardWirelessPenalty::Set::max_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Set, max));
}
inline CardWirelessPenalty::Set& CardWirelessPenalty::Set::min_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Set, min));
}
inline CardWirelessPenalty::Set& CardWirelessPenalty::Set::rate_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Set, rate));
}
inline CardWirelessPenalty::Set& CardWirelessPenalty::Set::reset_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Set, reset));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardWirelessPenalty::Set> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardWirelessPenalty::Set::keys_::rsp_status, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Set::Response, status)), ::note::FieldType::String},
        {::note::api::CardWirelessPenalty::Set::keys_::rsp_count, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Set::Response, count)), ::note::FieldType::Int},
        {::note::api::CardWirelessPenalty::Set::keys_::rsp_minutes, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Set::Response, minutes)), ::note::FieldType::Int},
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
        {::note::api::CardWirelessPenalty::Set::keys_::rsp_seconds, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Set::Response, seconds)), ::note::FieldType::Int},
#endif
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardWirelessPenalty::Set::keys_::add, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Set, add)), ::note::ReqFieldType::Int},
            {::note::api::CardWirelessPenalty::Set::keys_::max, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Set, max)), ::note::ReqFieldType::Int},
            {::note::api::CardWirelessPenalty::Set::keys_::min, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Set, min)), ::note::ReqFieldType::Int},
            {::note::api::CardWirelessPenalty::Set::keys_::rate, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Set, rate)), ::note::ReqFieldType::Double},
            {::note::api::CardWirelessPenalty::Set::keys_::reset, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Set, reset)), ::note::ReqFieldType::Bool},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardWirelessPenalty::Set::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::set), true);
    using meta_ = ::note::detail::request_traits<CardWirelessPenalty::Set>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<typename CardWirelessPenalty::Set::Response> CardWirelessPenalty::Set::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardWirelessPenalty::Set>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
#if !NOTE_NO_RESPONSE_RAII
    // The type-erased thunk couldn't reach `attach_allocator`. Mark the
    // Response as owning its interned strings so its dtor frees them on
    // the SINGLETON path (parity with the Notecard::execute template path,
    // which attaches via `detail::attach_allocator`). The dtor still gates
    // the actual free on `g_singleton_allocator_present`, so marking when
    // no allocator is configured stays safe.
    rsp_.alloc_.reset(::note::detail::g_singleton_allocator);
#endif
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardWirelessPenalty::Set::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardWirelessPenalty::Set>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardWirelessPenalty::Clear& CardWirelessPenalty::Clear::add_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Clear*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Clear, add));
}
inline CardWirelessPenalty::Clear& CardWirelessPenalty::Clear::max_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Clear*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Clear, max));
}
inline CardWirelessPenalty::Clear& CardWirelessPenalty::Clear::min_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Clear*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Clear, min));
}
inline CardWirelessPenalty::Clear& CardWirelessPenalty::Clear::rate_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Clear*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Clear, rate));
}
inline CardWirelessPenalty::Clear& CardWirelessPenalty::Clear::set_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardWirelessPenalty::Clear*>(
        reinterpret_cast<char*>(this) - offsetof(CardWirelessPenalty::Clear, set));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardWirelessPenalty::Clear> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardWirelessPenalty::Clear::keys_::rsp_status, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Clear::Response, status)), ::note::FieldType::String},
        {::note::api::CardWirelessPenalty::Clear::keys_::rsp_count, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Clear::Response, count)), ::note::FieldType::Int},
        {::note::api::CardWirelessPenalty::Clear::keys_::rsp_minutes, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Clear::Response, minutes)), ::note::FieldType::Int},
#if NOTE_API_VERSION >= NOTE_VERSION(4, 1, 1) || !defined(NOTE_API_STRICT)
        {::note::api::CardWirelessPenalty::Clear::keys_::rsp_seconds, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Clear::Response, seconds)), ::note::FieldType::Int},
#endif
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardWirelessPenalty::Clear::keys_::add, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Clear, add)), ::note::ReqFieldType::Int},
            {::note::api::CardWirelessPenalty::Clear::keys_::max, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Clear, max)), ::note::ReqFieldType::Int},
            {::note::api::CardWirelessPenalty::Clear::keys_::min, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Clear, min)), ::note::ReqFieldType::Int},
            {::note::api::CardWirelessPenalty::Clear::keys_::rate, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Clear, rate)), ::note::ReqFieldType::Double},
            {::note::api::CardWirelessPenalty::Clear::keys_::set, static_cast<uint16_t>(offsetof(::note::api::CardWirelessPenalty::Clear, set)), ::note::ReqFieldType::Bool},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardWirelessPenalty::Clear::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::reset), true);
    using meta_ = ::note::detail::request_traits<CardWirelessPenalty::Clear>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<typename CardWirelessPenalty::Clear::Response> CardWirelessPenalty::Clear::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardWirelessPenalty::Clear>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
#if !NOTE_NO_RESPONSE_RAII
    // The type-erased thunk couldn't reach `attach_allocator`. Mark the
    // Response as owning its interned strings so its dtor frees them on
    // the SINGLETON path (parity with the Notecard::execute template path,
    // which attaches via `detail::attach_allocator`). The dtor still gates
    // the actual free on `g_singleton_allocator_present`, so marking when
    // no allocator is configured stays safe.
    rsp_.alloc_.reset(::note::detail::g_singleton_allocator);
#endif
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardWirelessPenalty::Clear::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardWirelessPenalty::Clear>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif


} // namespace note::api
