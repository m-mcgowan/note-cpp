// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once
#include <note/note_config.hpp>

// NOTE_EXTRAS default is set in note_config.hpp.
#if NOTE_EXTRAS
#include <note/dyn_field.hpp>
#endif
#include <note/generic_builder.hpp>
#include <note/generic_sink.hpp>
#include <note/notecard.hpp>
#include <note/arena.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/json_sax.hpp>
#include <note/binary_request.hpp>
#include <note/print.hpp>
#include <note/response_release.hpp>
#include <note/safety.hpp>
#include <note/string_pool.hpp>
#include <note/types.hpp>
#include <note/progmem.hpp>
#include <note/target.hpp>

namespace note::api {









/// Sets location-related configuration settings. Retrieves the current location
/// mode when passed with no argument.
///
/// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
struct CardLocationMode {

    struct Get {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.location.mode";
            static constexpr char delete_[] NOTE_FLASH_ATTR = "delete";
            static constexpr char lat[] NOTE_FLASH_ATTR = "lat";
            static constexpr char lon[] NOTE_FLASH_ATTR = "lon";
            static constexpr char max[] NOTE_FLASH_ATTR = "max";
            static constexpr char minutes[] NOTE_FLASH_ATTR = "minutes";
            static constexpr char mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char threshold[] NOTE_FLASH_ATTR = "threshold";
            static constexpr char vseconds[] NOTE_FLASH_ATTR = "vseconds";
            static constexpr char rsp_mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char rsp_vseconds[] NOTE_FLASH_ATTR = "vseconds";
            static constexpr char rsp_lat[] NOTE_FLASH_ATTR = "lat";
            static constexpr char rsp_lon[] NOTE_FLASH_ATTR = "lon";
            static constexpr char rsp_max[] NOTE_FLASH_ATTR = "max";
            static constexpr char rsp_minutes[] NOTE_FLASH_ATTR = "minutes";
            static constexpr char rsp_seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char rsp_threshold[] NOTE_FLASH_ATTR = "threshold";
        };

        static constexpr string_view notecard_request = "card.location.mode";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::ReadOnly;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// Set to `true` to delete the last known location stored in the
        /// Notecard.
        struct delete_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// Set to `true` to delete the last known location stored in the
            /// Notecard.
            CardLocationMode::Get& operator()(bool v);
        } delete_{};
        /// When in periodic or continuous mode, providing this value enables
        /// geofencing. The value you provide for this argument should be the
        /// latitude of the center of the geofence, in degrees. When in fixed
        /// mode, the value you provide for this argument should be the latitude
        /// location of the device itself, in degrees.
        struct lat_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// When in periodic or continuous mode, providing this value
            /// enables geofencing. The value you provide for this argument
            /// should be the latitude of the center of the geofence, in
            /// degrees. When in fixed mode, the value you provide for this
            /// argument should be the latitude location of the device itself,
            /// in degrees.
            CardLocationMode::Get& operator()(double v);
        } lat{};
        /// When in periodic or continuous mode, providing this value enables
        /// geofencing. The value you provide for this argument should be the
        /// longitude of the center of the geofence, in degrees. When in fixed
        /// mode, the value you provide for this argument should be the
        /// longitude location of the device itself, in degrees.
        struct lon_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// When in periodic or continuous mode, providing this value
            /// enables geofencing. The value you provide for this argument
            /// should be the longitude of the center of the geofence, in
            /// degrees. When in fixed mode, the value you provide for this
            /// argument should be the longitude location of the device itself,
            /// in degrees.
            CardLocationMode::Get& operator()(double v);
        } lon{};
        /// Meters from a geofence center. Used to enable geofence location
        /// tracking.
        struct max_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// Meters from a geofence center. Used to enable geofence location
            /// tracking.
            CardLocationMode::Get& operator()(note::json_int_t v);
        } max{};
        /// When geofence is enabled, the number of minutes the device should be
        /// outside the geofence before the Notecard location is tracked.
        struct minutes_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When geofence is enabled, the number of minutes the device
            /// should be outside the geofence before the Notecard location is
            /// tracked.
            CardLocationMode::Get& operator()(note::json_int_t v);
        } minutes{};
        /// Sets the location mode.
        // mode:  | off | periodic | continuous | fixed | -
        struct mode_t : Field<note::string_view> {
#if __cplusplus >= 202002L && !defined(__clang__)
            constexpr mode_t() = default;
            template<std::size_t N>
            consteval mode_t(const char (&s)[N])
                : Field<note::string_view>(note::string_view(s, N - 1)) {
                note::string_view sv(s, N - 1);
                if (sv != "" && sv != "off" && sv != "periodic" && sv != "continuous" && sv != "fixed" && sv != "-")
                    throw "card.location.mode: invalid value for 'mode'";
            }
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, mode_t>)
            constexpr mode_t(U&& v) : Field<note::string_view>(note::string_view(std::forward<U>(v))) {}
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, mode_t>)
            mode_t& operator=(U&& v) {
                Field<note::string_view>::operator=(note::string_view(std::forward<U>(v)));
                return *this;
            }
            mode_t& operator=(std::nullopt_t) { Field<note::string_view>::reset(); return *this; }
            mode_t(const mode_t&) = default;
            mode_t& operator=(const mode_t&) = default;
            mode_t(mode_t&&) = default;
            mode_t& operator=(mode_t&&) = default;
#else
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
#endif
            static constexpr note::string_view off{"off"};
            static constexpr note::string_view periodic{"periodic"};
            static constexpr note::string_view continuous{"continuous"};
            static constexpr note::string_view fixed{"fixed"};
            static constexpr note::string_view _{"-"};
            /// Sets the location mode.
            CardLocationMode::Get& operator()(note::string_view v);
        } mode{};
        /// When in `periodic` mode, location will be sampled at this interval,
        /// if the Notecard detects motion. If seconds is < 300, during periods
        /// of sustained movement the Notecard will leave its onboard GPS/GNSS
        /// on continuously to avoid powering the module on and off repeatedly.
        struct seconds_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When in `periodic` mode, location will be sampled at this
            /// interval, if the Notecard detects motion. If seconds is < 300,
            /// during periods of sustained movement the Notecard will leave its
            /// onboard GPS/GNSS on continuously to avoid powering the module on
            /// and off repeatedly.
            CardLocationMode::Get& operator()(note::json_int_t v);
        } seconds{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        /// When in `periodic` mode, the number of motion events (registered by
        /// the built-in accelerometer) required to trigger GPS to turn on.
        ///
        /// @since{3.4.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
        [[deprecated("requires firmware >= 3.4.1")]]
#endif
        struct threshold_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When in `periodic` mode, the number of motion events (registered
            /// by the built-in accelerometer) required to trigger GPS to turn
            /// on.
            ///
            /// @since{3.4.1}
            CardLocationMode::Get& operator()(note::json_int_t v);
        } threshold{};
#endif
        /// In `periodic` mode, overrides `seconds` with a voltage-variable
        /// value.
        struct vseconds_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// In `periodic` mode, overrides `seconds` with a voltage-variable
            /// value.
            CardLocationMode::Get& operator()(note::string_view v);
        } vseconds{};

        // Valid values for 'mode':
        //   "off" — Turns location mode off. Approximate location may still be [ascertain...
        //   "periodic" — Samples location at a specified interval, if the device has moved.
        //   "continuous" — Enables the Notecard's onboard GPS/GNSS module for continuous samplin...
        //   "fixed" — Reports the location as a fixed location using the specified `lat` an...
        //   "-" — Resets the mode to the default value (`off`).
        // consteval: only callable at compile time (C++20)
#if __cplusplus >= 202002L
        static consteval note::string_view validatedMode(const char* v) {
            note::string_view sv{v};
            if (sv != "" && sv != "off" && sv != "periodic" && sv != "continuous" && sv != "fixed" && sv != "-")
                throw "card.location.mode: invalid value for 'mode'";
            return sv;
        }
#endif

#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "delete") return note::dyn_field_for(delete_);
            if (k_ == "lat") return note::dyn_field_for(lat);
            if (k_ == "lon") return note::dyn_field_for(lon);
            if (k_ == "max") return note::dyn_field_for(max);
            if (k_ == "minutes") return note::dyn_field_for(minutes);
            if (k_ == "mode") return note::dyn_field_for(mode);
            if (k_ == "seconds") return note::dyn_field_for(seconds);
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            if (k_ == "threshold") return note::dyn_field_for(threshold);
#endif
            if (k_ == "vseconds") return note::dyn_field_for(vseconds);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Successful response
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(33) +
                ::note::detail::arena_cost(49) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// The current location mode.
            note::ResponseField<note::string_view> mode{};
            /// If specified, the voltage-variable period.
            note::ResponseField<note::string_view> vseconds{};
            /// If geofence is enabled, the geofence center latitude in degrees.
            note::ResponseField<double> lat{};
            /// If geofence is enabled, the geofence center longitude in
            /// degrees.
            note::ResponseField<double> lon{};
            /// If geofence is enabled, the meters from geofence center.
            note::ResponseField<note::json_int_t> max{};
            /// If geofence is enabled, the currently configured geofence
            /// debounce period.
            note::ResponseField<note::json_int_t> minutes{};
            /// If specified, the periodic sample interval.
            note::ResponseField<note::json_int_t> seconds{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            /// When in periodic mode, the number of motion events (registered
            /// by the built-in accelerometer) required to trigger GPS to turn
            /// on.
            ///
            /// @since{3.4.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
            [[deprecated("requires firmware >= 3.4.1")]]
#endif
            note::ResponseField<note::json_int_t> threshold{};
#endif

            /// Allocator that minted this Response's interned string fields,
            /// attached by Notecard execute paths when the Response is parsed.
            /// Empty == no cleanup needed (default-constructed Response, or a
            /// tree-mode parse with no `set_allocator` configured).
            ::note::AllocatorRef alloc_;

            ~Response() {
                if (!alloc_) return;
#if NOTE_RESPONSE_RELEASE_LOOP
                ::note::detail::release_string_fields(*alloc_,
                    &mode,
                    2);
#else
                ::note::detail::deallocate_if_present(*alloc_, mode.value());
                ::note::detail::deallocate_if_present(*alloc_, vseconds.value());
#endif
            }

            // Move-only: copying a Response would alias the interned-string
            // ownership and double-free on the second destruction. AllocatorRef
            // nulls the source's pointer so only the live owner runs cleanup.
            Response() = default;
            Response(Response&&) noexcept = default;
            Response& operator=(Response&& o) noexcept {
                if (this != &o) {
                    this->~Response();
                    ::new (this) Response(::std::move(o));
                }
                return *this;
            }
            Response(const Response&) = delete;
            Response& operator=(const Response&) = delete;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("mode")) rsp.mode = reader_->get_string("mode");
                if (reader_->has("vseconds")) rsp.vseconds = reader_->get_string("vseconds");
                if (reader_->has("lat")) rsp.lat = reader_->get_double("lat");
                if (reader_->has("lon")) rsp.lon = reader_->get_double("lon");
                if (reader_->has("max")) rsp.max = reader_->get_int("max");
                if (reader_->has("minutes")) rsp.minutes = reader_->get_int("minutes");
                if (reader_->has("seconds")) rsp.seconds = reader_->get_int("seconds");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (reader_->has("threshold")) rsp.threshold = reader_->get_int("threshold");
#endif
                rsp.reader_ = std::move(reader_);
                return rsp;
            }
#pragma GCC diagnostic pop

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("mode")) rsp.mode = reader_.get_string("mode");
                if (reader_.has("vseconds")) rsp.vseconds = reader_.get_string("vseconds");
                if (reader_.has("lat")) rsp.lat = reader_.get_double("lat");
                if (reader_.has("lon")) rsp.lon = reader_.get_double("lon");
                if (reader_.has("max")) rsp.max = reader_.get_int("max");
                if (reader_.has("minutes")) rsp.minutes = reader_.get_int("minutes");
                if (reader_.has("seconds")) rsp.seconds = reader_.get_int("seconds");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (reader_.has("threshold")) rsp.threshold = reader_.get_int("threshold");
#endif
                return rsp;
            }
#pragma GCC diagnostic pop
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_mode) == k_) { rsp.mode = v_; return; }
                    if (note::flash(keys_::rsp_vseconds) == k_) { rsp.vseconds = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_number(::note::string_view k_, ::note::string_view raw_) {
                    if (note::flash(keys_::rsp_max) == k_) { rsp.max = ::note::parse_int(raw_); return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = ::note::parse_int(raw_); return; }
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = ::note::parse_int(raw_); return; }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_threshold) == k_) { rsp.threshold = ::note::parse_int(raw_); return; }
#endif
                    if (note::flash(keys_::rsp_lat) == k_) { rsp.lat = ::note::parse_double(raw_); return; }
                    if (note::flash(keys_::rsp_lon) == k_) { rsp.lon = ::note::parse_double(raw_); return; }
                }
                NOTE_SINK_NOINLINE void on_int(::note::string_view k_, ::note::json_int_t v_) {
                    if (note::flash(keys_::rsp_max) == k_) { rsp.max = v_; return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = v_; return; }
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = v_; return; }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_threshold) == k_) { rsp.threshold = v_; return; }
#endif
                }
                NOTE_SINK_NOINLINE void on_float(::note::string_view k_, double v_) {
                    if (note::flash(keys_::rsp_lat) == k_) { rsp.lat = v_; return; }
                    if (note::flash(keys_::rsp_lon) == k_) { rsp.lon = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };
#pragma GCC diagnostic pop

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            void intern_strings(::note::StringPool& pool) {
                if (!mode.empty()) mode = pool.intern(mode);
                if (!vseconds.empty()) vseconds = pool.intern(vseconds);
            }
#pragma GCC diagnostic pop

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"mode\":");
                n += note::detail::print_json_value(p, mode.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"vseconds\":");
                n += note::detail::print_json_value(p, vseconds.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"lat\":");
                n += note::detail::print_json_value(p, lat.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"lon\":");
                n += note::detail::print_json_value(p, lon.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"max\":");
                n += note::detail::print_json_value(p, max.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"minutes\":");
                n += note::detail::print_json_value(p, minutes.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"seconds\":");
                n += note::detail::print_json_value(p, seconds.value());
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"threshold\":");
                n += note::detail::print_json_value(p, threshold.value());
#endif
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardLocationMode::Get&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (delete_) {
                n += p.print(",\"delete\":");
                n += note::detail::print_json_value(p, *delete_);
            }
            if (lat) {
                n += p.print(",\"lat\":");
                n += note::detail::print_json_value(p, *lat);
            }
            if (lon) {
                n += p.print(",\"lon\":");
                n += note::detail::print_json_value(p, *lon);
            }
            if (max) {
                n += p.print(",\"max\":");
                n += note::detail::print_json_value(p, *max);
            }
            if (minutes) {
                n += p.print(",\"minutes\":");
                n += note::detail::print_json_value(p, *minutes);
            }
            if (mode) {
                n += p.print(",\"mode\":");
                n += note::detail::print_json_value(p, *mode);
            }
            if (seconds) {
                n += p.print(",\"seconds\":");
                n += note::detail::print_json_value(p, *seconds);
            }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            if (threshold) {
                n += p.print(",\"threshold\":");
                n += note::detail::print_json_value(p, *threshold);
            }
#endif
            if (vseconds) {
                n += p.print(",\"vseconds\":");
                n += note::detail::print_json_value(p, *vseconds);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Sets location-related configuration settings. Retrieves the current
    /// location mode when passed with no argument.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Set {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.location.mode";
            static constexpr char delete_[] NOTE_FLASH_ATTR = "delete";
            static constexpr char lat[] NOTE_FLASH_ATTR = "lat";
            static constexpr char lon[] NOTE_FLASH_ATTR = "lon";
            static constexpr char max[] NOTE_FLASH_ATTR = "max";
            static constexpr char minutes[] NOTE_FLASH_ATTR = "minutes";
            static constexpr char mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char threshold[] NOTE_FLASH_ATTR = "threshold";
            static constexpr char vseconds[] NOTE_FLASH_ATTR = "vseconds";
            static constexpr char rsp_mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char rsp_vseconds[] NOTE_FLASH_ATTR = "vseconds";
            static constexpr char rsp_lat[] NOTE_FLASH_ATTR = "lat";
            static constexpr char rsp_lon[] NOTE_FLASH_ATTR = "lon";
            static constexpr char rsp_max[] NOTE_FLASH_ATTR = "max";
            static constexpr char rsp_minutes[] NOTE_FLASH_ATTR = "minutes";
            static constexpr char rsp_seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char rsp_threshold[] NOTE_FLASH_ATTR = "threshold";
        };

        static constexpr string_view notecard_request = "card.location.mode";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// Set to `true` to delete the last known location stored in the
        /// Notecard.
        struct delete_t : Field<bool> {
            using Field<bool>::Field;
            using Field<bool>::operator=;
            /// Set to `true` to delete the last known location stored in the
            /// Notecard.
            CardLocationMode::Set& operator()(bool v);
        } delete_{};
        /// When in periodic or continuous mode, providing this value enables
        /// geofencing. The value you provide for this argument should be the
        /// latitude of the center of the geofence, in degrees. When in fixed
        /// mode, the value you provide for this argument should be the latitude
        /// location of the device itself, in degrees.
        struct lat_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// When in periodic or continuous mode, providing this value
            /// enables geofencing. The value you provide for this argument
            /// should be the latitude of the center of the geofence, in
            /// degrees. When in fixed mode, the value you provide for this
            /// argument should be the latitude location of the device itself,
            /// in degrees.
            CardLocationMode::Set& operator()(double v);
        } lat{};
        /// When in periodic or continuous mode, providing this value enables
        /// geofencing. The value you provide for this argument should be the
        /// longitude of the center of the geofence, in degrees. When in fixed
        /// mode, the value you provide for this argument should be the
        /// longitude location of the device itself, in degrees.
        struct lon_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// When in periodic or continuous mode, providing this value
            /// enables geofencing. The value you provide for this argument
            /// should be the longitude of the center of the geofence, in
            /// degrees. When in fixed mode, the value you provide for this
            /// argument should be the longitude location of the device itself,
            /// in degrees.
            CardLocationMode::Set& operator()(double v);
        } lon{};
        /// Meters from a geofence center. Used to enable geofence location
        /// tracking.
        struct max_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// Meters from a geofence center. Used to enable geofence location
            /// tracking.
            CardLocationMode::Set& operator()(note::json_int_t v);
        } max{};
        /// When geofence is enabled, the number of minutes the device should be
        /// outside the geofence before the Notecard location is tracked.
        struct minutes_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When geofence is enabled, the number of minutes the device
            /// should be outside the geofence before the Notecard location is
            /// tracked.
            CardLocationMode::Set& operator()(note::json_int_t v);
        } minutes{};
        /// Sets the location mode.
        // mode:  | off | periodic | continuous | fixed | -
        struct mode_t : Field<note::string_view> {
#if __cplusplus >= 202002L && !defined(__clang__)
            constexpr mode_t() = default;
            template<std::size_t N>
            consteval mode_t(const char (&s)[N])
                : Field<note::string_view>(note::string_view(s, N - 1)) {
                note::string_view sv(s, N - 1);
                if (sv != "" && sv != "off" && sv != "periodic" && sv != "continuous" && sv != "fixed" && sv != "-")
                    throw "card.location.mode: invalid value for 'mode'";
            }
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, mode_t>)
            constexpr mode_t(U&& v) : Field<note::string_view>(note::string_view(std::forward<U>(v))) {}
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, mode_t>)
            mode_t& operator=(U&& v) {
                Field<note::string_view>::operator=(note::string_view(std::forward<U>(v)));
                return *this;
            }
            mode_t& operator=(std::nullopt_t) { Field<note::string_view>::reset(); return *this; }
            mode_t(const mode_t&) = default;
            mode_t& operator=(const mode_t&) = default;
            mode_t(mode_t&&) = default;
            mode_t& operator=(mode_t&&) = default;
#else
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
#endif
            static constexpr note::string_view off{"off"};
            static constexpr note::string_view periodic{"periodic"};
            static constexpr note::string_view continuous{"continuous"};
            static constexpr note::string_view fixed{"fixed"};
            static constexpr note::string_view _{"-"};
            /// Sets the location mode.
            CardLocationMode::Set& operator()(note::string_view v);
        } mode{};
        /// When in `periodic` mode, location will be sampled at this interval,
        /// if the Notecard detects motion. If seconds is < 300, during periods
        /// of sustained movement the Notecard will leave its onboard GPS/GNSS
        /// on continuously to avoid powering the module on and off repeatedly.
        struct seconds_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When in `periodic` mode, location will be sampled at this
            /// interval, if the Notecard detects motion. If seconds is < 300,
            /// during periods of sustained movement the Notecard will leave its
            /// onboard GPS/GNSS on continuously to avoid powering the module on
            /// and off repeatedly.
            CardLocationMode::Set& operator()(note::json_int_t v);
        } seconds{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        /// When in `periodic` mode, the number of motion events (registered by
        /// the built-in accelerometer) required to trigger GPS to turn on.
        ///
        /// @since{3.4.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
        [[deprecated("requires firmware >= 3.4.1")]]
#endif
        struct threshold_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When in `periodic` mode, the number of motion events (registered
            /// by the built-in accelerometer) required to trigger GPS to turn
            /// on.
            ///
            /// @since{3.4.1}
            CardLocationMode::Set& operator()(note::json_int_t v);
        } threshold{};
#endif
        /// In `periodic` mode, overrides `seconds` with a voltage-variable
        /// value.
        struct vseconds_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// In `periodic` mode, overrides `seconds` with a voltage-variable
            /// value.
            CardLocationMode::Set& operator()(note::string_view v);
        } vseconds{};

        // Valid values for 'mode':
        //   "off" — Turns location mode off. Approximate location may still be [ascertain...
        //   "periodic" — Samples location at a specified interval, if the device has moved.
        //   "continuous" — Enables the Notecard's onboard GPS/GNSS module for continuous samplin...
        //   "fixed" — Reports the location as a fixed location using the specified `lat` an...
        //   "-" — Resets the mode to the default value (`off`).
        // consteval: only callable at compile time (C++20)
#if __cplusplus >= 202002L
        static consteval note::string_view validatedMode(const char* v) {
            note::string_view sv{v};
            if (sv != "" && sv != "off" && sv != "periodic" && sv != "continuous" && sv != "fixed" && sv != "-")
                throw "card.location.mode: invalid value for 'mode'";
            return sv;
        }
#endif

#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "delete") return note::dyn_field_for(delete_);
            if (k_ == "lat") return note::dyn_field_for(lat);
            if (k_ == "lon") return note::dyn_field_for(lon);
            if (k_ == "max") return note::dyn_field_for(max);
            if (k_ == "minutes") return note::dyn_field_for(minutes);
            if (k_ == "mode") return note::dyn_field_for(mode);
            if (k_ == "seconds") return note::dyn_field_for(seconds);
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            if (k_ == "threshold") return note::dyn_field_for(threshold);
#endif
            if (k_ == "vseconds") return note::dyn_field_for(vseconds);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Successful response
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(33) +
                ::note::detail::arena_cost(49) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// The current location mode.
            note::ResponseField<note::string_view> mode{};
            /// If specified, the voltage-variable period.
            note::ResponseField<note::string_view> vseconds{};
            /// If geofence is enabled, the geofence center latitude in degrees.
            note::ResponseField<double> lat{};
            /// If geofence is enabled, the geofence center longitude in
            /// degrees.
            note::ResponseField<double> lon{};
            /// If geofence is enabled, the meters from geofence center.
            note::ResponseField<note::json_int_t> max{};
            /// If geofence is enabled, the currently configured geofence
            /// debounce period.
            note::ResponseField<note::json_int_t> minutes{};
            /// If specified, the periodic sample interval.
            note::ResponseField<note::json_int_t> seconds{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            /// When in periodic mode, the number of motion events (registered
            /// by the built-in accelerometer) required to trigger GPS to turn
            /// on.
            ///
            /// @since{3.4.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
            [[deprecated("requires firmware >= 3.4.1")]]
#endif
            note::ResponseField<note::json_int_t> threshold{};
#endif

            /// Allocator that minted this Response's interned string fields,
            /// attached by Notecard execute paths when the Response is parsed.
            /// Empty == no cleanup needed (default-constructed Response, or a
            /// tree-mode parse with no `set_allocator` configured).
            ::note::AllocatorRef alloc_;

            ~Response() {
                if (!alloc_) return;
#if NOTE_RESPONSE_RELEASE_LOOP
                ::note::detail::release_string_fields(*alloc_,
                    &mode,
                    2);
#else
                ::note::detail::deallocate_if_present(*alloc_, mode.value());
                ::note::detail::deallocate_if_present(*alloc_, vseconds.value());
#endif
            }

            // Move-only: copying a Response would alias the interned-string
            // ownership and double-free on the second destruction. AllocatorRef
            // nulls the source's pointer so only the live owner runs cleanup.
            Response() = default;
            Response(Response&&) noexcept = default;
            Response& operator=(Response&& o) noexcept {
                if (this != &o) {
                    this->~Response();
                    ::new (this) Response(::std::move(o));
                }
                return *this;
            }
            Response(const Response&) = delete;
            Response& operator=(const Response&) = delete;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("mode")) rsp.mode = reader_->get_string("mode");
                if (reader_->has("vseconds")) rsp.vseconds = reader_->get_string("vseconds");
                if (reader_->has("lat")) rsp.lat = reader_->get_double("lat");
                if (reader_->has("lon")) rsp.lon = reader_->get_double("lon");
                if (reader_->has("max")) rsp.max = reader_->get_int("max");
                if (reader_->has("minutes")) rsp.minutes = reader_->get_int("minutes");
                if (reader_->has("seconds")) rsp.seconds = reader_->get_int("seconds");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (reader_->has("threshold")) rsp.threshold = reader_->get_int("threshold");
#endif
                rsp.reader_ = std::move(reader_);
                return rsp;
            }
#pragma GCC diagnostic pop

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("mode")) rsp.mode = reader_.get_string("mode");
                if (reader_.has("vseconds")) rsp.vseconds = reader_.get_string("vseconds");
                if (reader_.has("lat")) rsp.lat = reader_.get_double("lat");
                if (reader_.has("lon")) rsp.lon = reader_.get_double("lon");
                if (reader_.has("max")) rsp.max = reader_.get_int("max");
                if (reader_.has("minutes")) rsp.minutes = reader_.get_int("minutes");
                if (reader_.has("seconds")) rsp.seconds = reader_.get_int("seconds");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (reader_.has("threshold")) rsp.threshold = reader_.get_int("threshold");
#endif
                return rsp;
            }
#pragma GCC diagnostic pop
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_mode) == k_) { rsp.mode = v_; return; }
                    if (note::flash(keys_::rsp_vseconds) == k_) { rsp.vseconds = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_number(::note::string_view k_, ::note::string_view raw_) {
                    if (note::flash(keys_::rsp_max) == k_) { rsp.max = ::note::parse_int(raw_); return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = ::note::parse_int(raw_); return; }
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = ::note::parse_int(raw_); return; }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_threshold) == k_) { rsp.threshold = ::note::parse_int(raw_); return; }
#endif
                    if (note::flash(keys_::rsp_lat) == k_) { rsp.lat = ::note::parse_double(raw_); return; }
                    if (note::flash(keys_::rsp_lon) == k_) { rsp.lon = ::note::parse_double(raw_); return; }
                }
                NOTE_SINK_NOINLINE void on_int(::note::string_view k_, ::note::json_int_t v_) {
                    if (note::flash(keys_::rsp_max) == k_) { rsp.max = v_; return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = v_; return; }
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = v_; return; }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_threshold) == k_) { rsp.threshold = v_; return; }
#endif
                }
                NOTE_SINK_NOINLINE void on_float(::note::string_view k_, double v_) {
                    if (note::flash(keys_::rsp_lat) == k_) { rsp.lat = v_; return; }
                    if (note::flash(keys_::rsp_lon) == k_) { rsp.lon = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };
#pragma GCC diagnostic pop

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            void intern_strings(::note::StringPool& pool) {
                if (!mode.empty()) mode = pool.intern(mode);
                if (!vseconds.empty()) vseconds = pool.intern(vseconds);
            }
#pragma GCC diagnostic pop

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"mode\":");
                n += note::detail::print_json_value(p, mode.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"vseconds\":");
                n += note::detail::print_json_value(p, vseconds.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"lat\":");
                n += note::detail::print_json_value(p, lat.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"lon\":");
                n += note::detail::print_json_value(p, lon.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"max\":");
                n += note::detail::print_json_value(p, max.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"minutes\":");
                n += note::detail::print_json_value(p, minutes.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"seconds\":");
                n += note::detail::print_json_value(p, seconds.value());
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"threshold\":");
                n += note::detail::print_json_value(p, threshold.value());
#endif
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardLocationMode::Set&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (delete_) {
                n += p.print(",\"delete\":");
                n += note::detail::print_json_value(p, *delete_);
            }
            if (lat) {
                n += p.print(",\"lat\":");
                n += note::detail::print_json_value(p, *lat);
            }
            if (lon) {
                n += p.print(",\"lon\":");
                n += note::detail::print_json_value(p, *lon);
            }
            if (max) {
                n += p.print(",\"max\":");
                n += note::detail::print_json_value(p, *max);
            }
            if (minutes) {
                n += p.print(",\"minutes\":");
                n += note::detail::print_json_value(p, *minutes);
            }
            if (mode) {
                n += p.print(",\"mode\":");
                n += note::detail::print_json_value(p, *mode);
            }
            if (seconds) {
                n += p.print(",\"seconds\":");
                n += note::detail::print_json_value(p, *seconds);
            }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            if (threshold) {
                n += p.print(",\"threshold\":");
                n += note::detail::print_json_value(p, *threshold);
            }
#endif
            if (vseconds) {
                n += p.print(",\"vseconds\":");
                n += note::detail::print_json_value(p, *vseconds);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Enable continuous GPS/GNSS sampling.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Continuous {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.location.mode";
            static constexpr char threshold[] NOTE_FLASH_ATTR = "threshold";
            static constexpr char vseconds[] NOTE_FLASH_ATTR = "vseconds";
            static constexpr char mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char rsp_mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char rsp_vseconds[] NOTE_FLASH_ATTR = "vseconds";
            static constexpr char rsp_threshold[] NOTE_FLASH_ATTR = "threshold";
        };

        static constexpr string_view notecard_request = "card.location.mode";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        /// When in `periodic` mode, the number of motion events (registered by
        /// the built-in accelerometer) required to trigger GPS to turn on.
        ///
        /// @since{3.4.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
        [[deprecated("requires firmware >= 3.4.1")]]
#endif
        struct threshold_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When in `periodic` mode, the number of motion events (registered
            /// by the built-in accelerometer) required to trigger GPS to turn
            /// on.
            ///
            /// @since{3.4.1}
            CardLocationMode::Continuous& operator()(note::json_int_t v);
        } threshold{};
#endif
        /// In `periodic` mode, overrides `seconds` with a voltage-variable
        /// value.
        struct vseconds_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// In `periodic` mode, overrides `seconds` with a voltage-variable
            /// value.
            CardLocationMode::Continuous& operator()(note::string_view v);
        } vseconds{};


#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            if (k_ == "threshold") return note::dyn_field_for(threshold);
#endif
            if (k_ == "vseconds") return note::dyn_field_for(vseconds);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Enable continuous GPS/GNSS sampling.
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(33) +
                ::note::detail::arena_cost(49) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// The current location mode.
            note::ResponseField<note::string_view> mode{};
            /// If specified, the voltage-variable period.
            note::ResponseField<note::string_view> vseconds{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            /// When in periodic mode, the number of motion events (registered
            /// by the built-in accelerometer) required to trigger GPS to turn
            /// on.
            ///
            /// @since{3.4.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
            [[deprecated("requires firmware >= 3.4.1")]]
#endif
            note::ResponseField<note::json_int_t> threshold{};
#endif

            /// Allocator that minted this Response's interned string fields,
            /// attached by Notecard execute paths when the Response is parsed.
            /// Empty == no cleanup needed (default-constructed Response, or a
            /// tree-mode parse with no `set_allocator` configured).
            ::note::AllocatorRef alloc_;

            ~Response() {
                if (!alloc_) return;
#if NOTE_RESPONSE_RELEASE_LOOP
                ::note::detail::release_string_fields(*alloc_,
                    &mode,
                    2);
#else
                ::note::detail::deallocate_if_present(*alloc_, mode.value());
                ::note::detail::deallocate_if_present(*alloc_, vseconds.value());
#endif
            }

            // Move-only: copying a Response would alias the interned-string
            // ownership and double-free on the second destruction. AllocatorRef
            // nulls the source's pointer so only the live owner runs cleanup.
            Response() = default;
            Response(Response&&) noexcept = default;
            Response& operator=(Response&& o) noexcept {
                if (this != &o) {
                    this->~Response();
                    ::new (this) Response(::std::move(o));
                }
                return *this;
            }
            Response(const Response&) = delete;
            Response& operator=(const Response&) = delete;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("mode")) rsp.mode = reader_->get_string("mode");
                if (reader_->has("vseconds")) rsp.vseconds = reader_->get_string("vseconds");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (reader_->has("threshold")) rsp.threshold = reader_->get_int("threshold");
#endif
                rsp.reader_ = std::move(reader_);
                return rsp;
            }
#pragma GCC diagnostic pop

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("mode")) rsp.mode = reader_.get_string("mode");
                if (reader_.has("vseconds")) rsp.vseconds = reader_.get_string("vseconds");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (reader_.has("threshold")) rsp.threshold = reader_.get_int("threshold");
#endif
                return rsp;
            }
#pragma GCC diagnostic pop
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_mode) == k_) { rsp.mode = v_; return; }
                    if (note::flash(keys_::rsp_vseconds) == k_) { rsp.vseconds = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_number(::note::string_view k_, ::note::string_view raw_) {
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_threshold) == k_) { rsp.threshold = ::note::parse_int(raw_); return; }
#endif
                }
                NOTE_SINK_NOINLINE void on_int(::note::string_view k_, ::note::json_int_t v_) {
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_threshold) == k_) { rsp.threshold = v_; return; }
#endif
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };
#pragma GCC diagnostic pop

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            void intern_strings(::note::StringPool& pool) {
                if (!mode.empty()) mode = pool.intern(mode);
                if (!vseconds.empty()) vseconds = pool.intern(vseconds);
            }
#pragma GCC diagnostic pop

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"mode\":");
                n += note::detail::print_json_value(p, mode.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"vseconds\":");
                n += note::detail::print_json_value(p, vseconds.value());
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"threshold\":");
                n += note::detail::print_json_value(p, threshold.value());
#endif
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardLocationMode::Continuous&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            if (threshold) {
                n += p.print(",\"threshold\":");
                n += note::detail::print_json_value(p, *threshold);
            }
#endif
            if (vseconds) {
                n += p.print(",\"vseconds\":");
                n += note::detail::print_json_value(p, *vseconds);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Enable periodic location sampling, optionally with geofencing.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Periodic {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.location.mode";
            static constexpr char lat[] NOTE_FLASH_ATTR = "lat";
            static constexpr char lon[] NOTE_FLASH_ATTR = "lon";
            static constexpr char max[] NOTE_FLASH_ATTR = "max";
            static constexpr char minutes[] NOTE_FLASH_ATTR = "minutes";
            static constexpr char seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char threshold[] NOTE_FLASH_ATTR = "threshold";
            static constexpr char vseconds[] NOTE_FLASH_ATTR = "vseconds";
            static constexpr char mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char rsp_mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char rsp_vseconds[] NOTE_FLASH_ATTR = "vseconds";
            static constexpr char rsp_lat[] NOTE_FLASH_ATTR = "lat";
            static constexpr char rsp_lon[] NOTE_FLASH_ATTR = "lon";
            static constexpr char rsp_max[] NOTE_FLASH_ATTR = "max";
            static constexpr char rsp_minutes[] NOTE_FLASH_ATTR = "minutes";
            static constexpr char rsp_seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char rsp_threshold[] NOTE_FLASH_ATTR = "threshold";
        };

        static constexpr string_view notecard_request = "card.location.mode";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// When in periodic or continuous mode, providing this value enables
        /// geofencing. The value you provide for this argument should be the
        /// latitude of the center of the geofence, in degrees. When in fixed
        /// mode, the value you provide for this argument should be the latitude
        /// location of the device itself, in degrees.
        struct lat_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// When in periodic or continuous mode, providing this value
            /// enables geofencing. The value you provide for this argument
            /// should be the latitude of the center of the geofence, in
            /// degrees. When in fixed mode, the value you provide for this
            /// argument should be the latitude location of the device itself,
            /// in degrees.
            CardLocationMode::Periodic& operator()(double v);
        } lat{};
        /// When in periodic or continuous mode, providing this value enables
        /// geofencing. The value you provide for this argument should be the
        /// longitude of the center of the geofence, in degrees. When in fixed
        /// mode, the value you provide for this argument should be the
        /// longitude location of the device itself, in degrees.
        struct lon_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// When in periodic or continuous mode, providing this value
            /// enables geofencing. The value you provide for this argument
            /// should be the longitude of the center of the geofence, in
            /// degrees. When in fixed mode, the value you provide for this
            /// argument should be the longitude location of the device itself,
            /// in degrees.
            CardLocationMode::Periodic& operator()(double v);
        } lon{};
        /// Meters from a geofence center. Used to enable geofence location
        /// tracking.
        struct max_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// Meters from a geofence center. Used to enable geofence location
            /// tracking.
            CardLocationMode::Periodic& operator()(note::json_int_t v);
        } max{};
        /// When geofence is enabled, the number of minutes the device should be
        /// outside the geofence before the Notecard location is tracked.
        struct minutes_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When geofence is enabled, the number of minutes the device
            /// should be outside the geofence before the Notecard location is
            /// tracked.
            CardLocationMode::Periodic& operator()(note::json_int_t v);
        } minutes{};
        /// When in `periodic` mode, location will be sampled at this interval,
        /// if the Notecard detects motion. If seconds is < 300, during periods
        /// of sustained movement the Notecard will leave its onboard GPS/GNSS
        /// on continuously to avoid powering the module on and off repeatedly.
        struct seconds_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When in `periodic` mode, location will be sampled at this
            /// interval, if the Notecard detects motion. If seconds is < 300,
            /// during periods of sustained movement the Notecard will leave its
            /// onboard GPS/GNSS on continuously to avoid powering the module on
            /// and off repeatedly.
            CardLocationMode::Periodic& operator()(note::json_int_t v);
        } seconds{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        /// When in `periodic` mode, the number of motion events (registered by
        /// the built-in accelerometer) required to trigger GPS to turn on.
        ///
        /// @since{3.4.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
        [[deprecated("requires firmware >= 3.4.1")]]
#endif
        struct threshold_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When in `periodic` mode, the number of motion events (registered
            /// by the built-in accelerometer) required to trigger GPS to turn
            /// on.
            ///
            /// @since{3.4.1}
            CardLocationMode::Periodic& operator()(note::json_int_t v);
        } threshold{};
#endif
        /// In `periodic` mode, overrides `seconds` with a voltage-variable
        /// value.
        struct vseconds_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// In `periodic` mode, overrides `seconds` with a voltage-variable
            /// value.
            CardLocationMode::Periodic& operator()(note::string_view v);
        } vseconds{};


#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "lat") return note::dyn_field_for(lat);
            if (k_ == "lon") return note::dyn_field_for(lon);
            if (k_ == "max") return note::dyn_field_for(max);
            if (k_ == "minutes") return note::dyn_field_for(minutes);
            if (k_ == "seconds") return note::dyn_field_for(seconds);
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            if (k_ == "threshold") return note::dyn_field_for(threshold);
#endif
            if (k_ == "vseconds") return note::dyn_field_for(vseconds);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Enable periodic location sampling, optionally with geofencing.
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(33) +
                ::note::detail::arena_cost(49) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// The current location mode.
            note::ResponseField<note::string_view> mode{};
            /// If specified, the voltage-variable period.
            note::ResponseField<note::string_view> vseconds{};
            /// If geofence is enabled, the geofence center latitude in degrees.
            note::ResponseField<double> lat{};
            /// If geofence is enabled, the geofence center longitude in
            /// degrees.
            note::ResponseField<double> lon{};
            /// If geofence is enabled, the meters from geofence center.
            note::ResponseField<note::json_int_t> max{};
            /// If geofence is enabled, the currently configured geofence
            /// debounce period.
            note::ResponseField<note::json_int_t> minutes{};
            /// If specified, the periodic sample interval.
            note::ResponseField<note::json_int_t> seconds{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            /// When in periodic mode, the number of motion events (registered
            /// by the built-in accelerometer) required to trigger GPS to turn
            /// on.
            ///
            /// @since{3.4.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
            [[deprecated("requires firmware >= 3.4.1")]]
#endif
            note::ResponseField<note::json_int_t> threshold{};
#endif

            /// Allocator that minted this Response's interned string fields,
            /// attached by Notecard execute paths when the Response is parsed.
            /// Empty == no cleanup needed (default-constructed Response, or a
            /// tree-mode parse with no `set_allocator` configured).
            ::note::AllocatorRef alloc_;

            ~Response() {
                if (!alloc_) return;
#if NOTE_RESPONSE_RELEASE_LOOP
                ::note::detail::release_string_fields(*alloc_,
                    &mode,
                    2);
#else
                ::note::detail::deallocate_if_present(*alloc_, mode.value());
                ::note::detail::deallocate_if_present(*alloc_, vseconds.value());
#endif
            }

            // Move-only: copying a Response would alias the interned-string
            // ownership and double-free on the second destruction. AllocatorRef
            // nulls the source's pointer so only the live owner runs cleanup.
            Response() = default;
            Response(Response&&) noexcept = default;
            Response& operator=(Response&& o) noexcept {
                if (this != &o) {
                    this->~Response();
                    ::new (this) Response(::std::move(o));
                }
                return *this;
            }
            Response(const Response&) = delete;
            Response& operator=(const Response&) = delete;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("mode")) rsp.mode = reader_->get_string("mode");
                if (reader_->has("vseconds")) rsp.vseconds = reader_->get_string("vseconds");
                if (reader_->has("lat")) rsp.lat = reader_->get_double("lat");
                if (reader_->has("lon")) rsp.lon = reader_->get_double("lon");
                if (reader_->has("max")) rsp.max = reader_->get_int("max");
                if (reader_->has("minutes")) rsp.minutes = reader_->get_int("minutes");
                if (reader_->has("seconds")) rsp.seconds = reader_->get_int("seconds");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (reader_->has("threshold")) rsp.threshold = reader_->get_int("threshold");
#endif
                rsp.reader_ = std::move(reader_);
                return rsp;
            }
#pragma GCC diagnostic pop

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("mode")) rsp.mode = reader_.get_string("mode");
                if (reader_.has("vseconds")) rsp.vseconds = reader_.get_string("vseconds");
                if (reader_.has("lat")) rsp.lat = reader_.get_double("lat");
                if (reader_.has("lon")) rsp.lon = reader_.get_double("lon");
                if (reader_.has("max")) rsp.max = reader_.get_int("max");
                if (reader_.has("minutes")) rsp.minutes = reader_.get_int("minutes");
                if (reader_.has("seconds")) rsp.seconds = reader_.get_int("seconds");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (reader_.has("threshold")) rsp.threshold = reader_.get_int("threshold");
#endif
                return rsp;
            }
#pragma GCC diagnostic pop
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_mode) == k_) { rsp.mode = v_; return; }
                    if (note::flash(keys_::rsp_vseconds) == k_) { rsp.vseconds = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_number(::note::string_view k_, ::note::string_view raw_) {
                    if (note::flash(keys_::rsp_max) == k_) { rsp.max = ::note::parse_int(raw_); return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = ::note::parse_int(raw_); return; }
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = ::note::parse_int(raw_); return; }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_threshold) == k_) { rsp.threshold = ::note::parse_int(raw_); return; }
#endif
                    if (note::flash(keys_::rsp_lat) == k_) { rsp.lat = ::note::parse_double(raw_); return; }
                    if (note::flash(keys_::rsp_lon) == k_) { rsp.lon = ::note::parse_double(raw_); return; }
                }
                NOTE_SINK_NOINLINE void on_int(::note::string_view k_, ::note::json_int_t v_) {
                    if (note::flash(keys_::rsp_max) == k_) { rsp.max = v_; return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = v_; return; }
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = v_; return; }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_threshold) == k_) { rsp.threshold = v_; return; }
#endif
                }
                NOTE_SINK_NOINLINE void on_float(::note::string_view k_, double v_) {
                    if (note::flash(keys_::rsp_lat) == k_) { rsp.lat = v_; return; }
                    if (note::flash(keys_::rsp_lon) == k_) { rsp.lon = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };
#pragma GCC diagnostic pop

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            void intern_strings(::note::StringPool& pool) {
                if (!mode.empty()) mode = pool.intern(mode);
                if (!vseconds.empty()) vseconds = pool.intern(vseconds);
            }
#pragma GCC diagnostic pop

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"mode\":");
                n += note::detail::print_json_value(p, mode.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"vseconds\":");
                n += note::detail::print_json_value(p, vseconds.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"lat\":");
                n += note::detail::print_json_value(p, lat.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"lon\":");
                n += note::detail::print_json_value(p, lon.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"max\":");
                n += note::detail::print_json_value(p, max.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"minutes\":");
                n += note::detail::print_json_value(p, minutes.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"seconds\":");
                n += note::detail::print_json_value(p, seconds.value());
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"threshold\":");
                n += note::detail::print_json_value(p, threshold.value());
#endif
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardLocationMode::Periodic&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (lat) {
                n += p.print(",\"lat\":");
                n += note::detail::print_json_value(p, *lat);
            }
            if (lon) {
                n += p.print(",\"lon\":");
                n += note::detail::print_json_value(p, *lon);
            }
            if (max) {
                n += p.print(",\"max\":");
                n += note::detail::print_json_value(p, *max);
            }
            if (minutes) {
                n += p.print(",\"minutes\":");
                n += note::detail::print_json_value(p, *minutes);
            }
            if (seconds) {
                n += p.print(",\"seconds\":");
                n += note::detail::print_json_value(p, *seconds);
            }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            if (threshold) {
                n += p.print(",\"threshold\":");
                n += note::detail::print_json_value(p, *threshold);
            }
#endif
            if (vseconds) {
                n += p.print(",\"vseconds\":");
                n += note::detail::print_json_value(p, *vseconds);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Set a fixed location for the device.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Fixed {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.location.mode";
            static constexpr char lat[] NOTE_FLASH_ATTR = "lat";
            static constexpr char lon[] NOTE_FLASH_ATTR = "lon";
            static constexpr char mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char rsp_mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char rsp_lat[] NOTE_FLASH_ATTR = "lat";
            static constexpr char rsp_lon[] NOTE_FLASH_ATTR = "lon";
        };

        static constexpr string_view notecard_request = "card.location.mode";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Idempotent;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// When in periodic or continuous mode, providing this value enables
        /// geofencing. The value you provide for this argument should be the
        /// latitude of the center of the geofence, in degrees. When in fixed
        /// mode, the value you provide for this argument should be the latitude
        /// location of the device itself, in degrees.
        struct lat_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// When in periodic or continuous mode, providing this value
            /// enables geofencing. The value you provide for this argument
            /// should be the latitude of the center of the geofence, in
            /// degrees. When in fixed mode, the value you provide for this
            /// argument should be the latitude location of the device itself,
            /// in degrees.
            CardLocationMode::Fixed& operator()(double v);
        } lat{};
        /// When in periodic or continuous mode, providing this value enables
        /// geofencing. The value you provide for this argument should be the
        /// longitude of the center of the geofence, in degrees. When in fixed
        /// mode, the value you provide for this argument should be the
        /// longitude location of the device itself, in degrees.
        struct lon_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// When in periodic or continuous mode, providing this value
            /// enables geofencing. The value you provide for this argument
            /// should be the longitude of the center of the geofence, in
            /// degrees. When in fixed mode, the value you provide for this
            /// argument should be the longitude location of the device itself,
            /// in degrees.
            CardLocationMode::Fixed& operator()(double v);
        } lon{};


#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "lat") return note::dyn_field_for(lat);
            if (k_ == "lon") return note::dyn_field_for(lon);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Set a fixed location for the device.
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(33) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// The current location mode.
            note::ResponseField<note::string_view> mode{};
            /// If geofence is enabled, the geofence center latitude in degrees.
            note::ResponseField<double> lat{};
            /// If geofence is enabled, the geofence center longitude in
            /// degrees.
            note::ResponseField<double> lon{};

            /// Allocator that minted this Response's interned string fields,
            /// attached by Notecard execute paths when the Response is parsed.
            /// Empty == no cleanup needed (default-constructed Response, or a
            /// tree-mode parse with no `set_allocator` configured).
            ::note::AllocatorRef alloc_;

            ~Response() {
                if (!alloc_) return;
#if NOTE_RESPONSE_RELEASE_LOOP
                ::note::detail::release_string_fields(*alloc_,
                    &mode,
                    1);
#else
                ::note::detail::deallocate_if_present(*alloc_, mode.value());
#endif
            }

            // Move-only: copying a Response would alias the interned-string
            // ownership and double-free on the second destruction. AllocatorRef
            // nulls the source's pointer so only the live owner runs cleanup.
            Response() = default;
            Response(Response&&) noexcept = default;
            Response& operator=(Response&& o) noexcept {
                if (this != &o) {
                    this->~Response();
                    ::new (this) Response(::std::move(o));
                }
                return *this;
            }
            Response(const Response&) = delete;
            Response& operator=(const Response&) = delete;

#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("mode")) rsp.mode = reader_->get_string("mode");
                if (reader_->has("lat")) rsp.lat = reader_->get_double("lat");
                if (reader_->has("lon")) rsp.lon = reader_->get_double("lon");
                rsp.reader_ = std::move(reader_);
                return rsp;
            }

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("mode")) rsp.mode = reader_.get_string("mode");
                if (reader_.has("lat")) rsp.lat = reader_.get_double("lat");
                if (reader_.has("lon")) rsp.lon = reader_.get_double("lon");
                return rsp;
            }
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_mode) == k_) { rsp.mode = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_number(::note::string_view k_, ::note::string_view raw_) {
                    if (note::flash(keys_::rsp_lat) == k_) { rsp.lat = ::note::parse_double(raw_); return; }
                    if (note::flash(keys_::rsp_lon) == k_) { rsp.lon = ::note::parse_double(raw_); return; }
                }
                NOTE_SINK_NOINLINE void on_float(::note::string_view k_, double v_) {
                    if (note::flash(keys_::rsp_lat) == k_) { rsp.lat = v_; return; }
                    if (note::flash(keys_::rsp_lon) == k_) { rsp.lon = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };

            void intern_strings(::note::StringPool& pool) {
                if (!mode.empty()) mode = pool.intern(mode);
            }

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"mode\":");
                n += note::detail::print_json_value(p, mode.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"lat\":");
                n += note::detail::print_json_value(p, lat.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"lon\":");
                n += note::detail::print_json_value(p, lon.value());
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardLocationMode::Fixed&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (lat) {
                n += p.print(",\"lat\":");
                n += note::detail::print_json_value(p, *lat);
            }
            if (lon) {
                n += p.print(",\"lon\":");
                n += note::detail::print_json_value(p, *lon);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };

    /// Sets location-related configuration settings. Retrieves the current
    /// location mode when passed with no argument.
    ///
    /// @skus{CELL,CELL+WIFI,LORA,SKYLO,WIFI}
    struct Remove {
        struct keys_ {
            static constexpr char req[] NOTE_FLASH_ATTR = "card.location.mode";
            static constexpr char delete_[] NOTE_FLASH_ATTR = "delete";
            static constexpr char lat[] NOTE_FLASH_ATTR = "lat";
            static constexpr char lon[] NOTE_FLASH_ATTR = "lon";
            static constexpr char max[] NOTE_FLASH_ATTR = "max";
            static constexpr char minutes[] NOTE_FLASH_ATTR = "minutes";
            static constexpr char mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char threshold[] NOTE_FLASH_ATTR = "threshold";
            static constexpr char vseconds[] NOTE_FLASH_ATTR = "vseconds";
            static constexpr char rsp_mode[] NOTE_FLASH_ATTR = "mode";
            static constexpr char rsp_vseconds[] NOTE_FLASH_ATTR = "vseconds";
            static constexpr char rsp_lat[] NOTE_FLASH_ATTR = "lat";
            static constexpr char rsp_lon[] NOTE_FLASH_ATTR = "lon";
            static constexpr char rsp_max[] NOTE_FLASH_ATTR = "max";
            static constexpr char rsp_minutes[] NOTE_FLASH_ATTR = "minutes";
            static constexpr char rsp_seconds[] NOTE_FLASH_ATTR = "seconds";
            static constexpr char rsp_threshold[] NOTE_FLASH_ATTR = "threshold";
        };

        static constexpr string_view notecard_request = "card.location.mode";
        static constexpr bool supports_cmd = true;
        static constexpr Safety safety = Safety::Destructive;
        static constexpr RadiosSupport radios{};
        static constexpr Firmware min_firmware{};

#if !NOTE_SINGLETON
        void* nc_ = nullptr;
#endif


        /// When in periodic or continuous mode, providing this value enables
        /// geofencing. The value you provide for this argument should be the
        /// latitude of the center of the geofence, in degrees. When in fixed
        /// mode, the value you provide for this argument should be the latitude
        /// location of the device itself, in degrees.
        struct lat_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// When in periodic or continuous mode, providing this value
            /// enables geofencing. The value you provide for this argument
            /// should be the latitude of the center of the geofence, in
            /// degrees. When in fixed mode, the value you provide for this
            /// argument should be the latitude location of the device itself,
            /// in degrees.
            CardLocationMode::Remove& operator()(double v);
        } lat{};
        /// When in periodic or continuous mode, providing this value enables
        /// geofencing. The value you provide for this argument should be the
        /// longitude of the center of the geofence, in degrees. When in fixed
        /// mode, the value you provide for this argument should be the
        /// longitude location of the device itself, in degrees.
        struct lon_t : Field<double> {
            using Field<double>::Field;
            using Field<double>::operator=;
            /// When in periodic or continuous mode, providing this value
            /// enables geofencing. The value you provide for this argument
            /// should be the longitude of the center of the geofence, in
            /// degrees. When in fixed mode, the value you provide for this
            /// argument should be the longitude location of the device itself,
            /// in degrees.
            CardLocationMode::Remove& operator()(double v);
        } lon{};
        /// Meters from a geofence center. Used to enable geofence location
        /// tracking.
        struct max_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// Meters from a geofence center. Used to enable geofence location
            /// tracking.
            CardLocationMode::Remove& operator()(note::json_int_t v);
        } max{};
        /// When geofence is enabled, the number of minutes the device should be
        /// outside the geofence before the Notecard location is tracked.
        struct minutes_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When geofence is enabled, the number of minutes the device
            /// should be outside the geofence before the Notecard location is
            /// tracked.
            CardLocationMode::Remove& operator()(note::json_int_t v);
        } minutes{};
        /// Sets the location mode.
        // mode:  | off | periodic | continuous | fixed | -
        struct mode_t : Field<note::string_view> {
#if __cplusplus >= 202002L && !defined(__clang__)
            constexpr mode_t() = default;
            template<std::size_t N>
            consteval mode_t(const char (&s)[N])
                : Field<note::string_view>(note::string_view(s, N - 1)) {
                note::string_view sv(s, N - 1);
                if (sv != "" && sv != "off" && sv != "periodic" && sv != "continuous" && sv != "fixed" && sv != "-")
                    throw "card.location.mode: invalid value for 'mode'";
            }
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, mode_t>)
            constexpr mode_t(U&& v) : Field<note::string_view>(note::string_view(std::forward<U>(v))) {}
            template<typename U>
                requires std::is_convertible_v<U, note::string_view>
                      && (!std::is_array_v<std::remove_reference_t<U>>)
                      && (!std::is_same_v<std::decay_t<U>, mode_t>)
            mode_t& operator=(U&& v) {
                Field<note::string_view>::operator=(note::string_view(std::forward<U>(v)));
                return *this;
            }
            mode_t& operator=(std::nullopt_t) { Field<note::string_view>::reset(); return *this; }
            mode_t(const mode_t&) = default;
            mode_t& operator=(const mode_t&) = default;
            mode_t(mode_t&&) = default;
            mode_t& operator=(mode_t&&) = default;
#else
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
#endif
            static constexpr note::string_view off{"off"};
            static constexpr note::string_view periodic{"periodic"};
            static constexpr note::string_view continuous{"continuous"};
            static constexpr note::string_view fixed{"fixed"};
            static constexpr note::string_view _{"-"};
            /// Sets the location mode.
            CardLocationMode::Remove& operator()(note::string_view v);
        } mode{};
        /// When in `periodic` mode, location will be sampled at this interval,
        /// if the Notecard detects motion. If seconds is < 300, during periods
        /// of sustained movement the Notecard will leave its onboard GPS/GNSS
        /// on continuously to avoid powering the module on and off repeatedly.
        struct seconds_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When in `periodic` mode, location will be sampled at this
            /// interval, if the Notecard detects motion. If seconds is < 300,
            /// during periods of sustained movement the Notecard will leave its
            /// onboard GPS/GNSS on continuously to avoid powering the module on
            /// and off repeatedly.
            CardLocationMode::Remove& operator()(note::json_int_t v);
        } seconds{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        /// When in `periodic` mode, the number of motion events (registered by
        /// the built-in accelerometer) required to trigger GPS to turn on.
        ///
        /// @since{3.4.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
        [[deprecated("requires firmware >= 3.4.1")]]
#endif
        struct threshold_t : Field<note::json_int_t> {
            using Field<note::json_int_t>::Field;
            using Field<note::json_int_t>::operator=;
            /// When in `periodic` mode, the number of motion events (registered
            /// by the built-in accelerometer) required to trigger GPS to turn
            /// on.
            ///
            /// @since{3.4.1}
            CardLocationMode::Remove& operator()(note::json_int_t v);
        } threshold{};
#endif
        /// In `periodic` mode, overrides `seconds` with a voltage-variable
        /// value.
        struct vseconds_t : Field<note::string_view> {
            using Field<note::string_view>::Field;
            using Field<note::string_view>::operator=;
            /// In `periodic` mode, overrides `seconds` with a voltage-variable
            /// value.
            CardLocationMode::Remove& operator()(note::string_view v);
        } vseconds{};

        // Valid values for 'mode':
        //   "off" — Turns location mode off. Approximate location may still be [ascertain...
        //   "periodic" — Samples location at a specified interval, if the device has moved.
        //   "continuous" — Enables the Notecard's onboard GPS/GNSS module for continuous samplin...
        //   "fixed" — Reports the location as a fixed location using the specified `lat` an...
        //   "-" — Resets the mode to the default value (`off`).
        // consteval: only callable at compile time (C++20)
#if __cplusplus >= 202002L
        static consteval note::string_view validatedMode(const char* v) {
            note::string_view sv{v};
            if (sv != "" && sv != "off" && sv != "periodic" && sv != "continuous" && sv != "fixed" && sv != "-")
                throw "card.location.mode: invalid value for 'mode'";
            return sv;
        }
#endif

#if NOTE_EXTRAS
        /// Add an arbitrary key/value pair to the request, beyond the typed fields
        /// declared above. Useful for fields the schema doesn't yet model.
        /// Capacity is bounded by NOTE_EXTRAS_MAX; excess pairs are silently dropped.
        template<typename T>
        auto& extra(note::string_view k_, T v_) {
            if (extras_count_ < NOTE_EXTRAS_MAX)
                extras_[extras_count_++] = {k_, note::DynValue{v_}};
            return *this;
        }
        /// String-literal overload of extra().
        auto& extra(note::string_view k_, const char* v_) {
            return extra(k_, note::string_view{v_});
        }

        /// Index-style access to fields by wire name. Returns a DynField proxy
        /// usable for assignment; unknown keys are added as extras (subject to
        /// NOTE_EXTRAS_MAX). Prefer the typed setters above when possible.
        note::DynField operator[](note::string_view k_) {
            if (k_ == "lat") return note::dyn_field_for(lat);
            if (k_ == "lon") return note::dyn_field_for(lon);
            if (k_ == "max") return note::dyn_field_for(max);
            if (k_ == "minutes") return note::dyn_field_for(minutes);
            if (k_ == "mode") return note::dyn_field_for(mode);
            if (k_ == "seconds") return note::dyn_field_for(seconds);
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            if (k_ == "threshold") return note::dyn_field_for(threshold);
#endif
            if (k_ == "vseconds") return note::dyn_field_for(vseconds);
            if (extras_count_ < NOTE_EXTRAS_MAX) {
                auto& slot = extras_[extras_count_++];
                slot.key = k_;
                return note::dyn_field_for(slot.value);
            }
            return {};
        }

        std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
        uint8_t extras_count_ = 0;
#endif // NOTE_EXTRAS

        /// Successful response
        struct Response {
            /// Compile-time arena budget for this response type.
            static constexpr size_t max_arena_size =
                ::note::detail::arena_cost(33) +
                ::note::detail::arena_cost(49) +
                ::note::detail::arena_cost(65);  // error reserve (+1 for null terminator)

            /// The current location mode.
            note::ResponseField<note::string_view> mode{};
            /// If specified, the voltage-variable period.
            note::ResponseField<note::string_view> vseconds{};
            /// If geofence is enabled, the geofence center latitude in degrees.
            note::ResponseField<double> lat{};
            /// If geofence is enabled, the geofence center longitude in
            /// degrees.
            note::ResponseField<double> lon{};
            /// If geofence is enabled, the meters from geofence center.
            note::ResponseField<note::json_int_t> max{};
            /// If geofence is enabled, the currently configured geofence
            /// debounce period.
            note::ResponseField<note::json_int_t> minutes{};
            /// If specified, the periodic sample interval.
            note::ResponseField<note::json_int_t> seconds{};
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            /// When in periodic mode, the number of motion events (registered
            /// by the built-in accelerometer) required to trigger GPS to turn
            /// on.
            ///
            /// @since{3.4.1}
#if NOTE_API_VERSION < NOTE_VERSION(3, 4, 1)
            [[deprecated("requires firmware >= 3.4.1")]]
#endif
            note::ResponseField<note::json_int_t> threshold{};
#endif

            /// Allocator that minted this Response's interned string fields,
            /// attached by Notecard execute paths when the Response is parsed.
            /// Empty == no cleanup needed (default-constructed Response, or a
            /// tree-mode parse with no `set_allocator` configured).
            ::note::AllocatorRef alloc_;

            ~Response() {
                if (!alloc_) return;
#if NOTE_RESPONSE_RELEASE_LOOP
                ::note::detail::release_string_fields(*alloc_,
                    &mode,
                    2);
#else
                ::note::detail::deallocate_if_present(*alloc_, mode.value());
                ::note::detail::deallocate_if_present(*alloc_, vseconds.value());
#endif
            }

            // Move-only: copying a Response would alias the interned-string
            // ownership and double-free on the second destruction. AllocatorRef
            // nulls the source's pointer so only the live owner runs cleanup.
            Response() = default;
            Response(Response&&) noexcept = default;
            Response& operator=(Response&& o) noexcept {
                if (this != &o) {
                    this->~Response();
                    ::new (this) Response(::std::move(o));
                }
                return *this;
            }
            Response(const Response&) = delete;
            Response& operator=(const Response&) = delete;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if !NOTE_NO_JSON_TREE
            static Response parse(std::unique_ptr<JsonReader> reader_) {
                Response rsp;
                if (reader_->has("mode")) rsp.mode = reader_->get_string("mode");
                if (reader_->has("vseconds")) rsp.vseconds = reader_->get_string("vseconds");
                if (reader_->has("lat")) rsp.lat = reader_->get_double("lat");
                if (reader_->has("lon")) rsp.lon = reader_->get_double("lon");
                if (reader_->has("max")) rsp.max = reader_->get_int("max");
                if (reader_->has("minutes")) rsp.minutes = reader_->get_int("minutes");
                if (reader_->has("seconds")) rsp.seconds = reader_->get_int("seconds");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (reader_->has("threshold")) rsp.threshold = reader_->get_int("threshold");
#endif
                rsp.reader_ = std::move(reader_);
                return rsp;
            }
#pragma GCC diagnostic pop

            // Non-owning parse: string_views point into the reader's data.
            // The reader (and its underlying JSON buffer) must outlive the Response,
            // or the caller must consume all string fields before the reader is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            static Response parse(const JsonReader& reader_) {
                Response rsp;
                if (reader_.has("mode")) rsp.mode = reader_.get_string("mode");
                if (reader_.has("vseconds")) rsp.vseconds = reader_.get_string("vseconds");
                if (reader_.has("lat")) rsp.lat = reader_.get_double("lat");
                if (reader_.has("lon")) rsp.lon = reader_.get_double("lon");
                if (reader_.has("max")) rsp.max = reader_.get_int("max");
                if (reader_.has("minutes")) rsp.minutes = reader_.get_int("minutes");
                if (reader_.has("seconds")) rsp.seconds = reader_.get_int("seconds");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (reader_.has("threshold")) rsp.threshold = reader_.get_int("threshold");
#endif
                return rsp;
            }
#pragma GCC diagnostic pop
#endif // !NOTE_NO_JSON_TREE

            // SAX sink — zero-allocation streaming parse into Response fields.
            // String fields are interned into the StringPool immediately, so
            // string_views survive after the parser's scratch buffer is reused.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            struct Sink : ::note::DefaultSink {
                Response& rsp;
                ::note::StringPool& pool_;
                Sink(Response& r, ::note::StringPool& pool) : rsp(r), pool_(pool) {
                }
                NOTE_SINK_NOINLINE void on_string(::note::string_view k_, ::note::string_view v_) {
                    v_ = pool_.intern(v_);
                    if (note::flash(keys_::rsp_mode) == k_) { rsp.mode = v_; return; }
                    if (note::flash(keys_::rsp_vseconds) == k_) { rsp.vseconds = v_; return; }
                }
                NOTE_SINK_NOINLINE void on_number(::note::string_view k_, ::note::string_view raw_) {
                    if (note::flash(keys_::rsp_max) == k_) { rsp.max = ::note::parse_int(raw_); return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = ::note::parse_int(raw_); return; }
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = ::note::parse_int(raw_); return; }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_threshold) == k_) { rsp.threshold = ::note::parse_int(raw_); return; }
#endif
                    if (note::flash(keys_::rsp_lat) == k_) { rsp.lat = ::note::parse_double(raw_); return; }
                    if (note::flash(keys_::rsp_lon) == k_) { rsp.lon = ::note::parse_double(raw_); return; }
                }
                NOTE_SINK_NOINLINE void on_int(::note::string_view k_, ::note::json_int_t v_) {
                    if (note::flash(keys_::rsp_max) == k_) { rsp.max = v_; return; }
                    if (note::flash(keys_::rsp_minutes) == k_) { rsp.minutes = v_; return; }
                    if (note::flash(keys_::rsp_seconds) == k_) { rsp.seconds = v_; return; }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                    if (note::flash(keys_::rsp_threshold) == k_) { rsp.threshold = v_; return; }
#endif
                }
                NOTE_SINK_NOINLINE void on_float(::note::string_view k_, double v_) {
                    if (note::flash(keys_::rsp_lat) == k_) { rsp.lat = v_; return; }
                    if (note::flash(keys_::rsp_lon) == k_) { rsp.lon = v_; return; }
                }
                NOTE_SINK_NOINLINE void reset() {
                    rsp = Response{};
                }
            };
#pragma GCC diagnostic pop

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
            void intern_strings(::note::StringPool& pool) {
                if (!mode.empty()) mode = pool.intern(mode);
                if (!vseconds.empty()) vseconds = pool.intern(vseconds);
            }
#pragma GCC diagnostic pop

#ifdef ARDUINO
            /// Arduino Printable: prints response fields to Serial or any Print stream.
            size_t printTo(Print& p) const {
                size_t n = p.print("{");
                bool first_ = true;
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"mode\":");
                n += note::detail::print_json_value(p, mode.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"vseconds\":");
                n += note::detail::print_json_value(p, vseconds.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"lat\":");
                n += note::detail::print_json_value(p, lat.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"lon\":");
                n += note::detail::print_json_value(p, lon.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"max\":");
                n += note::detail::print_json_value(p, max.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"minutes\":");
                n += note::detail::print_json_value(p, minutes.value());
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"seconds\":");
                n += note::detail::print_json_value(p, seconds.value());
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
                if (!first_) n += p.print(",");
                first_ = false;
                n += p.print("\"threshold\":");
                n += note::detail::print_json_value(p, threshold.value());
#endif
                n += p.print("}");
                return n;
            }
#endif

#if !NOTE_NO_JSON_TREE
        private:
            std::unique_ptr<JsonReader> reader_;
#endif
        };

#if NOTE_SINGLETON
        /// Send this request to the Notecard and wait for a response.
        /// Returns an ApiResult<Response> — boolean-convertible to true on success;
        /// dereference (or use member-of-pointer ->) to read response fields,
        /// or call .error() to inspect the ErrorInfo on failure.
        ApiResult<Response> execute() const;
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const;
#else
        ApiResult<Response>(*execute_fn_)(void*, const CardLocationMode::Remove&) = nullptr;
        Result<void>(*send_fn_)(void*, BuildFn, void*) = nullptr;
        /// Send this request to the Notecard and wait for a response.
        auto execute() const { return execute_fn_(nc_, *this); }
        /// Send this request as a fire-and-forget command (cmd) — the Notecard
        /// processes it without sending a response. Lower power and bandwidth
        /// than execute() when you don't need the result.
        Result<void> command() const {
            auto build_ = [&](JsonBuilder& b_) {
                b_.add("cmd", notecard_request);
                this->build(b_);
            };
            BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
                (*static_cast<decltype(build_)*>(p_))(b_);
            };
            return send_fn_(nc_, fn_, &build_);
        }
#endif

        private:
        void build(JsonBuilder& b) const;
        public:


#ifdef ARDUINO
        /// Arduino Printable: prints the JSON request to Serial or any Print stream.
        size_t printTo(Print& p) const {
            size_t n = p.print("{\"req\":\"");
            n += p.print(notecard_request.data());
            n += p.print("\"");
            if (lat) {
                n += p.print(",\"lat\":");
                n += note::detail::print_json_value(p, *lat);
            }
            if (lon) {
                n += p.print(",\"lon\":");
                n += note::detail::print_json_value(p, *lon);
            }
            if (max) {
                n += p.print(",\"max\":");
                n += note::detail::print_json_value(p, *max);
            }
            if (minutes) {
                n += p.print(",\"minutes\":");
                n += note::detail::print_json_value(p, *minutes);
            }
            if (mode) {
                n += p.print(",\"mode\":");
                n += note::detail::print_json_value(p, *mode);
            }
            if (seconds) {
                n += p.print(",\"seconds\":");
                n += note::detail::print_json_value(p, *seconds);
            }
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            if (threshold) {
                n += p.print(",\"threshold\":");
                n += note::detail::print_json_value(p, *threshold);
            }
#endif
            if (vseconds) {
                n += p.print(",\"vseconds\":");
                n += note::detail::print_json_value(p, *vseconds);
            }
            n += p.print("}");
            return n;
        }
#endif

        private:
        friend class ::note::Notecard;
        template<typename> friend class ::note::StaticNotecard;
#if NOTE_NO_POLYMORPHIC || __cplusplus < 202002L
        template<typename> friend class ::note::Api;
#else
        template<typename, typename> friend class ::note::Api;
#endif
        public:

    };
    using Delete = Remove;  // legacy alias
};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline CardLocationMode::Get& CardLocationMode::Get::delete_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Get, delete_));
}
inline CardLocationMode::Get& CardLocationMode::Get::lat_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Get, lat));
}
inline CardLocationMode::Get& CardLocationMode::Get::lon_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Get, lon));
}
inline CardLocationMode::Get& CardLocationMode::Get::max_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Get, max));
}
inline CardLocationMode::Get& CardLocationMode::Get::minutes_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Get, minutes));
}
inline CardLocationMode::Get& CardLocationMode::Get::mode_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Get, mode));
}
inline CardLocationMode::Get& CardLocationMode::Get::seconds_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Get, seconds));
}
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
inline CardLocationMode::Get& CardLocationMode::Get::threshold_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Get, threshold));
}
#endif
inline CardLocationMode::Get& CardLocationMode::Get::vseconds_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Get*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Get, vseconds));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardLocationMode::Get> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardLocationMode::Get::keys_::rsp_mode, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get::Response, mode)), ::note::FieldType::String},
        {::note::api::CardLocationMode::Get::keys_::rsp_vseconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get::Response, vseconds)), ::note::FieldType::String},
        {::note::api::CardLocationMode::Get::keys_::rsp_lat, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get::Response, lat)), ::note::FieldType::Double},
        {::note::api::CardLocationMode::Get::keys_::rsp_lon, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get::Response, lon)), ::note::FieldType::Double},
        {::note::api::CardLocationMode::Get::keys_::rsp_max, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get::Response, max)), ::note::FieldType::Int},
        {::note::api::CardLocationMode::Get::keys_::rsp_minutes, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get::Response, minutes)), ::note::FieldType::Int},
        {::note::api::CardLocationMode::Get::keys_::rsp_seconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get::Response, seconds)), ::note::FieldType::Int},
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        {::note::api::CardLocationMode::Get::keys_::rsp_threshold, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get::Response, threshold)), ::note::FieldType::Int},
#endif
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardLocationMode::Get::keys_::delete_, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get, delete_)), ::note::ReqFieldType::Bool},
            {::note::api::CardLocationMode::Get::keys_::lat, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get, lat)), ::note::ReqFieldType::Double},
            {::note::api::CardLocationMode::Get::keys_::lon, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get, lon)), ::note::ReqFieldType::Double},
            {::note::api::CardLocationMode::Get::keys_::max, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get, max)), ::note::ReqFieldType::Int},
            {::note::api::CardLocationMode::Get::keys_::minutes, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get, minutes)), ::note::ReqFieldType::Int},
            {::note::api::CardLocationMode::Get::keys_::mode, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get, mode)), ::note::ReqFieldType::String},
            {::note::api::CardLocationMode::Get::keys_::seconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get, seconds)), ::note::ReqFieldType::Int},
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            {::note::api::CardLocationMode::Get::keys_::threshold, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get, threshold)), ::note::ReqFieldType::Int},
#endif
            {::note::api::CardLocationMode::Get::keys_::vseconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Get, vseconds)), ::note::ReqFieldType::String},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline void CardLocationMode::Get::build(JsonBuilder& b) const {
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
#endif
    using meta_ = ::note::detail::request_traits<CardLocationMode::Get>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}
#pragma GCC diagnostic pop

#if NOTE_SINGLETON
inline ApiResult<typename CardLocationMode::Get::Response> CardLocationMode::Get::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardLocationMode::Get>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    // The type-erased thunk couldn't reach `attach_allocator`. Mark the
    // Response as owning its interned strings so its dtor frees them on
    // the SINGLETON path (parity with the Notecard::execute template path,
    // which attaches via `detail::attach_allocator`). The dtor still gates
    // the actual free on `g_singleton_allocator_present`, so marking when
    // no allocator is configured stays safe.
    rsp_.alloc_.reset(::note::detail::g_singleton_allocator);
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardLocationMode::Get::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardLocationMode::Get>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline CardLocationMode::Set& CardLocationMode::Set::delete_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Set, delete_));
}
inline CardLocationMode::Set& CardLocationMode::Set::lat_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Set, lat));
}
inline CardLocationMode::Set& CardLocationMode::Set::lon_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Set, lon));
}
inline CardLocationMode::Set& CardLocationMode::Set::max_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Set, max));
}
inline CardLocationMode::Set& CardLocationMode::Set::minutes_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Set, minutes));
}
inline CardLocationMode::Set& CardLocationMode::Set::mode_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Set, mode));
}
inline CardLocationMode::Set& CardLocationMode::Set::seconds_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Set, seconds));
}
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
inline CardLocationMode::Set& CardLocationMode::Set::threshold_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Set, threshold));
}
#endif
inline CardLocationMode::Set& CardLocationMode::Set::vseconds_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Set*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Set, vseconds));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardLocationMode::Set> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardLocationMode::Set::keys_::rsp_mode, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set::Response, mode)), ::note::FieldType::String},
        {::note::api::CardLocationMode::Set::keys_::rsp_vseconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set::Response, vseconds)), ::note::FieldType::String},
        {::note::api::CardLocationMode::Set::keys_::rsp_lat, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set::Response, lat)), ::note::FieldType::Double},
        {::note::api::CardLocationMode::Set::keys_::rsp_lon, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set::Response, lon)), ::note::FieldType::Double},
        {::note::api::CardLocationMode::Set::keys_::rsp_max, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set::Response, max)), ::note::FieldType::Int},
        {::note::api::CardLocationMode::Set::keys_::rsp_minutes, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set::Response, minutes)), ::note::FieldType::Int},
        {::note::api::CardLocationMode::Set::keys_::rsp_seconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set::Response, seconds)), ::note::FieldType::Int},
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        {::note::api::CardLocationMode::Set::keys_::rsp_threshold, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set::Response, threshold)), ::note::FieldType::Int},
#endif
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardLocationMode::Set::keys_::delete_, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set, delete_)), ::note::ReqFieldType::Bool},
            {::note::api::CardLocationMode::Set::keys_::lat, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set, lat)), ::note::ReqFieldType::Double},
            {::note::api::CardLocationMode::Set::keys_::lon, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set, lon)), ::note::ReqFieldType::Double},
            {::note::api::CardLocationMode::Set::keys_::max, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set, max)), ::note::ReqFieldType::Int},
            {::note::api::CardLocationMode::Set::keys_::minutes, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set, minutes)), ::note::ReqFieldType::Int},
            {::note::api::CardLocationMode::Set::keys_::mode, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set, mode)), ::note::ReqFieldType::String},
            {::note::api::CardLocationMode::Set::keys_::seconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set, seconds)), ::note::ReqFieldType::Int},
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            {::note::api::CardLocationMode::Set::keys_::threshold, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set, threshold)), ::note::ReqFieldType::Int},
#endif
            {::note::api::CardLocationMode::Set::keys_::vseconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Set, vseconds)), ::note::ReqFieldType::String},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline void CardLocationMode::Set::build(JsonBuilder& b) const {
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
#endif
    using meta_ = ::note::detail::request_traits<CardLocationMode::Set>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}
#pragma GCC diagnostic pop

#if NOTE_SINGLETON
inline ApiResult<typename CardLocationMode::Set::Response> CardLocationMode::Set::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardLocationMode::Set>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    // The type-erased thunk couldn't reach `attach_allocator`. Mark the
    // Response as owning its interned strings so its dtor frees them on
    // the SINGLETON path (parity with the Notecard::execute template path,
    // which attaches via `detail::attach_allocator`). The dtor still gates
    // the actual free on `g_singleton_allocator_present`, so marking when
    // no allocator is configured stays safe.
    rsp_.alloc_.reset(::note::detail::g_singleton_allocator);
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardLocationMode::Set::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardLocationMode::Set>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
inline CardLocationMode::Continuous& CardLocationMode::Continuous::threshold_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Continuous*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Continuous, threshold));
}
#endif
inline CardLocationMode::Continuous& CardLocationMode::Continuous::vseconds_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Continuous*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Continuous, vseconds));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardLocationMode::Continuous> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardLocationMode::Continuous::keys_::rsp_mode, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Continuous::Response, mode)), ::note::FieldType::String},
        {::note::api::CardLocationMode::Continuous::keys_::rsp_vseconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Continuous::Response, vseconds)), ::note::FieldType::String},
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        {::note::api::CardLocationMode::Continuous::keys_::rsp_threshold, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Continuous::Response, threshold)), ::note::FieldType::Int},
#endif
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            {::note::api::CardLocationMode::Continuous::keys_::threshold, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Continuous, threshold)), ::note::ReqFieldType::Int},
#endif
            {::note::api::CardLocationMode::Continuous::keys_::vseconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Continuous, vseconds)), ::note::ReqFieldType::String},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline void CardLocationMode::Continuous::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::mode), "continuous");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
#endif
    using meta_ = ::note::detail::request_traits<CardLocationMode::Continuous>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}
#pragma GCC diagnostic pop

#if NOTE_SINGLETON
inline ApiResult<typename CardLocationMode::Continuous::Response> CardLocationMode::Continuous::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardLocationMode::Continuous>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    // The type-erased thunk couldn't reach `attach_allocator`. Mark the
    // Response as owning its interned strings so its dtor frees them on
    // the SINGLETON path (parity with the Notecard::execute template path,
    // which attaches via `detail::attach_allocator`). The dtor still gates
    // the actual free on `g_singleton_allocator_present`, so marking when
    // no allocator is configured stays safe.
    rsp_.alloc_.reset(::note::detail::g_singleton_allocator);
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardLocationMode::Continuous::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardLocationMode::Continuous>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline CardLocationMode::Periodic& CardLocationMode::Periodic::lat_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Periodic*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Periodic, lat));
}
inline CardLocationMode::Periodic& CardLocationMode::Periodic::lon_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Periodic*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Periodic, lon));
}
inline CardLocationMode::Periodic& CardLocationMode::Periodic::max_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Periodic*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Periodic, max));
}
inline CardLocationMode::Periodic& CardLocationMode::Periodic::minutes_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Periodic*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Periodic, minutes));
}
inline CardLocationMode::Periodic& CardLocationMode::Periodic::seconds_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Periodic*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Periodic, seconds));
}
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
inline CardLocationMode::Periodic& CardLocationMode::Periodic::threshold_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Periodic*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Periodic, threshold));
}
#endif
inline CardLocationMode::Periodic& CardLocationMode::Periodic::vseconds_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Periodic*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Periodic, vseconds));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardLocationMode::Periodic> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardLocationMode::Periodic::keys_::rsp_mode, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic::Response, mode)), ::note::FieldType::String},
        {::note::api::CardLocationMode::Periodic::keys_::rsp_vseconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic::Response, vseconds)), ::note::FieldType::String},
        {::note::api::CardLocationMode::Periodic::keys_::rsp_lat, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic::Response, lat)), ::note::FieldType::Double},
        {::note::api::CardLocationMode::Periodic::keys_::rsp_lon, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic::Response, lon)), ::note::FieldType::Double},
        {::note::api::CardLocationMode::Periodic::keys_::rsp_max, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic::Response, max)), ::note::FieldType::Int},
        {::note::api::CardLocationMode::Periodic::keys_::rsp_minutes, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic::Response, minutes)), ::note::FieldType::Int},
        {::note::api::CardLocationMode::Periodic::keys_::rsp_seconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic::Response, seconds)), ::note::FieldType::Int},
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        {::note::api::CardLocationMode::Periodic::keys_::rsp_threshold, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic::Response, threshold)), ::note::FieldType::Int},
#endif
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardLocationMode::Periodic::keys_::lat, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic, lat)), ::note::ReqFieldType::Double},
            {::note::api::CardLocationMode::Periodic::keys_::lon, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic, lon)), ::note::ReqFieldType::Double},
            {::note::api::CardLocationMode::Periodic::keys_::max, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic, max)), ::note::ReqFieldType::Int},
            {::note::api::CardLocationMode::Periodic::keys_::minutes, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic, minutes)), ::note::ReqFieldType::Int},
            {::note::api::CardLocationMode::Periodic::keys_::seconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic, seconds)), ::note::ReqFieldType::Int},
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            {::note::api::CardLocationMode::Periodic::keys_::threshold, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic, threshold)), ::note::ReqFieldType::Int},
#endif
            {::note::api::CardLocationMode::Periodic::keys_::vseconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Periodic, vseconds)), ::note::ReqFieldType::String},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline void CardLocationMode::Periodic::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::mode), "periodic");
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
#endif
    using meta_ = ::note::detail::request_traits<CardLocationMode::Periodic>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}
#pragma GCC diagnostic pop

#if NOTE_SINGLETON
inline ApiResult<typename CardLocationMode::Periodic::Response> CardLocationMode::Periodic::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardLocationMode::Periodic>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    // The type-erased thunk couldn't reach `attach_allocator`. Mark the
    // Response as owning its interned strings so its dtor frees them on
    // the SINGLETON path (parity with the Notecard::execute template path,
    // which attaches via `detail::attach_allocator`). The dtor still gates
    // the actual free on `g_singleton_allocator_present`, so marking when
    // no allocator is configured stays safe.
    rsp_.alloc_.reset(::note::detail::g_singleton_allocator);
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardLocationMode::Periodic::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardLocationMode::Periodic>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardLocationMode::Fixed& CardLocationMode::Fixed::lat_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Fixed*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Fixed, lat));
}
inline CardLocationMode::Fixed& CardLocationMode::Fixed::lon_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Fixed*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Fixed, lon));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardLocationMode::Fixed> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardLocationMode::Fixed::keys_::rsp_mode, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Fixed::Response, mode)), ::note::FieldType::String},
        {::note::api::CardLocationMode::Fixed::keys_::rsp_lat, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Fixed::Response, lat)), ::note::FieldType::Double},
        {::note::api::CardLocationMode::Fixed::keys_::rsp_lon, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Fixed::Response, lon)), ::note::FieldType::Double},
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardLocationMode::Fixed::keys_::lat, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Fixed, lat)), ::note::ReqFieldType::Double},
            {::note::api::CardLocationMode::Fixed::keys_::lon, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Fixed, lon)), ::note::ReqFieldType::Double},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

inline void CardLocationMode::Fixed::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::mode), "fixed");
    using meta_ = ::note::detail::request_traits<CardLocationMode::Fixed>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}

#if NOTE_SINGLETON
inline ApiResult<typename CardLocationMode::Fixed::Response> CardLocationMode::Fixed::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardLocationMode::Fixed>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    // The type-erased thunk couldn't reach `attach_allocator`. Mark the
    // Response as owning its interned strings so its dtor frees them on
    // the SINGLETON path (parity with the Notecard::execute template path,
    // which attaches via `detail::attach_allocator`). The dtor still gates
    // the actual free on `g_singleton_allocator_present`, so marking when
    // no allocator is configured stays safe.
    rsp_.alloc_.reset(::note::detail::g_singleton_allocator);
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardLocationMode::Fixed::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardLocationMode::Fixed>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline CardLocationMode::Remove& CardLocationMode::Remove::lat_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Remove*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Remove, lat));
}
inline CardLocationMode::Remove& CardLocationMode::Remove::lon_t::operator()(double v) {
    Field<double>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Remove*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Remove, lon));
}
inline CardLocationMode::Remove& CardLocationMode::Remove::max_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Remove*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Remove, max));
}
inline CardLocationMode::Remove& CardLocationMode::Remove::minutes_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Remove*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Remove, minutes));
}
inline CardLocationMode::Remove& CardLocationMode::Remove::mode_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Remove*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Remove, mode));
}
inline CardLocationMode::Remove& CardLocationMode::Remove::seconds_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Remove*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Remove, seconds));
}
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
inline CardLocationMode::Remove& CardLocationMode::Remove::threshold_t::operator()(note::json_int_t v) {
    Field<note::json_int_t>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Remove*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Remove, threshold));
}
#endif
inline CardLocationMode::Remove& CardLocationMode::Remove::vseconds_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardLocationMode::Remove*>(
        reinterpret_cast<char*>(this) - offsetof(CardLocationMode::Remove, vseconds));
}
#pragma GCC diagnostic pop

} // namespace note::api
namespace note::detail {
template<>
struct request_traits<::note::api::CardLocationMode::Remove> {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
    static constexpr ::note::FieldDesc field_descs_table_[] NOTE_FLASH_ATTR = {
        {::note::api::CardLocationMode::Remove::keys_::rsp_mode, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove::Response, mode)), ::note::FieldType::String},
        {::note::api::CardLocationMode::Remove::keys_::rsp_vseconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove::Response, vseconds)), ::note::FieldType::String},
        {::note::api::CardLocationMode::Remove::keys_::rsp_lat, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove::Response, lat)), ::note::FieldType::Double},
        {::note::api::CardLocationMode::Remove::keys_::rsp_lon, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove::Response, lon)), ::note::FieldType::Double},
        {::note::api::CardLocationMode::Remove::keys_::rsp_max, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove::Response, max)), ::note::FieldType::Int},
        {::note::api::CardLocationMode::Remove::keys_::rsp_minutes, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove::Response, minutes)), ::note::FieldType::Int},
        {::note::api::CardLocationMode::Remove::keys_::rsp_seconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove::Response, seconds)), ::note::FieldType::Int},
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
        {::note::api::CardLocationMode::Remove::keys_::rsp_threshold, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove::Response, threshold)), ::note::FieldType::Int},
#endif
    };
#pragma GCC diagnostic pop
    static constexpr uint8_t field_count = sizeof(field_descs_table_) / sizeof(field_descs_table_[0]);
    static const ::note::FieldDesc* field_descs_ptr() { return field_descs_table_; }
    static const ::note::ReqFieldDesc* req_field_descs_ptr_(uint8_t& n_out) {
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
        static constexpr ::note::ReqFieldDesc table_[] NOTE_FLASH_ATTR = {
            {::note::api::CardLocationMode::Remove::keys_::lat, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove, lat)), ::note::ReqFieldType::Double},
            {::note::api::CardLocationMode::Remove::keys_::lon, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove, lon)), ::note::ReqFieldType::Double},
            {::note::api::CardLocationMode::Remove::keys_::max, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove, max)), ::note::ReqFieldType::Int},
            {::note::api::CardLocationMode::Remove::keys_::minutes, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove, minutes)), ::note::ReqFieldType::Int},
            {::note::api::CardLocationMode::Remove::keys_::mode, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove, mode)), ::note::ReqFieldType::String},
            {::note::api::CardLocationMode::Remove::keys_::seconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove, seconds)), ::note::ReqFieldType::Int},
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
            {::note::api::CardLocationMode::Remove::keys_::threshold, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove, threshold)), ::note::ReqFieldType::Int},
#endif
            {::note::api::CardLocationMode::Remove::keys_::vseconds, static_cast<uint16_t>(offsetof(::note::api::CardLocationMode::Remove, vseconds)), ::note::ReqFieldType::String},
        };
#pragma GCC diagnostic pop
        n_out = sizeof(table_) / sizeof(table_[0]);
        return table_;
    }
#if NOTE_SINGLETON
    static inline ::note::Result<void>(*execute_generic_fn_)(void*, ::note::string_view, ::note::BuildFn, void*, void*, const ::note::FieldDesc*, uint8_t, ::note::detail::NcErrorCapture&, bool&, void*, ::note::BodyHandlerFactory, ::note::Safety) = nullptr;
    static inline ::note::Result<void>(*send_fn_)(void*, ::note::BuildFn, void*) = nullptr;
#endif
};
} // namespace note::detail
namespace note::api {

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
inline void CardLocationMode::Remove::build(JsonBuilder& b) const {
    note::add_flash(b, note::flash(keys_::delete_), true);
#if NOTE_API_VERSION >= NOTE_VERSION(3, 4, 1) || !defined(NOTE_API_STRICT)
#endif
    using meta_ = ::note::detail::request_traits<CardLocationMode::Remove>;
    uint8_t n_; auto* descs_ = meta_::req_field_descs_ptr_(n_);
    ::note::generic_build(b, this, descs_, n_);
#if NOTE_EXTRAS
    for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
        std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                   extras_[i_].value);
#endif
}
#pragma GCC diagnostic pop

#if NOTE_SINGLETON
inline ApiResult<typename CardLocationMode::Remove::Response> CardLocationMode::Remove::execute() const {
    auto build_ = [&](JsonBuilder& b_) { this->build(b_); };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) { (*static_cast<decltype(build_)*>(p_))(b_); };
    Response rsp_{};
    ::note::detail::NcErrorCapture nc_err_;
    bool exhausted_ = false;
    using meta_ = ::note::detail::request_traits<CardLocationMode::Remove>;
    auto rv_ = meta_::execute_generic_fn_(::note::detail::api_nc_singleton_, notecard_request, fn_, &build_, &rsp_, meta_::field_descs_ptr(), meta_::field_count, nc_err_, exhausted_, nullptr, nullptr, safety);
    if (!rv_) return ::note::Unexpected(rv_.error());
    if (!nc_err_.empty()) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Notecard, ::note::Cause::Unspecified, nc_err_.view()});
    if (exhausted_) return ApiResult<Response>(::note::ErrorInfo{::note::Error::Overflow, ::note::Cause::Unspecified, NOTE_ERR("arena exhausted")});
    // The type-erased thunk couldn't reach `attach_allocator`. Mark the
    // Response as owning its interned strings so its dtor frees them on
    // the SINGLETON path (parity with the Notecard::execute template path,
    // which attaches via `detail::attach_allocator`). The dtor still gates
    // the actual free on `g_singleton_allocator_present`, so marking when
    // no allocator is configured stays safe.
    rsp_.alloc_.reset(::note::detail::g_singleton_allocator);
    return ApiResult<Response>(std::move(rsp_));
}
inline Result<void> CardLocationMode::Remove::command() const {
    auto build_ = [&](JsonBuilder& b_) {
        b_.add("cmd", notecard_request);
        this->build(b_);
    };
    BuildFn fn_ = [](JsonBuilder& b_, void* p_) {
        (*static_cast<decltype(build_)*>(p_))(b_);
    };
    using meta_ = ::note::detail::request_traits<CardLocationMode::Remove>;
    return meta_::send_fn_(::note::detail::api_nc_singleton_, fn_, &build_);
}
#endif


} // namespace note::api
