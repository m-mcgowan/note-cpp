// AUTO-GENERATED by tools/codegen/generate.py -- DO NOT EDIT
#pragma once

#include <note/dyn_field.hpp>
#include <note/field.hpp>
#include <note/json.hpp>
#include <note/notecard.hpp>
#include <note/safety.hpp>
#include <note/types.hpp>

namespace note::api {







struct CardSleep {
    static constexpr string_view notecard_request = "card.sleep";
    static constexpr bool supports_cmd = true;
    static constexpr Safety safety = Safety::Idempotent;

    Notecard* nc_ = nullptr;

    /// Set to `"accel"` to wake from deep sleep on any movement detected by the
    /// onboard accelerometer. Set to `"-accel"` to reset to the default
    /// setting.
    // mode: accel | -accel
    struct mode_t : Field<note::string_view> {
        using Field<note::string_view>::Field;
        using Field<note::string_view>::operator=;
        CardSleep& operator()(note::string_view v);
    } mode{};
    /// Set to `true` to disable the sleep mode on Notecard.
    struct off_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardSleep& operator()(bool v);
    } off{};
    /// Set to `true` to enable Notecard to sleep once it is idle for >= 30
    /// seconds.
    struct on_t : Field<bool> {
        using Field<bool>::Field;
        using Field<bool>::operator=;
        CardSleep& operator()(bool v);
    } on{};
    /// The number of seconds the Notecard will wait before entering sleep mode
    /// (minimum value is 30).
    struct seconds_t : Field<int32_t> {
        using Field<int32_t>::Field;
        using Field<int32_t>::operator=;
        CardSleep& operator()(int32_t v);
    } seconds{};

    // consteval: only callable at compile time
    static consteval note::string_view validatedMode(const char* v) {
        note::string_view sv{v};
        if (sv != "accel" && sv != "-accel")
            throw "card.sleep: invalid value for 'mode'";
        return sv;
    }

    template<typename T>
    auto& extra(note::string_view key, T value) {
        if (extras_count_ < NOTE_EXTRAS_MAX)
            extras_[extras_count_++] = {key, note::DynValue{value}};
        return *this;
    }
    auto& extra(note::string_view key, const char* value) {
        return extra(key, note::string_view{value});
    }

    note::DynField operator[](note::string_view k_) {
        if (k_ == "mode") return note::dyn_field_for(mode);
        if (k_ == "off") return note::dyn_field_for(off);
        if (k_ == "on") return note::dyn_field_for(on);
        if (k_ == "seconds") return note::dyn_field_for(seconds);
        if (extras_count_ < NOTE_EXTRAS_MAX) {
            auto& slot = extras_[extras_count_++];
            slot.key = k_;
            return note::dyn_field_for(slot.value);
        }
        return {};
    }

    std::array<note::detail::ExtraSlot, NOTE_EXTRAS_MAX> extras_{};
    uint8_t extras_count_ = 0;

    struct Response {
        /// Returns `"accel"` if the Notecard is configured to wake from deep
        /// sleep on any movement detected by the onboard accelerometer.
        note::string_view mode{};
        /// `true` if sleep mode is disabled.
        bool off{};
        /// `true` if sleep mode is enabled.
        bool on{};
        /// The number of seconds the Notecard will wait before entering sleep
        /// mode (only included if default settings are overridden).
        int32_t seconds{};

        static Response parse(std::unique_ptr<JsonReader> reader_) {
            Response rsp;
            rsp.mode = reader_->get_string("mode");
            rsp.off = reader_->get_bool("off");
            rsp.on = reader_->get_bool("on");
            rsp.seconds = reader_->get_int("seconds");
            rsp.reader_ = std::move(reader_);
            return rsp;
        }

    private:
        std::unique_ptr<JsonReader> reader_;
    };

    void build(JsonBuilder& b) const {
        if (mode) b.add("mode", *mode);
        if (off) b.add("off", *off);
        if (on) b.add("on", *on);
        if (seconds) b.add("seconds", *seconds);
        for (uint8_t i_ = 0; i_ < extras_count_; ++i_)
            std::visit([&](auto&& v_) { b.add(extras_[i_].key, v_); },
                       extras_[i_].value);
    }

    auto execute() const { return nc_->execute(*this); }
    auto execute(Notecard& nc) const { return nc.execute(*this); }
    Result<void> command() const { return nc_->command_typed(*this); }
    Result<void> command(Notecard& nc) const { return nc.command_typed(*this); }

};

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winvalid-offsetof"
inline CardSleep& CardSleep::mode_t::operator()(note::string_view v) {
    Field<note::string_view>::operator=(v);
    return *reinterpret_cast<CardSleep*>(
        reinterpret_cast<char*>(this) - offsetof(CardSleep, mode));
}
inline CardSleep& CardSleep::off_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardSleep*>(
        reinterpret_cast<char*>(this) - offsetof(CardSleep, off));
}
inline CardSleep& CardSleep::on_t::operator()(bool v) {
    Field<bool>::operator=(v);
    return *reinterpret_cast<CardSleep*>(
        reinterpret_cast<char*>(this) - offsetof(CardSleep, on));
}
inline CardSleep& CardSleep::seconds_t::operator()(int32_t v) {
    Field<int32_t>::operator=(v);
    return *reinterpret_cast<CardSleep*>(
        reinterpret_cast<char*>(this) - offsetof(CardSleep, seconds));
}
#pragma GCC diagnostic pop


} // namespace note::api
